{
  "id": "ec51f39b508db3e9d4f7a4f07916ba18733cd95678e53488edbbc94911d06af8",
  "source_uri": ".planning/phases/46-firecrawl-context-integration/46-01-PLAN.md",
  "type": "internal",
  "content_markdown": "---\nphase: 46\nplan: 01\ntype: execute\nwave: 1\ndepends_on: []\nfiles_modified:\n\n  - get-stuff-done/bin/lib/context-schema.cjs\n  - get-stuff-done/bin/lib/firecrawl-client.cjs\n  - tests/firecrawl-crawl.test.cjs\n\nautonomous: true\nrequirements:\n\n  - FIRECRAWL-CONTROL-02\n  - CONTEXT-UNIFY-01\n\nmust_haves:\n  truths:\n\n    - \"Unified context specification schema is defined and exported from context-schema.cjs\"\n    - \"Firecrawl client has crawl(spec) and getArtifact(id) methods that correctly forward to Firecrawl service\"\n    - \"Unit tests verify crawl validation and API dispatch behavior\"\n  artifacts:\n\n    - path: \"get-stuff-done/bin/lib/context-schema.cjs\"\n      provides: \"Zod schema for unified context spec\"\n      contains: \"crawlSpecSchema\"\n\n    - path: \"get-stuff-done/bin/lib/firecrawl-client.cjs\"\n      provides: \"crawl() and getArtifact() methods\"\n      contains: [\"async crawl(spec)\", \"async getArtifact(id)\"]\n\n    - path: \"tests/firecrawl-crawl.test.cjs\"\n      provides: \"Unit tests for crawl and getArtifact\"\n      contains: \"test('crawl validates spec'\"\n  key_links:\n\n    - from: \"firecrawl-client.cjs:crawl\"\n      to: \"context-schema.cjs:crawlSpecSchema\"\n      via: \"validate using crawlSpecSchema.parse\"\n      pattern: \"crawlSpecSchema\\\\.parse\"\n\n    - from: \"firecrawl-client.cjs:crawl\"\n      to: \"Firecrawl service /v1/context/crawl\"\n      via: \"_request('crawl', 'context/crawl', spec)\"\n      pattern: \"_request\\\\('crawl', 'context/crawl'\"\n---\n\n<objective>\nCreate the client-side foundation for unified context retrieval: define the specification schema and extend the Firecrawl client with methods to submit a crawl spec and fetch individual artifacts. This enables all GSD agents to use a single API for context instead of ad-hoc file reads and separate searches.\n</objective>\n\n<execution_context>\n@~/.claude/get-stuff-done/workflows/execute-plan.md\n@~/.claude/get-stuff-done/templates/summary.md\n</execution_context>\n\n<context>\n.planning/ROADMAP.md\n.planning/STATE.md\n.planning/phases/46-firecrawl-context-integration/46-RESEARCH.md\nget-stuff-done/bin/lib/firecrawl-client.cjs\nget-stuff-done/bin/lib/artifact-schema.cjs\n\n<interfaces>\nFrom get-stuff-done/bin/lib/firecrawl-client.cjs (current):\n\n```javascript\nclass FirecrawlClient {\n  async scrape(url)\n  async search(query)\n  async extract(url, schema)\n  async map(url)\n  async check()\n}\n```\n\nFrom get-stuff-done/bin/lib/artifact-schema.cjs:\n\n```javascript\nconst contextArtifactSchema = z.object({\n  id: z.string(),\n  source_uri: z.string(),\n  title: z.string().optional(),\n  type: z.enum(['external', 'internal']),\n  content_markdown: z.string(),\n  content_hash: z.string(),\n  normalized_at: z.string(),\n  provenance: z.object({\n    producer: z.enum(['firecrawl', 'internal-normalizer', 'firecrawl-normalizer']),\n    producer_version: z.string(),\n    parameters_hash: z.string().nullable()\n  }),\n  is_sanctioned: z.boolean().default(false)\n});\n```\n\n</interfaces>\n\n</context>\n\n<tasks>\n\n<task type=\"auto\">\n  <name>Task 1: Create context-schema.cjs with unified spec definition</name>\n  <files>\n    get-stuff-done/bin/lib/context-schema.cjs\n  </files>\n  <read_first>\n\n    - get-stuff-done/bin/lib/artifact-schema.cjs\n  </read_first>\n  <action>\n    Create a new module `get-stuff-done/bin/lib/context-schema.cjs` that exports a Zod schema named `crawlSpecSchema` for the unified context specification. The schema must:\n\n    - Require a `sources` property: an array of non-empty strings (minimum 1 item).\n    - Allow an optional `options` object with optional fields:\n      - `extract_schema` (any, for structured extraction),\n      - `filter_globs` (array of strings),\n      - `max_total_bytes` (positive integer),\n      - `timeout_ms` (positive integer),\n      - `normalize` (boolean).\n    Use consistent style with artifact-schema.cjs (const z = require('zod');). Also export the raw schema object.\n  </action>\n  <verify>\n    <automated>\n      node -c get-stuff-done/bin/lib/context-schema.cjs &&\n      grep -q \"crawlSpecSchema\" get-stuff-done/bin/lib/context-schema.cjs &&\n      node -e \"const {crawlSpecSchema}=require('./get-stuff-done/bin/lib/context-schema.cjs'); crawlSpecSchema.parse({sources:['a']});\" &&\n      node -e \"const {crawlSpecSchema}=require('./get-stuff-done/bin/lib/context-schema.cjs'); try { crawlSpecSchema.parse({}); process.exit(1) } catch { process.exit(0) }\"\n    </automated>\n  </verify>\n  <done>Context schema module created and validated.</done>\n</task>\n\n<task type=\"auto\">\n  <name>Task 2: Extend firecrawl-client.cjs with crawl and getArtifact</name>\n  <files>\n    get-stuff-done/bin/lib/firecrawl-client.cjs\n  </files>\n  <read_first>\n\n    - get-stuff-done/bin/lib/firecrawl-client.cjs\n    - get-stuff-done/bin/lib/context-schema.cjs\n  </read_first>\n  <action>\n    Modify `get-stuff-done/bin/lib/firecrawl-client.cjs`:\n\n    1. Add `const { crawlSpecSchema } = require('./context-schema.cjs');` near the top after other requires.\n    2. Inside the `FirecrawlClient` class, add:\n       - `async crawl(spec)`:\n         * Validate `spec` via `crawlSpecSchema.parse(spec)`.\n         * Call `this._request('crawl', 'context/crawl', validatedSpec)` and return the result.\n       - `async getArtifact(id)`:\n         * Validate `id` is a non-empty string; if invalid throw Error('Invalid artifact ID').\n         * Call `this._request('getArtifact', 'artifacts/get', { id })` and return the result.\n    3. Preserve existing methods; do not alter audit/logging behavior.\n  </action>\n  <verify>\n    <automated>\n      grep -q \"async crawl(spec)\" get-stuff-done/bin/lib/firecrawl-client.cjs &&\n      grep -q \"async getArtifact(id)\" get-stuff-done/bin/lib/firecrawl-client.cjs &&\n      grep -q \"crawlSpecSchema.parse\" get-stuff-done/bin/lib/firecrawl-client.cjs &&\n      node -e \"const c=require('./get-stuff-done/bin/lib/firecrawl-client.cjs'); if (typeof c.crawl !== 'function' || typeof c.getArtifact !== 'function') process.exit(1); console.log('ok');\"\n    </automated>\n  </verify>\n  <done>Client methods implemented with proper validation and request dispatch.</done>\n</task>\n\n<task type=\"auto\">\n  <name>Task 3: Add unit tests for crawl and getArtifact</name>\n  <files>\n    tests/firecrawl-crawl.test.cjs\n  </files>\n  <read_first>\n\n    - tests/artifact-schema.test.cjs\n    - get-stuff-done/bin/lib/firecrawl-client.cjs\n  </read_first>\n  <action>\n    Create `tests/firecrawl-crawl.test.cjs` using `node:test` and `assert`:\n\n    - Mock FirecrawlClient._request to avoid real network.\n    - Test that `crawl()` with invalid spec (missing sources) throws a Zod validation error.\n    - Test that `crawl()` with valid spec calls `_request` with correct action, endpoint, and parsed spec.\n    - Test that `getArtifact(id)` validates non-empty ID and calls `_request` with action 'getArtifact', endpoint 'artifacts/get', body `{id}`.\n    - Ensure tests isolate require.cache if needed.\n  </action>\n  <verify>\n    <automated>\n      node -c tests/firecrawl-crawl.test.cjs &&\n      npm test -- tests/firecrawl-crawl.test.cjs\n    </automated>\n  </verify>\n  <done>Unit tests created and passing.</done>\n</task>\n\n</tasks>\n\n<verification>\n\n- context-schema.cjs exists and exports crawlSpecSchema.\n- firecrawl-client.cjs includes crawl and getArtifact with validation.\n- Unit tests for new methods run and pass.\n\n</verification>\n\n<success_criteria>\n\n- Agents can construct a valid unified context spec and call client.crawl(spec) to retrieve normalized artifacts.\n- client.getArtifact(id) allows fetching individual artifacts by ID.\n- The Firecrawl service will be ready to accept the /v1/context/crawl endpoint.\n\n</success_criteria>\n\n<output>\nAfter completion, create `.planning/phases/46-firecrawl-context-integration/46-01-SUMMARY.md`\n</output>\n",
  "content_hash": "f9e726419e1beba86c6c690c1fd20926c95f79907ddd3a85dd7225d02ce71ca4",
  "normalized_at": "2026-03-29T18:41:58.008Z",
  "provenance": {
    "producer": "internal-normalizer",
    "producer_version": "1.1.0",
    "parameters_hash": null
  }
}
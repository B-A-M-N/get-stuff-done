{
  "id": "0cc6e9ffe259868f58250a20e10a10622f89948cff1c39986c402301e3495f1c",
  "source_uri": ".planning/phases/51-enforcement-restoration/51-01-PLAN.md",
  "type": "internal",
  "content_markdown": "---\nwave: 1\ndepends_on: []\nfiles_modified:\n\n  - get-stuff-done/bin/lib/state.cjs\n  - get-stuff-done/workflows/execute-phase.md\n  - get-stuff-done/workflows/plan-phase.md\n  - get-stuff-done/workflows/autonomous.md\n  - get-stuff-done/bin/lib/verify.cjs\n\nrequirements:\n\n  - ENFORCE-09\n  - ENFORCE-10\n\nautonomous: true\n---\n\n# Plan 51-01: Pre-condition Checking & Phase Completeness\n\n## Task 1: Enhance cmdStateAssert with blocked and checkpoint checks\n\n<read_first>\n\n- get-stuff-done/bin/lib/state.cjs\n\n</read_first>\n<acceptance_criteria>\n\n- `cmdStateAssert` validates PROJECT.md exists at cwd\n- `cmdStateAssert` checks STATE.md for `clarification_status: blocked` and fails if present\n- `cmdStateAssert` scans all phase directories for CHECKPOINT.md with frontmatter `status: awaiting-response` and fails if found\n- `node --test tests/state-assert.test.cjs` passes (create this test)\n\n</acceptance_criteria>\n<action>\nIn `get-stuff-done/bin/lib/state.cjs`:\n\n1. Modify `cmdStateAssert(cwd, raw)` (around line 733):\n   - Add check: `PROJECT.md` must exist at cwd (not just in .planning)\n   - After parsing STATE.md frontmatter, check `clarification_status` field; if value is `blocked`, add to errors\n   - Add checkpoint check: scan `$cwd/.planning/phases/*/CHECKPOINT.md` (glob pattern) for files with frontmatter status `awaiting-response`\n     - Use `extractFrontmatter` to read status from each checkpoint\n     - If any checkpoint has `status === 'awaiting-response'`, add error: \"Pending checkpoint requires user response — resolve with /gsd:resume-project\"\n   - Keep existing checks (STATE.md exists/parseable, not paused, config.json exists, orphaned .planning/CHECKPOINT.md warning)\n   - Exit 1 if errors exist; output JSON with passed/errors/warnings\n\n2. Ensure function is exported in `module.exports` (already present at line 1308)\n\n</action>\n\n## Task 2: Wire state assert into all workflows (standardize pre-condition gate)\n\n<read_first>\n\n- get-stuff-done/workflows/execute-phase.md\n- get-stuff-done/workflows/plan-phase.md\n- get-stuff-done/workflows/autonomous.md\n\n</read_first>\n<acceptance_criteria>\n\n- `execute-phase.md` has `state assert` at step \"assert_preconditions\" before any phase operations (already present at line 85-100)\n- `plan-phase.md` has `state assert` at step \"assert_preconditions\" before research/planning (already present at line 84-92)\n- `autonomous.md` has `state assert` call at start of \"execute_phase\" step, BEFORE any phase-specific operations\n- Manual `clarification_status` checks become redundant; workflows rely on `state assert` to fail fast\n- Grep confirms: no duplicate manual `clarification_status` blocks immediately before state assert (allow removal of old blocks in follow-up cleanup)\n\n</acceptance_criteria>\n<action>\n\n1. In `get-stuff-done/workflows/autonomous.md`:\n   - In step \"execute_phase\" (starting line 117), BEFORE any `init phase-op` calls or phase operations, insert:\n     ```bash\n     node \"$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs\" state assert\n     ```\n\n   - Remove the existing manual Clarification Gate that checks `clarification_status` (lines 142-153) — `state assert` now handles this\n   - Keep the check for `has_context` and other phase-specific logic after state assert\n\n2. Verify `execute-phase.md` already has `state assert` at line 89 in step \"assert_preconditions\" (no change needed)\n3. Verify `plan-phase.md` already has `state assert` at line 86-87 in step \"assert_preconditions\" (no change needed)\n\n</action>\n\n## Task 3: Ensure verify phase-completeness uses live disk scan and fails on missing summaries\n\n<read_first>\n\n- get-stuff-done/bin/lib/verify.cjs\n- get-stuff-done/workflows/execute-phase.md\n\n</read_first>\n<acceptance_criteria>\n\n- `cmdVerifyPhaseCompleteness` reads filesystem directly (does not rely on STATE.md counters)\n- Compares count of `-PLAN.md` vs `-SUMMARY.md` files in phase directory\n- Exits non-zero if any `-SUMMARY.md` missing (incomplete plans list)\n- `node --test tests/phase-completeness.test.cjs` passes (create this test)\n- `execute-phase.md` calls `verify phase-completeness` BEFORE `verify_phase_goal` (already true at line 511)\n\n</acceptance_criteria>\n<action>\n\n1. In `get-stuff-done/bin/lib/verify.cjs`:\n   - Verify `cmdVerifyPhaseCompleteness` implementation (line 821-866) already uses `fs.readdirSync` to list actual files and compares sets of plan IDs vs summary IDs\n   - Ensure it does NOT reference STATE.md counters; only filesystem counts\n   - Confirm it returns `complete: errors.length === 0` and fails on missing summaries\n   - No changes needed if implementation matches; otherwise adjust to use direct file listing\n\n2. In `get-stuff-done/workflows/execute-phase.md`:\n   - Verify `verify phase-completeness` step (lines 507-529) is BEFORE `verify_phase_goal` step (line 531)\n   - No changes needed if order is correct\n\n3. Consider adding `verify phase-completeness` to `plan-phase.md` after plan creation to validate summaries exist before marking phase ready? (Not strictly required; execute-phase already enforces)\n\n</action>\n\n## Verification Criteria\n\n- [ ] `gsd-tools state assert --cwd .` exits 0 on valid project with no blocked status and no awaiting-response checkpoints\n- [ ] `gsd-tools state assert` exits 1 when STATE.md has `clarification_status: blocked`\n- [ ] `gsd-tools state assert` exits 1 when any phase directory has CHECKPOINT.md with `status: awaiting-response`\n- [ ] All workflows (execute-phase, plan-phase, autonomous) call `state assert` before any phase operations\n- [ ] `verify phase-completeness` fails if SUMMARY.md missing, even if STATE.md counters might be incorrect\n- [ ] `node --test` passes for new tests: `tests/state-assert.test.cjs`, `tests/phase-completeness.test.cjs`\n",
  "content_hash": "18736aa9bc9bf15ca7569426cab01eb45b07c8740ad2dd1714a4d6358d63e416",
  "normalized_at": "2026-03-28T03:46:21.448Z",
  "provenance": {
    "producer": "internal-normalizer",
    "producer_version": "1.1.0",
    "parameters_hash": null
  }
}
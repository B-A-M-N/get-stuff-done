{
  "id": "c36245f83176eb3317b44e9f5c4350ee889e179038580c6f7657074ae674c1fb",
  "source_uri": ".planning/phases/55-open-brain-v1-foundations/55-04-PLAN.md",
  "type": "internal",
  "content_markdown": "---\nphase: 55-open-brain-v1-foundations\nplan: 04\ntype: execute\nwave: 4\ndepends_on:\n\n  - 55-01\n  - 55-03\n\nfiles_modified:\n\n  - bin/install.js\n  - get-stuff-done/workflows/update.md\n  - tests/install-isolation.test.cjs\n  - tests/open-brain-degraded-mode.test.cjs\n\nautonomous: true\nrequirements:\n\n  - OPEN-BRAIN-06\n\ngap_closure: true\nmust_haves:\n  truths:\n\n    - \"Running `node \\\"$HOME/.codex/get-shit-done/bin/gsd-tools.cjs\\\" brain open-status --raw` succeeds on a repaired Codex install and reports Open Brain sidecar readiness without claiming Second Brain execution truth moved.\"\n    - \"The shipped Codex operator path no longer fails with `Unknown command: brain` when the user invokes the documented Open Brain status command from a legacy `~/.codex/get-shit-done` entrypoint.\"\n    - \"Codex install and update flows repair the legacy `~/.codex/get-shit-done` operator entrypoint only to restore the Phase 55 `brain open-status` surface, not to broaden packaging scope.\"\n    - \"The operator-facing Open Brain status surface continues to degrade cleanly when embeddings or storage are unavailable.\"\n  artifacts:\n\n    - path: \"bin/install.js\"\n      provides: \"Codex legacy-install repair logic that writes a compatibility CLI shim pointing at `dostuff/get-stuff-done/bin/gsd-tools.cjs`.\"\n\n    - path: \"get-stuff-done/workflows/update.md\"\n      provides: \"Update detection that recognizes both current Codex installs under `dostuff/get-stuff-done` and legacy installs under `get-shit-done` so the shipped `brain open-status` operator path can be repaired.\"\n\n    - path: \"tests/install-isolation.test.cjs\"\n      provides: \"Regression coverage that a Codex install creates the legacy shim and that the shim can execute `brain open-status --raw`.\"\n\n    - path: \"tests/open-brain-degraded-mode.test.cjs\"\n      provides: \"Focused Open Brain status assertions that preserve `schema`, `sidecar_only`, and `execution_truth_owner` truth fields.\"\n  key_links:\n\n    - from: \"bin/install.js\"\n      to: \".codex/get-shit-done/bin/gsd-tools.cjs\"\n      via: \"legacy Codex compatibility shim\"\n      pattern: \"get-shit-done/bin/gsd-tools\\\\.cjs|dostuff/get-stuff-done/bin/gsd-tools\\\\.cjs\"\n\n    - from: \"get-stuff-done/workflows/update.md\"\n      to: \"bin/install.js\"\n      via: \"legacy Codex marker detection and reinstall path\"\n      pattern: \"get-shit-done/VERSION|dostuff/get-stuff-done/VERSION|npx get-stuff-done-cc --codex --global\"\n\n    - from: \"tests/install-isolation.test.cjs\"\n      to: \"bin/install.js\"\n      via: \"installed shim smoke test\"\n      pattern: \"brain open-status --raw|get-shit-done/bin/gsd-tools\\\\.cjs\"\n---\n\n<objective>\nClose the surviving Phase 55 operator-surface gap by restoring a truthful Open Brain status command for the Codex-installed CLI path the user actually runs.\n\nPurpose: Phase 55 already implemented `brain open-status` in the repo copy, but the shipped Codex operator entrypoint can remain stale under `~/.codex/get-shit-done`, so the documented command `node \"$HOME/.codex/get-shit-done/bin/gsd-tools.cjs\" brain open-status --raw` fails with `Unknown command: brain`. This plan fixes only the operator-facing gap needed to keep the Open Brain sidecar observable and safely bounded under `OPEN-BRAIN-06`.\nOutput: A focused Codex gap-closure plan that repairs the legacy installed CLI route only where needed to restore `brain open-status`, teaches update flows how to detect that repairable operator path, and proves the repaired command returns truthful Open Brain readiness JSON.\nConstraint: This additive post-UAT plan does not redefine the original Phase 55 ingest/embed/retrieve/feedback success criteria; it closes an installed operator-surface defect that surfaced only after Phase 55 execution and verification.\n</objective>\n\n<execution_context>\n@/home/bamn/.codex/get-shit-done/workflows/execute-plan.md\n@/home/bamn/.codex/get-shit-done/templates/summary.md\n</execution_context>\n\n<context>\n@.planning/PROJECT.md\n@.planning/ROADMAP.md\n@.planning/REQUIREMENTS.md\n@.planning/STATE.md\n@.planning/phases/55-open-brain-v1-foundations/55-CONTEXT.md\n@.planning/phases/55-open-brain-v1-foundations/55-RESEARCH.md\n@.planning/phases/55-open-brain-v1-foundations/55-01-SUMMARY.md\n@.planning/phases/55-open-brain-v1-foundations/55-03-SUMMARY.md\n@bin/install.js\n@get-stuff-done/bin/gsd-tools.cjs\n@get-stuff-done/workflows/update.md\n@tests/install-isolation.test.cjs\n@tests/open-brain-degraded-mode.test.cjs\n\n<interfaces>\nGap truth to close in this additive plan:\n\n```text\nInstalled Codex operator path:\nnode \"$HOME/.codex/get-shit-done/bin/gsd-tools.cjs\" brain open-status --raw\n\nCurrent failure on stale legacy entrypoint:\nUnknown command: brain\n```\n\nFrom get-stuff-done/bin/gsd-tools.cjs:\n\n```javascript\ncase 'brain': {\n  const openBrain = require('./lib/open-brain.cjs');\n  const subcommand = args[1];\n  if (subcommand === 'open-status') {\n    const status = openBrain.checkAvailability();\n    process.stdout.write(JSON.stringify(status, null, 2) + '\\n');\n    process.exit(0);\n  }\n}\n```\n\nFrom bin/install.js:\n\n```javascript\nfunction install(isGlobal, runtime = 'claude') { ... }\nfunction writeManifest(configDir, runtime = 'claude') { ... }\nfunction finishInstall(settingsPath, settings, statuslineCommand, shouldInstallStatusline, runtime = 'claude', isGlobal = true) { ... }\n```\n\n</interfaces>\n</context>\n\n<tasks>\n\n<task type=\"auto\" id=\"55-04-01\" tdd=\"true\">\n  <name>Task 1: Repair legacy Codex installs so `get-shit-done` forwards to the real `brain open-status` CLI</name>\n  <files>bin/install.js, get-stuff-done/workflows/update.md, tests/install-isolation.test.cjs</files>\n  <read_first>\n\n- bin/install.js\n- get-stuff-done/workflows/update.md\n- tests/install-isolation.test.cjs\n- get-stuff-done/bin/gsd-tools.cjs\n  </read_first>\n  <acceptance_criteria>\n\n- `bin/install.js` contains the exact legacy Codex path fragment `get-shit-done/bin/gsd-tools.cjs`.\n- `bin/install.js` contains the exact canonical Codex engine path fragment `dostuff/get-stuff-done/bin/gsd-tools.cjs`.\n- `get-stuff-done/workflows/update.md` contains both `get-shit-done/VERSION` and `dostuff/get-stuff-done/VERSION` so update detection can repair either install shape when restoring the shipped `brain open-status` command.\n- `tests/install-isolation.test.cjs` asserts a Codex install creates `.codex/dostuff/get-stuff-done/bin/gsd-tools.cjs`.\n- `tests/install-isolation.test.cjs` asserts a Codex install creates `.codex/get-shit-done/bin/gsd-tools.cjs`.\n- `bin/install.js` writes a legacy Codex compatibility shim at `path.join(targetDir, 'get-shit-done', 'bin', 'gsd-tools.cjs')`.\n- The shim launches `path.join(targetDir, 'dostuff', 'get-stuff-done', 'bin', 'gsd-tools.cjs')` with `process.argv.slice(2)` and exits with the child status code.\n- `bin/install.js` also writes `path.join(targetDir, 'get-shit-done', 'VERSION')` with the current package version so workflow update detection has a concrete legacy marker to find.\n- `get-stuff-done/workflows/update.md` treats current Codex installs under `$HOME/.codex/dostuff/get-stuff-done` as canonical and legacy installs under `$HOME/.codex/get-shit-done` as repairable targets.\n  </acceptance_criteria>\n  <behavior>\n\n    - Test 1: `install(false, 'codex')` creates the canonical engine under `.codex/dostuff/get-stuff-done`.\n    - Test 2: The same install writes a legacy compatibility launcher under `.codex/get-shit-done/bin/gsd-tools.cjs`.\n    - Test 3: Update detection can recognize both `dostuff/get-stuff-done/VERSION` and `get-shit-done/VERSION`.\n  </behavior>\n  <action>\nWrite the Codex installer regression in `tests/install-isolation.test.cjs` first. Add a new Codex-specific test that runs `install(false, 'codex')` inside a temp project and asserts these exact files exist afterward so the Phase 55 operator status command has a valid installed route:\n\n- `.codex/dostuff/get-stuff-done/bin/gsd-tools.cjs`\n- `.codex/get-shit-done/bin/gsd-tools.cjs`\n- `.codex/get-shit-done/VERSION`\n\nThen update `bin/install.js` with a dedicated helper such as `ensureLegacyCodexCliShim(targetDir)` and call it from the `runtime === 'codex'` install path after the canonical engine copy succeeds but before `writeManifest(targetDir, runtime)`. The helper must:\n\n- `fs.mkdirSync(path.join(targetDir, 'get-shit-done', 'bin'), { recursive: true })`\n- write `path.join(targetDir, 'get-shit-done', 'VERSION')` with `pkg.version`\n- write `path.join(targetDir, 'get-shit-done', 'bin', 'gsd-tools.cjs')` as a Node launcher with a shebang that imports `node:path` and `node:child_process`, resolves `path.join(targetDir, 'dostuff', 'get-stuff-done', 'bin', 'gsd-tools.cjs')`, runs `spawnSync(process.execPath, [realCli, ...process.argv.slice(2)], { stdio: 'inherit' })`, and exits with `result.status ?? 1`\n\nUpdate `get-stuff-done/workflows/update.md` so both the local and global install detection blocks check these exact marker families for Codex only as repair markers for the shipped `brain open-status` surface:\n\n- `./.codex/dostuff/get-stuff-done/VERSION`\n- `./.codex/dostuff/get-stuff-done/workflows/update.md`\n- `./.codex/get-shit-done/VERSION`\n- `./.codex/get-shit-done/workflows/update.md`\n- the same four `$HOME/...` paths for global detection\n\nDo not change the canonical install target away from `dostuff/get-stuff-done`; the gap closure is a repair path for stale legacy operator installs so Phase 55's operator-facing Open Brain status remains usable, not a rollback to the legacy layout or a broad packaging rewrite.\n  </action>\n  <verify>\n    <automated>node --test tests/install-isolation.test.cjs</automated>\n  </verify>\n  <done>A Codex install repairs the legacy `get-shit-done` operator path while keeping `dostuff/get-stuff-done` as the canonical engine root.</done>\n</task>\n\n<task type=\"auto\" id=\"55-04-02\" tdd=\"true\">\n  <name>Task 2: Prove the repaired installed operator path returns truthful Open Brain readiness JSON</name>\n  <files>tests/install-isolation.test.cjs, tests/open-brain-degraded-mode.test.cjs</files>\n  <read_first>\n\n- tests/install-isolation.test.cjs\n- tests/open-brain-degraded-mode.test.cjs\n- get-stuff-done/bin/gsd-tools.cjs\n- get-stuff-done/bin/lib/open-brain.cjs\n- bin/install.js\n  </read_first>\n  <acceptance_criteria>\n\n- `tests/install-isolation.test.cjs` contains the exact command fragment `brain open-status --raw`.\n- `tests/install-isolation.test.cjs` contains the exact legacy installed path fragment `.codex/get-shit-done/bin/gsd-tools.cjs`.\n- `tests/install-isolation.test.cjs` asserts JSON keys `schema`, `sidecar_only`, `execution_truth_owner`, and `blocked`.\n- `tests/open-brain-degraded-mode.test.cjs` still asserts `schema === 'gsd_open_brain'`.\n- `tests/open-brain-degraded-mode.test.cjs` still asserts `execution_truth_owner === 'second_brain'`.\n- The new installed-path smoke test runs `node .codex/get-shit-done/bin/gsd-tools.cjs brain open-status --raw` after `install(false, 'codex')` and expects exit code `0`.\n- The smoke test clears ambient `PGHOST`, `PGPORT`, `PGDATABASE`, `PGUSER`, `PGPASSWORD`, and `DATABASE_URL` and sets `GSD_MEMORY_MODE=sqlite` so the status check proves degraded Open Brain truth instead of depending on live local Postgres credentials.\n- The parsed JSON must include `\"schema\": \"gsd_open_brain\"`, `\"sidecar_only\": true`, `\"execution_truth_owner\": \"second_brain\"`, and `\"blocked\": false`.\n  </acceptance_criteria>\n  <behavior>\n\n    - Test 1: The installed legacy shim can execute the same `brain open-status --raw` subcommand that passes in the repo-local test suite.\n    - Test 2: The returned JSON keeps Open Brain clearly separate from Second Brain execution truth.\n    - Test 3: The operator surface remains truthful in degraded mode when embeddings are unavailable locally.\n  </behavior>\n  <action>\nExtend `tests/install-isolation.test.cjs` with an operator smoke test that uses a temporary local Codex install, then spawns this exact command from the temp project root to prove the shipped Phase 55 status surface works from the installed entrypoint:\n\n`node .codex/get-shit-done/bin/gsd-tools.cjs brain open-status --raw`\n\nRun it with:\n\n- `cwd` set to the temp project root\n- `encoding: 'utf-8'`\n- `GSD_MEMORY_MODE=sqlite`\n- `PGHOST=''`\n- `PGPORT=''`\n- `PGDATABASE=''`\n- `PGUSER=''`\n- `PGPASSWORD=''`\n- `DATABASE_URL=''`\n\nParse the output as JSON and assert these exact truth fields:\n\n- `schema === 'gsd_open_brain'`\n- `sidecar_only === true`\n- `execution_truth_owner === 'second_brain'`\n- `blocked === false`\n\nKeep `tests/open-brain-degraded-mode.test.cjs` as the repo-local truth contract for `brain open-status --raw`, but update it only if the installed-shim path reveals a missing assertion that should also hold for the repo-local entrypoint. Do not broaden the test to live Postgres or embedding dependencies; the point is to prove the operator surface is truthful and bounded even when Open Brain is degraded.\n  </action>\n  <verify>\n    <automated>node --test tests/install-isolation.test.cjs tests/open-brain-degraded-mode.test.cjs</automated>\n  </verify>\n  <done>The installed legacy Codex operator path executes `brain open-status --raw` successfully and returns the same bounded Open Brain truth shape as the repo-local CLI.</done>\n</task>\n\n<task type=\"auto\" id=\"55-04-03\">\n  <name>Task 3: Verify the Codex repair path, Open Brain status truth, and installer syntax</name>\n  <files>bin/install.js, get-stuff-done/workflows/update.md, tests/install-isolation.test.cjs, tests/open-brain-degraded-mode.test.cjs</files>\n  <read_first>\n\n- bin/install.js\n- get-stuff-done/workflows/update.md\n- tests/install-isolation.test.cjs\n- tests/open-brain-degraded-mode.test.cjs\n  </read_first>\n  <acceptance_criteria>\n\n- `node --check bin/install.js` exits 0.\n- `node --check get-stuff-done/bin/gsd-tools.cjs` exits 0.\n- `node --test tests/install-isolation.test.cjs tests/open-brain-degraded-mode.test.cjs` exits 0.\n- `rg -n \"get-shit-done/VERSION|dostuff/get-stuff-done/VERSION\" get-stuff-done/workflows/update.md` returns matches.\n  </acceptance_criteria>\n  <action>\nRun focused verification after implementation:\n\n- `node --check bin/install.js`\n- `node --check get-stuff-done/bin/gsd-tools.cjs`\n- `node --test tests/install-isolation.test.cjs tests/open-brain-degraded-mode.test.cjs`\n- `rg -n \"get-shit-done/VERSION|dostuff/get-stuff-done/VERSION\" get-stuff-done/workflows/update.md`\n\nIf any check fails, fix the Codex repair path before considering this gap closed. Do not skip the grep check; the update workflow must explicitly advertise both marker families so future repairs of the shipped `brain open-status` surface stay truthful without widening beyond this operator-surface fix.\n  </action>\n  <verify>\n    <automated>node --check bin/install.js && node --check get-stuff-done/bin/gsd-tools.cjs && node --test tests/install-isolation.test.cjs tests/open-brain-degraded-mode.test.cjs && rg -n \"get-shit-done/VERSION|dostuff/get-stuff-done/VERSION\" get-stuff-done/workflows/update.md</automated>\n  </verify>\n  <done>The Codex compatibility repair is syntax-safe, covered by focused tests, and discoverable through the update workflow.</done>\n</task>\n\n</tasks>\n\n<verification>\n\n- `node --check bin/install.js`\n- `node --check get-stuff-done/bin/gsd-tools.cjs`\n- `node --test tests/install-isolation.test.cjs tests/open-brain-degraded-mode.test.cjs`\n- `rg -n \"get-shit-done/VERSION|dostuff/get-stuff-done/VERSION\" get-stuff-done/workflows/update.md`\n\n</verification>\n\n<success_criteria>\nPhase 55 gap closure is satisfied when the installed Codex operator path can run `brain open-status`, any legacy install repair work is clearly in service of that shipped operator surface, and the returned status remains truthful about Open Brain being a sidecar separate from Second Brain execution truth.\n</success_criteria>\n\n<output>\nAfter completion, create `.planning/phases/55-open-brain-v1-foundations/55-04-SUMMARY.md`\n</output>\n",
  "content_hash": "c2610e47f19ba6de91ddef2fa35c1e4a959e9105b8875b68622df18f5724c960",
  "normalized_at": "2026-03-28T03:46:21.236Z",
  "provenance": {
    "producer": "internal-normalizer",
    "producer_version": "1.1.0",
    "parameters_hash": null
  }
}
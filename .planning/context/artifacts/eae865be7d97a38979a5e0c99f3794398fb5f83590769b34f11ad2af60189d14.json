{
  "id": "eae865be7d97a38979a5e0c99f3794398fb5f83590769b34f11ad2af60189d14",
  "source_uri": ".planning/phases/51-enforcement-restoration/51-03-PLAN.md",
  "type": "internal",
  "content_markdown": "---\nwave: 1\ndepends_on:\n\n  - 51-01\n\nfiles_modified:\n\n  - get-stuff-done/bin/lib/state.cjs\n  - get-stuff-done/bin/lib/config.cjs\n  - get-stuff-done/workflows/new-project.md\n  - commands/gsd/new-project.md\n  - docs/CONFIGURATION.md\n  - get-stuff-done/bin/gsd-tools.cjs\n  - get-stuff-done/workflows/progress.md\n\nrequirements:\n\n  - STATE-01\n  - STATE-02\n  - CONFIG-01\n  - METRICS-01\n\nautonomous: true\n---\n\n# Plan 51-03: State Improvements, Config Cleanup, and Metrics\n\n**Goal:** Restore state management integrity by adding deduplication, explicit pause/resume with continuation files, cleaning up dead configuration keys, and making performance metrics queryable.\n\n## Task 1: STATE-01 - Deduplication\n\n<read_first>\n\n- get-stuff-done/bin/lib/state.cjs\n\n</read_first>\n\n<acceptance_criteria>\n\n- `cmdStateAddDecision` computes a case-insensitive hash (deterministic function) of the normalized decision text (summary + rationale, trimmed, lowercased)\n- The function parses existing decisions from the Decisions section, computes hash for each, and compares against hash of new entry\n- If hash match found, prevents duplicate and returns `{added: false, reason: 'duplicate decision — already recorded'}`\n- Maintains backward compatibility: behavior identical from user perspective\n- No performance degradation (expected O(n) over small set)\n\n</acceptance_criteria>\n\n<action>\nIn `get-stuff-done/bin/lib/state.cjs`, within `cmdStateAddDecision`:\n\n1. Replace existing string-equality check with hash-based comparison:\n   - Implement `function computeHash(str)` using djb2 (or similar):\n     ```javascript\n     function computeHash(str) {\n       let hash = 5381;\n       for (let i = 0; i < str.length; i++) {\n         hash = ((hash << 5) + hash) + str.charCodeAt(i); // hash * 33 + c\n       }\n       return hash >>> 0; // ensure unsigned\n     }\n     ```\n\n   - Normalize the new entry: `const normalized = entry.trim().toLowerCase(); const newHash = computeHash(normalized);`\n   - For each existing entry line, normalize and compute its hash; store in a Set for O(1) lookup. If `newHash` exists in set, treat as duplicate.\n2. Keep the same output and early return behavior.\n\n</action>\n\n## Task 2: STATE-02 - Explicit Pause/Resume with `.continue-here`\n\n<read_first>\n\n- get-stuff-done/bin/lib/state.cjs\n- get-stuff-done/bin/gsd-tools.cjs\n\n</read_first>\n\n<acceptance_criteria>\n\n- `cmdStatePause(cwd, reason, raw)`:\n  * Writes `.continue-here` JSON file at project root containing: `{ phase: <current_phase_id>, reason: <string>, timestamp: <ISO>, state_snippet: <excerpt from STATE.md body (first 500 chars)> }`\n  * Updates STATE.md frontmatter to include `status: paused` and `paused_at: <ISO>` using frontmatter utilities\n  * Clears auto-chain flag: calls `setConfigValue(cwd, 'workflow._auto_chain_active', false)`\n  * Returns JSON with `paused: true` and file path\n- `cmdStateResume(cwd, options, raw)`:\n  * Accepts `--clear` flag via `options.clear`\n  * Reads `.continue-here` if exists and outputs its contents (as JSON) before clearing\n  * Removes `status: paused` and `paused_at` from STATE.md frontmatter\n  * If `options.clear` true, deletes `.continue-here` after successful resume\n  * Returns JSON with `resumed: true` and previous context if file existed\n- `gsd-tools` registers these commands with proper argument handling:\n  * `state pause [reason]` accepts reason as positional or `--reason`\n  * `state resume --clear` flag supported\n- Commands are idempotent (resuming without pause returns error/no-op)\n\n<action>\n\n- In `get-stuff-done/bin/lib/state.cjs`:\n  * Add helper `getCurrentPhase(cwd)` to read ROADMAP.md or STATE frontmatter to determine current phase.\n  * Modify `cmdStatePause(cwd, reason, raw)`:\n    - Parse STATE frontmatter and body. Determine phase via helper.\n    - Create `snippet` = body substring (first 500 chars) or extract \"Current Position\" section.\n    - Write `.continue-here` JSON with `{ phase, reason, timestamp: now, state_snippet }`.\n    - Update frontmatter: set `status: 'paused'` and `paused_at: now`. Use `extractFrontmatter`/`reconstructFrontmatter` to modify and write back.\n    - Clear `_auto_chain_active`: `const { setConfigValue } = require('./config.cjs'); setConfigValue(cwd, 'workflow._auto_chain_active', false);` (ignore output).\n  * Change `cmdStateResume` signature to `(cwd, options = {}, raw)`:\n    - Read `.continue-here`; if exists, store context and output (in raw mode, output JSON; otherwise print).\n    - Remove `status` and `paused_at` from frontmatter (delete keys).\n    - Write updated STATE.md.\n    - If `options.clear` and file exists, `fs.unlinkSync(continuePath)`.\n    - Output result.\n  * Add `cmdStateResume` to `module.exports`.\n- In `get-stuff-done/bin/gsd-tools.cjs`:\n  * For `case 'state'` and `subcommand === 'resume'`, parse `--clear` flag: `const clear = args.includes('--clear');` then call `state.cmdStateResume(cwd, { clear }, raw);`\n  * For `pause`, reason handling unchanged (reason taken from args[2] or `--reason` as before).\n- Add minimal error handling: if no `.continue-here` on resume, output warning but still clear paused status (or exit with error). Choose: if file missing, treat as warning but proceed to clear pause state.\n\n</action>\n\n## Task 3: CONFIG-01 - Configuration Cleanup and Documentation\n\n<read_first>\n\n- get-stuff-done/workflows/new-project.md\n- get-stuff-done/bin/lib/config.cjs\n- get-stuff-done/bin/lib/core.cjs\n- docs/CONFIGURATION.md\n\n</read_first>\n\n<acceptance_criteria>\n\n- New-project workflow no longer writes `mode` or `granularity` to `config.json`:\n  * Step 2a config JSON sample lacks these keys; `auto_advance` is placed under `workflow` object\n  * Step 5 interactive questions remove \"Mode\" and \"Granularity\" entirely; config example updated accordingly\n- `loadConfig()` (core.cjs) returns a `workflow` subobject containing `auto_advance` and other workflow flags, while preserving top-level keys for backward compatibility\n- `docs/CONFIGURATION.md` documents every valid config key with: key name, type, default, description, and deprecated status (mode, granularity marked deprecated)\n- Existing projects with legacy configs continue to work without breakage\n- `adversarial_test_harness` remains in `VALID_CONFIG_KEYS` and is properly implemented (no changes needed)\n\n<action>\n\n- In `get-stuff-done/workflows/new-project.md`:\n  * Step 2a: Remove `\"mode\": \"yolo\"` and `\"granularity\": \"[selected]\"` from config JSON sample. Ensure `\"workflow\": { \"auto_advance\": true }` is present.\n  * Step 5: Remove the entire \"Mode\" question block (four options). Remove or also delete the \"Granularity\" question (since it's deprecated). Update the subsequent \"Create `.planning/config.json`\" example to reflect only used keys (no mode, no granularity). Keep `workflow` with fields: `research`, `plan_check`, `verifier`, `auto_advance`, `nyquist_validation`, `adversarial_test_harness`, `ui_phase`, `ui_safety_gate`. Also include outer keys: `parallelization`, `commit_docs`, `model_profile`, `branching_strategy`, etc.\n  * Adjust any references to `granularity` in subsequent text (e.g., success criteria line about config.json contents).\n- In `get-stuff-done/bin/lib/core.cjs` (function `loadConfig`):\n  * After constructing the `config` object (just before the `// Apply log level` line), add:\n    ```javascript\n    // Provide nested workflow namespace for compatibility with dot-path config-get\n    config.workflow = {\n      auto_advance: config.auto_advance,\n      nyquist_validation: config.nyquist_validation,\n      adversarial_test_harness: config.adversarial_test_harness,\n      ui_phase: config.ui_phase,\n      ui_safety_gate: config.ui_safety_gate,\n      node_repair: config.node_repair,\n      node_repair_budget: config.node_repair_budget,\n      _auto_chain_active: config._auto_chain_active,\n    };\n    ```\n\n  * This ensures `config.workflow.auto_advance` exists while preserving top-level for legacy.\n- In `docs/CONFIGURATION.md`:\n  * Create or update to include a comprehensive table of configuration keys.\n  * For each key: name (e.g., `workflow.auto_advance`), type (boolean/number/string), default value, short description, and usage notes.\n  * Mark `mode` and `granularity` as deprecated (explain they are no longer written by new projects and will be removed in a future version).\n  * Group keys by category (Workflow, Git, Gates, Safety, etc.).\n- Optional: Consider deprecation warnings if mode/granularity are present in user config (could add in `loadConfig`). Not required for acceptance.\n\n</action>\n\n## Task 4: METRICS-01 - Parse and Expose Performance Metrics\n\n<read_first>\n\n- get-stuff-done/bin/lib/state.cjs\n- get-stuff-done/workflows/progress.md\n- get-stuff-done/bin/gsd-tools.cjs\n\n</read_first>\n\n<acceptance_criteria>\n\n- `parsePerformanceMetrics(content)` helper exists in state.cjs:\n  * Locates \"## Performance Metrics\" section\n  * Parses the \"By Phase\" markdown table into array `by_phase`: each entry `{ phase: number, plans: number, total: string, avg: string }`\n  * Parses individual plan metric lines (starting with `| Phase ...`) into array `plan_entries`: `{ phase_plan: string, duration: string, tasks: number|null, files: number|null }`\n  * Returns `{ by_phase, plan_entries }`\n- New command `state get-metrics` (`cmdStateGetMetrics`) implemented:\n  * Accepts optional flags `--phase <N>` and `--plan <M>` (plan number like \"P01\")\n  * Calls `parsePerformanceMetrics` on STATE.md\n  * Filters: for `by_phase`, exact match on `phase`; for `plan_entries`, matches where `phase_plan` includes the given plan (e.g., filter by \"Phase 23 P01\")\n  * Outputs filtered result as JSON (raw mode outputs raw JSON)\n- `gsd:progress` workflow displays performance metrics:\n  * In progress.md, a step fetches `gsd-tools state get-metrics --raw` and includes a \"## Performance Metrics\" section with by-phase table and recent plan entries (last 5)\n- Command registered in gsd-tools.cjs under `state` subcommand `get-metrics`\n- Changes do not break existing progress workflow behavior\n\n<action>\n\n- In `get-stuff-done/bin/lib/state.cjs`:\n  * Add `parsePerformanceMetrics(content)`:\n    - Use regex to find \"## Performance Metrics\" header and its content until next heading or EOF.\n    - Look for table: header line `| Phase | Plans | Total | Avg/Plan |` (or similar), separator, then rows. Split rows and convert numeric fields.\n    - Look for lines like `/^\\s*\\|\\s*Phase\\s+\\d+\\s+P\\d+\\s*\\|\\s*[\\d:]+\\s*\\|\\s*\\d+\\s+tasks\\s*\\|\\s*\\d+\\s+files\\s*\\|/` parse each into fields.\n    - Return `{ by_phase: [...], plan_entries: [...] }`.\n  * Add `cmdStateGetMetrics(cwd, options, raw)`:\n    - Read STATE.md (`safeReadFile`).\n    - If `!content`, output empty object.\n    - Call `parsePerformanceMetrics(content)`.\n    - Apply filters: if `options.phase` provided, filter `by_phase` to that phase, and filter `plan_entries` to those starting with `Phase ${options.phase}` (or containing \" Phase X \").\n    - If `options.plan` provided, further filter `plan_entries` to those containing `P${options.plan}` (normalize e.g., \"P01\" or \"1\").\n    - Output via `output(result, raw, JSON.stringify(result))`.\n  * Export `cmdStateGetMetrics` in `module.exports`.\n- In `get-stuff-done/bin/gsd-tools.cjs`:\n  * Inside `case 'state'`, add:\n    ```javascript\n    } else if (subcommand === 'get-metrics') {\n      const phaseIdx = args.indexOf('--phase');\n      const planIdx = args.indexOf('--plan');\n      state.cmdStateGetMetrics(cwd, {\n        phase: phaseIdx !== -1 ? args[phaseIdx + 1] : null,\n        plan: phaseIdx !== -1 ? args[planIdx + 1] : null,\n      }, raw);\n    }\n    ```\n\n- In `get-stuff-done/workflows/progress.md`:\n  * After step `report` (or within it), add a step to fetch metrics:\n    ```bash\n    METRICS=$(node \"$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs\" state get-metrics --raw)\n    ```\n\n  * Then include in the displayed report:\n    ```\n    ## Performance Metrics\n\n    **By Phase:**\n    | Phase | Plans | Total | Avg/Plan |\n    |-------|-------|-------|----------|\n    ... rows from $METRICS.by_phase ...\n\n    **Recent Plan Executions:**\n\n    - Phase 23 P01: 10m, 2 tasks, 3 files\n    ...\n    ```\n    (Use jq or simple formatting; within markdown we can embed the raw output or format nicely.)\n\n  * Ensure the step is idempotent: if no metrics, show \"No performance metrics recorded yet.\"\n- Add basic unit tests for `parsePerformanceMetrics` in `tests/` (create `tests/parse-metrics.test.cjs`) with sample STATE.md snippets covering both table and plan entries.\n\n</action>\n\n## Verification Criteria\n\nAfter implementation:\n\n- [ ] `gsd-tools state add-decision` prevents duplicates based on case-insensitive hash (test with same text in different cases)\n- [ ] `gsd-tools state pause` creates `.continue-here` with valid JSON, sets `status: paused` and `paused_at` in STATE.md frontmatter, and clears `_auto_chain_active`\n- [ ] `gsd-tools state resume` reads `.continue-here`, clears paused status; `--clear` removes continue file\n- [ ] `grep -E \"\\\"mode\\\"|\\\"granularity\\\"\"` in `get-stuff-done/workflows/new-project.md` shows no writes to config.json for these keys\n- [ ] `node -e \"const { loadConfig } = require('./get-stuff-done/bin/lib/core.cjs'); console.log(loadConfig(process.cwd()))\"` includes a `workflow` object with `auto_advance` property\n- [ ] `docs/CONFIGURATION.md` lists all keys with types, defaults, descriptions; marks `mode` and `granularity` as deprecated\n- [ ] `gsd-tools state get-metrics --raw` returns valid JSON with `by_phase` and `plan_entries` arrays\n- [ ] `/gsd:progress` output contains a \"Performance Metrics\" section with table data\n- [ ] All existing tests pass\n\n---\n\n## Notes\n\n- Deduplication hash: djb2 is sufficient; no need for cryptographic strength. Determinism across Node versions is fine (32-bit int).\n- `.continue-here` should be overwritten on each pause; same filename is fine.\n- The `workflow` namespace in `loadConfig` is additive; existing code using top-level keys (e.g., `config.auto_advance`) remains functional.\n- Parsing of Performance Metrics should be tolerant of variations in markdown table formatting (pipes, alignment).\n- The existing `cmdStateAddDecision` already has deduplication via string comparison; this task upgrades it to hash-based to satisfy requirement specification.\n- The existing `cmdStatePause` and `cmdStateResume` need enhancement to use `.continue-here` and frontmatter status; current implementation only modifies body.\n",
  "content_hash": "20266ffa27b160a66a5ee8b2f08c2011b7236e287300153524ae62afbade241a",
  "normalized_at": "2026-03-29T18:41:57.817Z",
  "provenance": {
    "producer": "internal-normalizer",
    "producer_version": "1.1.0",
    "parameters_hash": null
  }
}
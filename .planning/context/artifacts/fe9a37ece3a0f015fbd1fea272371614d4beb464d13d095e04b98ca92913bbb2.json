{
  "id": "fe9a37ece3a0f015fbd1fea272371614d4beb464d13d095e04b98ca92913bbb2",
  "source_uri": ".planning/phases/53-second-brain-connection-fallback-hardening/53-01-PLAN.md",
  "type": "internal",
  "content_markdown": "---\nphase: 53-second-brain-connection-fallback-hardening\nplan: 01\ntype: execute\nwave: 1\ndepends_on: []\nfiles_modified:\n\n  - get-stuff-done/bin/lib/second-brain.cjs\n  - get-stuff-done/bin/lib/brain-manager.cjs\n  - get-stuff-done/bin/gsd-tools.cjs\n  - tests/second-brain-state.test.cjs\n  - tests/second-brain-lifecycle.test.cjs\n  - tests/second-brain-status.test.cjs\n  - tests/second-brain-postgres-optin.test.cjs\n  - tests/checkpoint-plane-sync.test.cjs\n  - tests/summary-plane-sync.test.cjs\n\nautonomous: true\nrequirements:\n\n  - BRAIN-OPS-01\n  - BRAIN-OPS-02\n  - BRAIN-OPS-03\n\nmust_haves:\n  truths:\n\n    - \"Normal commands continue on Postgres auth, pool, or connectivity failure by switching to SQLite once and surfacing the degraded reason.\"\n    - \"Repeated degraded operations in the same process do not repeat the same user-facing warning.\"\n    - \"`brain status` reports the actual configured backend, active backend, degraded flag, degraded reason, warning emission state, and memory-critical block state.\"\n    - \"`brain health` exposes the same backend truth plus detailed diagnostics and a short degraded runbook.\"\n    - \"Repeated teardown and reinitialization in tests does not reuse an ended pool or exhaust local connections.\"\n    - \"Explicit Postgres-required execution fails hard instead of silently using SQLite, and Postgres integration coverage only runs when opted in.\"\n  artifacts:\n\n    - path: \"get-stuff-done/bin/lib/second-brain.cjs\"\n      provides: \"Authoritative backend state machine, one-time degraded warning, idempotent close/reset helpers, and Postgres-required guard.\"\n\n    - path: \"get-stuff-done/bin/lib/brain-manager.cjs\"\n      provides: \"Concise status surface and detailed health surface built from Second Brain state.\"\n\n    - path: \"get-stuff-done/bin/gsd-tools.cjs\"\n      provides: \"`brain status` and upgraded `brain health` CLI routes.\"\n\n    - path: \"tests/second-brain-state.test.cjs\"\n      provides: \"Regression coverage for degraded reason classification and one-time warning emission.\"\n\n    - path: \"tests/second-brain-lifecycle.test.cjs\"\n      provides: \"Regression coverage for idempotent close/reset and ended-pool non-reuse.\"\n\n    - path: \"tests/second-brain-status.test.cjs\"\n      provides: \"Regression coverage for status, health, and degraded runbook output.\"\n\n    - path: \"tests/second-brain-postgres-optin.test.cjs\"\n      provides: \"Explicit opt-in Postgres integration coverage gate.\"\n  key_links:\n\n    - from: \"get-stuff-done/bin/lib/second-brain.cjs\"\n      to: \"get-stuff-done/bin/lib/brain-manager.cjs\"\n      via: \"structured backend snapshot methods\"\n      pattern: \"getBackendState|requirePostgres|checkHealth|getStatus\"\n\n    - from: \"get-stuff-done/bin/gsd-tools.cjs\"\n      to: \"get-stuff-done/bin/lib/brain-manager.cjs\"\n      via: \"`brain status` and `brain health` command handlers\"\n      pattern: \"case 'brain'|subcommand === 'status'|subcommand === 'health'\"\n\n    - from: \"tests/checkpoint-plane-sync.test.cjs\"\n      to: \"get-stuff-done/bin/lib/second-brain.cjs\"\n      via: \"safe teardown helper\"\n      pattern: \"secondBrain\\\\.close\\\\(|secondBrain\\\\.resetForTests\\\\(\"\n\n    - from: \"tests/summary-plane-sync.test.cjs\"\n      to: \"get-stuff-done/bin/lib/second-brain.cjs\"\n      via: \"safe teardown helper\"\n      pattern: \"secondBrain\\\\.close\\\\(|secondBrain\\\\.resetForTests\\\\(\"\n---\n\n<objective>\nHarden the Second Brain connection lifecycle so Postgres failure degrades once, visibly, and truthfully to SQLite without turning normal commands or local tests into warning spam.\n\nPurpose: Stabilize the backend before Phase 54 introduces model-facing memory through MCP.\nOutput: One authoritative backend-state implementation, truthful operator surfaces, explicit Postgres-required blocking, and deterministic regression coverage.\n</objective>\n\n<execution_context>\n@/home/bamn/.codex/get-shit-done/workflows/execute-plan.md\n@/home/bamn/.codex/get-shit-done/templates/summary.md\n</execution_context>\n\n<context>\n@.planning/PROJECT.md\n@.planning/ROADMAP.md\n@.planning/STATE.md\n@.planning/REQUIREMENTS.md\n@.planning/phases/53-second-brain-connection-fallback-hardening/53-CONTEXT.md\n@.planning/phases/53-second-brain-connection-fallback-hardening/53-RESEARCH.md\n@docs/ARCHITECTURE-V0.4.0.md\n@get-stuff-done/bin/lib/second-brain.cjs\n@get-stuff-done/bin/lib/brain-manager.cjs\n@get-stuff-done/bin/gsd-tools.cjs\n@tests/checkpoint-plane-sync.test.cjs\n@tests/summary-plane-sync.test.cjs\n@tests/second-brain.test.cjs\n@tests/second-brain-grant.test.cjs\n\n<interfaces>\nCurrent CLI surface in `get-stuff-done/bin/gsd-tools.cjs`:\n\n```javascript\ncase 'brain': {\n  const brainManager = require('./lib/brain-manager.cjs');\n  const subcommand = args[1];\n  if (subcommand === 'health') {\n    const status = await brainManager.checkHealth();\n    process.stdout.write(JSON.stringify(status, null, 2) + '\\n');\n    process.exit(status.allOk ? 0 : 1);\n  } else {\n    error('Unknown brain subcommand. Available: health');\n  }\n  break;\n}\n```\n\nCurrent health entry point in `get-stuff-done/bin/lib/brain-manager.cjs`:\n\n```javascript\nasync checkHealth() {\n  const status = {\n    postgres: 'unknown',\n    rabbitmq: 'unknown',\n    planningServer: 'unknown',\n    allOk: false\n  };\n  // Postgres probe uses secondBrain.pool.connect()\n}\n```\n\nLocked backend-state contract from `53-CONTEXT.md`:\n\n```json\n{\n  \"configured_backend\": \"postgres\",\n  \"active_backend\": \"sqlite\",\n  \"degraded\": true,\n  \"degraded_reason\": \"postgres_auth_failed\",\n  \"warning_emitted\": true,\n  \"memory_critical_blocked\": false\n}\n```\n\nRequired degraded warning text from `53-CONTEXT.md`:\n\n```text\nBrain degraded: Postgres unavailable, using SQLite fallback.\n```\n\n</interfaces>\n</context>\n\n<tasks>\n\n<task type=\"auto\" id=\"53-01-01\" tdd=\"true\">\n  <name>Task 1: Centralize backend state, fallback transitions, and lifecycle reset</name>\n  <files>get-stuff-done/bin/lib/second-brain.cjs, tests/second-brain-state.test.cjs, tests/second-brain-lifecycle.test.cjs</files>\n  <read_first>\n\n- get-stuff-done/bin/lib/second-brain.cjs\n- .planning/phases/53-second-brain-connection-fallback-hardening/53-CONTEXT.md\n- .planning/phases/53-second-brain-connection-fallback-hardening/53-RESEARCH.md\n- tests/second-brain.test.cjs\n- tests/second-brain-grant.test.cjs\n- tests/checkpoint-plane-sync.test.cjs\n- tests/summary-plane-sync.test.cjs\n  </read_first>\n  <acceptance_criteria>\n\n- `get-stuff-done/bin/lib/second-brain.cjs` contains the exact state keys `configured_backend`, `active_backend`, `degraded`, `degraded_reason`, `warning_emitted`, and `memory_critical_blocked`.\n- `get-stuff-done/bin/lib/second-brain.cjs` contains callable methods or helpers named `getBackendState`, `requirePostgres`, and `resetForTests`.\n- The exact user-facing warning string `Brain degraded: Postgres unavailable, using SQLite fallback.` appears once in the production code path and is emitted only when entering a new degraded reason.\n- `close()` is idempotent: the file contains explicit guards that prevent reusing an ended pool and safely close SQLite only when a live handle exists.\n- `tests/second-brain-state.test.cjs` covers repeated same-reason degradation and changed-reason degradation, and `tests/second-brain-lifecycle.test.cjs` covers repeated `close()` or reset calls.\n  </acceptance_criteria>\n  <behavior>\n\n    - Test 1: A simulated Postgres auth/pool/connectivity failure transitions state to `active_backend: 'sqlite'`, `degraded: true`, and sets a concrete `degraded_reason`.\n    - Test 2: Repeating the same failure reason in one process does not emit a second degraded warning.\n    - Test 3: Changing the degraded reason emits exactly one new warning and updates `degraded_reason`.\n    - Test 4: Calling `close()` or `resetForTests()` twice does not throw and does not leave the singleton in an unusable ended-pool state.\n  </behavior>\n  <action>\nRefactor `get-stuff-done/bin/lib/second-brain.cjs` so all backend selection and fallback truth is owned by one authoritative state object on the singleton. Use these exact state fields: `configured_backend`, `active_backend`, `degraded`, `degraded_reason`, `warning_emitted`, `memory_critical_blocked`. Add concrete helpers with these names: `getBackendState()`, `classifyPostgresFailure(err)`, `transitionToDegraded(reason, details)`, `emitDegradedWarning(reason)`, `requirePostgres(operationName)`, and `resetForTests()`.\n\nReplace the current scattered `console.warn('[SecondBrain] Postgres ... falling back to SQLite:', err.message)` behavior with one transition path that:\n\n1. classifies failures into stable reasons such as `postgres_auth_failed`, `postgres_pool_exhausted`, `postgres_connect_failed`, or `postgres_unavailable`;\n2. sets `active_backend` to `sqlite`;\n3. emits the exact warning text `Brain degraded: Postgres unavailable, using SQLite fallback.` once per process for each distinct `degraded_reason`;\n4. stores detailed error text for health output instead of repeating it in normal command output.\n\nMake lifecycle cleanup deterministic. `close()` must be safe to call repeatedly, must not call `pool.end()` twice on the same pool, and must clear or recreate singleton handles so later tests and CLI calls do not hit `Cannot use a pool after calling end on the pool`. `resetForTests()` must restore a clean initial state for baseline tests without requiring module-cache gymnastics.\n\nWrite tests first in `tests/second-brain-state.test.cjs` and `tests/second-brain-lifecycle.test.cjs`, then implement the refactor until both pass.\n  </action>\n  <verify>\n    <automated>node --test tests/second-brain-state.test.cjs tests/second-brain-lifecycle.test.cjs</automated>\n  </verify>\n  <done>The Second Brain singleton exposes explicit backend truth, degrades once per reason, and survives repeated teardown/reset cycles without warning spam or ended-pool reuse.</done>\n</task>\n\n<task type=\"auto\" id=\"53-01-02\" tdd=\"true\">\n  <name>Task 2: Expose truthful `brain status` and detailed `brain health` with explicit Postgres-required blocking</name>\n  <files>get-stuff-done/bin/lib/second-brain.cjs, get-stuff-done/bin/lib/brain-manager.cjs, get-stuff-done/bin/gsd-tools.cjs, tests/second-brain-status.test.cjs</files>\n  <read_first>\n\n- get-stuff-done/bin/lib/brain-manager.cjs\n- get-stuff-done/bin/gsd-tools.cjs\n- get-stuff-done/bin/lib/second-brain.cjs\n- .planning/phases/53-second-brain-connection-fallback-hardening/53-CONTEXT.md\n- .planning/phases/53-second-brain-connection-fallback-hardening/53-RESEARCH.md\n- .planning/phases/50-plane-integration-observability/50-01-SUMMARY.md\n- tests/plane-health.test.cjs\n  </read_first>\n  <acceptance_criteria>\n\n- `get-stuff-done/bin/gsd-tools.cjs` contains a `subcommand === 'status'` branch under `case 'brain'`.\n- `get-stuff-done/bin/lib/brain-manager.cjs` returns the exact backend-state keys `configured_backend`, `active_backend`, `degraded`, `degraded_reason`, `warning_emitted`, and `memory_critical_blocked` in both status and health output.\n- `get-stuff-done/bin/lib/brain-manager.cjs` contains a degraded runbook/help string that includes the literal command text `brain health --raw`.\n- `get-stuff-done/bin/gsd-tools.cjs` accepts `brain health --require-postgres` and routes that flag to a hard-fail Postgres-required check instead of silently using SQLite.\n- `tests/second-brain-status.test.cjs` asserts both `brain status` and `brain health` JSON output shapes and the blocked behavior for `--require-postgres`.\n  </acceptance_criteria>\n  <behavior>\n\n    - Test 1: `brain status --raw` returns concise JSON with the authoritative backend-state keys and exit code 0 when SQLite fallback is active.\n    - Test 2: `brain health --raw` returns the same backend-state truth plus detailed diagnostics and a short runbook when degraded.\n    - Test 3: `brain health --require-postgres --raw` returns `memory_critical_blocked: true` and exits non-zero when Postgres is unavailable.\n  </behavior>\n  <action>\nExtend `get-stuff-done/bin/lib/brain-manager.cjs` with a concise `getStatus()` method and upgrade `checkHealth(options = {})` to consume `secondBrain.getBackendState()` instead of inferring truth from raw warnings. Both surfaces must include the exact backend-state keys from the phase contract. `brain status` should be the operator-facing summary. `brain health` should include the same backend-state object plus detailed probe data for Postgres, RabbitMQ, and Planning Server and a short degraded runbook string such as `Postgres is unavailable; SQLite fallback is active. Run \\`node get-stuff-done/bin/gsd-tools.cjs brain health --raw\\` for detailed diagnostics.`.\n\nIn `get-stuff-done/bin/gsd-tools.cjs`, add `brain status` and keep `brain health`. Parse a concrete `--require-postgres` flag for `brain health`; when present, call a `requirePostgres('brain health')` or equivalent explicit-memory-critical path before reporting success. Ordinary `brain health` and `brain status` must continue to work in degraded SQLite mode. Do not add any new subsystem or external dependency; reuse the Phase 50 pattern of surfacing operator truth from structured state.\n\nWrite `tests/second-brain-status.test.cjs` against the CLI route and manager output shape before finishing the implementation.\n  </action>\n  <verify>\n    <automated>node --test tests/second-brain-status.test.cjs</automated>\n  </verify>\n  <done>Operators can query `brain status` for concise truth, `brain health` for detailed diagnostics, and explicit Postgres-required requests fail hard instead of silently downgrading to SQLite.</done>\n</task>\n\n<task type=\"auto\" id=\"53-01-03\" tdd=\"true\">\n  <name>Task 3: Harden baseline test posture and add opt-in Postgres integration coverage</name>\n  <files>tests/second-brain-postgres-optin.test.cjs, tests/checkpoint-plane-sync.test.cjs, tests/summary-plane-sync.test.cjs, get-stuff-done/bin/lib/second-brain.cjs</files>\n  <read_first>\n\n- tests/checkpoint-plane-sync.test.cjs\n- tests/summary-plane-sync.test.cjs\n- get-stuff-done/bin/lib/second-brain.cjs\n- scripts/run-tests.cjs\n- package.json\n- .planning/phases/53-second-brain-connection-fallback-hardening/53-VALIDATION.md\n- .planning/phases/53-second-brain-connection-fallback-hardening/53-RESEARCH.md\n  </read_first>\n  <acceptance_criteria>\n\n- `tests/second-brain-postgres-optin.test.cjs` exists and contains the exact env gate `process.env.GSD_TEST_POSTGRES === '1'`.\n- `tests/checkpoint-plane-sync.test.cjs` and `tests/summary-plane-sync.test.cjs` explicitly clean up Second Brain state without leaving the singleton unusable for later tests.\n- Baseline tests do not require ambient `PGHOST`, `PGPORT`, `PGDATABASE`, `PGUSER`, `PGPASSWORD`, or `DATABASE_URL` to pass.\n- The opt-in verification command is exactly `GSD_TEST_POSTGRES=1 node --test tests/second-brain-postgres-optin.test.cjs`.\n- A focused baseline verification command runs cleanly without Postgres-specific warning spam: `node --test tests/second-brain-state.test.cjs tests/second-brain-lifecycle.test.cjs tests/second-brain-status.test.cjs tests/checkpoint-plane-sync.test.cjs tests/summary-plane-sync.test.cjs`.\n  </acceptance_criteria>\n  <behavior>\n\n    - Test 1: Baseline local runs scrub or ignore ambient Postgres configuration and still pass the new lifecycle/status regressions and existing Plane sync teardown tests.\n    - Test 2: The explicit opt-in Postgres integration test skips or no-ops unless `GSD_TEST_POSTGRES=1`.\n    - Test 3: When `GSD_TEST_POSTGRES=1` is set, the opt-in test exercises the Postgres-required path and proves the command does not silently fallback.\n  </behavior>\n  <action>\nAdd `tests/second-brain-postgres-optin.test.cjs` as the explicit integration gate for real Postgres behavior. The file must branch on `process.env.GSD_TEST_POSTGRES === '1'`: when the flag is absent, assert the test is skipped or returns early without touching Postgres; when the flag is present, run the concrete command `node get-stuff-done/bin/gsd-tools.cjs brain health --require-postgres --raw` and assert the result reports either active Postgres or an explicit `memory_critical_blocked` failure instead of silently switching to SQLite.\n\nUpdate `tests/checkpoint-plane-sync.test.cjs` and `tests/summary-plane-sync.test.cjs` so their teardown path cooperates with the new lifecycle helpers from Task 1. Save and restore any Postgres-related environment variables inside the tests, and ensure teardown uses the new idempotent cleanup path (`secondBrain.close()` or `secondBrain.resetForTests()`) without leaking ended-pool state across requires. Keep baseline local tests clean by unsetting or isolating `PGHOST`, `PGPORT`, `PGDATABASE`, `PGUSER`, `PGPASSWORD`, and `DATABASE_URL` inside tests that intentionally exercise SQLite fallback paths.\n  </action>\n  <verify>\n    <automated>node --test tests/second-brain-state.test.cjs tests/second-brain-lifecycle.test.cjs tests/second-brain-status.test.cjs tests/checkpoint-plane-sync.test.cjs tests/summary-plane-sync.test.cjs</automated>\n  </verify>\n  <done>Baseline tests stay quiet and deterministic without Postgres, and explicit Postgres integration coverage exists behind a single opt-in gate.</done>\n</task>\n\n</tasks>\n\n<verification>\nRun these checks after implementation:\n\n- `node --check get-stuff-done/bin/lib/second-brain.cjs`\n- `node --check get-stuff-done/bin/lib/brain-manager.cjs`\n- `node --check get-stuff-done/bin/gsd-tools.cjs`\n- `node --test tests/second-brain-state.test.cjs tests/second-brain-lifecycle.test.cjs tests/second-brain-status.test.cjs tests/checkpoint-plane-sync.test.cjs tests/summary-plane-sync.test.cjs`\n- `GSD_TEST_POSTGRES=1 node --test tests/second-brain-postgres-optin.test.cjs`\n- `node get-stuff-done/bin/gsd-tools.cjs brain status --raw`\n- `node get-stuff-done/bin/gsd-tools.cjs brain health --raw`\n\n</verification>\n\n<success_criteria>\n\n- Phase 53 implements all locked decisions from `53-CONTEXT.md` without introducing any Phase 54 MCP behavior.\n- All three requirement IDs `BRAIN-OPS-01`, `BRAIN-OPS-02`, and `BRAIN-OPS-03` are satisfied by a single autonomous execution plan.\n- The Second Brain backend truth is explicit, testable, operator-visible, and stable across repeated CLI and test execution.\n\n</success_criteria>\n\n<output>\nAfter completion, create `.planning/phases/53-second-brain-connection-fallback-hardening/53-01-SUMMARY.md`.\n</output>\n",
  "content_hash": "672e5d70c3ad1b96723967ecf5ac6bb715be3c25fb88e7a46a37e8d9898ed586",
  "normalized_at": "2026-03-28T03:46:21.344Z",
  "provenance": {
    "producer": "internal-normalizer",
    "producer_version": "1.1.0",
    "parameters_hash": null
  }
}
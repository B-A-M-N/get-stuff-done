---
phase: 55-open-brain-v1-foundations
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
files_modified:
  - get-stuff-done/bin/lib/open-brain.cjs
  - get-stuff-done/bin/lib/open-brain-ranker.cjs
  - get-stuff-done/bin/lib/internal-normalizer.cjs
  - get-stuff-done/bin/lib/context.cjs
  - tests/open-brain-ingestion.test.cjs
  - tests/open-brain-retrieval.test.cjs
  - tests/open-brain-feedback.test.cjs
autonomous: true
requirements:
  - OPEN-BRAIN-03
  - OPEN-BRAIN-04
  - OPEN-BRAIN-05
must_haves:
  truths:
    - "Open Brain ingests selected normalized artifacts through a dedicated sidecar API instead of coupling long-horizon recall to Second Brain operational tables."
    - "Semantic retrieval is bounded and ranked by relevance, recency, reuse, and feedback rather than exposing raw rows to prompts."
    - "Recall events and outcome feedback are persisted so later ranking can improve based on measured helpful and harmful signals."
  artifacts:
    - path: "get-stuff-done/bin/lib/open-brain.cjs"
      provides: "Ingestion, retrieval, and recall-feedback storage helpers."
    - path: "get-stuff-done/bin/lib/open-brain-ranker.cjs"
      provides: "Ranking, curation, and bounded recall selection logic."
    - path: "get-stuff-done/bin/lib/context.cjs"
      provides: "Concrete workflow-preparation handoff that promotes selected normalized artifacts before Open Brain retrieval."
    - path: "tests/open-brain-ingestion.test.cjs"
      provides: "Coverage for normalized artifact promotion into Open Brain."
    - path: "tests/open-brain-retrieval.test.cjs"
      provides: "Coverage for bounded semantic retrieval behavior."
    - path: "tests/open-brain-feedback.test.cjs"
      provides: "Coverage for recall-event recording and feedback-driven ranking updates."
  key_links:
    - from: "get-stuff-done/bin/lib/open-brain.cjs"
      to: "get-stuff-done/bin/lib/internal-normalizer.cjs"
      via: "normalized artifact ingestion boundary"
      pattern: "normalizeInternal|artifact|source_uri|content_markdown"
    - from: "get-stuff-done/bin/lib/context.cjs"
      to: "get-stuff-done/bin/lib/open-brain.cjs"
      via: "runtime producer handoff from normalized project artifacts into Open Brain ingestion before retrieval"
      pattern: "normalizeInternal\\(cwd\\)|ingestProjectNormalizedArtifacts|ingestNormalizedArtifact"
    - from: "get-stuff-done/bin/lib/open-brain-ranker.cjs"
      to: "get-stuff-done/bin/lib/open-brain.cjs"
      via: "runtime retrieval ranking path"
      pattern: "searchOpenBrain|recallForWorkflow|similarity|recency|reuse|helpful|harmful"
---

<objective>
Build the Open Brain ingestion, ranking, retrieval, and feedback loops so the sidecar can store selected normalized artifacts and return bounded curated semantic recall.
</objective>

<execution_context>
@/home/bamn/.codex/get-shit-done/workflows/execute-plan.md
@/home/bamn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-open-brain-v1-foundations/55-CONTEXT.md
@.planning/phases/55-open-brain-v1-foundations/55-RESEARCH.md
@.planning/phases/55-open-brain-v1-foundations/55-VALIDATION.md
@.planning/phases/55-open-brain-v1-foundations/55-01-PLAN.md
@docs/OPEN-BRAIN-ARCHITECTURE.md
@get-stuff-done/bin/lib/internal-normalizer.cjs
@get-stuff-done/bin/lib/open-brain.cjs
</context>

<tasks>

<task type="auto" id="55-02-01" tdd="true">
  <name>Task 1: Ingest selected normalized artifacts into Open Brain through a curated sidecar API</name>
  <files>get-stuff-done/bin/lib/open-brain.cjs, get-stuff-done/bin/lib/internal-normalizer.cjs, get-stuff-done/bin/lib/context.cjs, tests/open-brain-ingestion.test.cjs</files>
  <read_first>
- get-stuff-done/bin/lib/internal-normalizer.cjs
- get-stuff-done/bin/lib/open-brain.cjs
- get-stuff-done/bin/lib/context.cjs
- docs/OPEN-BRAIN-ARCHITECTURE.md
- .planning/phases/55-open-brain-v1-foundations/55-RESEARCH.md
  </read_first>
  <acceptance_criteria>
- `get-stuff-done/bin/lib/open-brain.cjs` exposes helpers such as `ingestNormalizedArtifact` and `promoteMemoryCandidate`.
- `get-stuff-done/bin/lib/open-brain.cjs` exposes a concrete producer handoff such as `ingestProjectNormalizedArtifacts(cwd, ...)` that calls `normalizeInternal(cwd)` and routes curated artifacts into `ingestNormalizedArtifact(...)`.
- Ingestion consumes normalized artifact shape rather than inventing a new raw input contract.
- The implementation prefers curated/promoted artifact types over indiscriminate raw-log ingestion.
- `get-stuff-done/bin/lib/context.cjs` calls the concrete Open Brain ingestion handoff before Open Brain retrieval so selected normalized artifacts from `normalizeInternal(cwd)` can actually flow into `gsd_open_brain`.
- `tests/open-brain-ingestion.test.cjs` asserts that selected normalized artifacts can be promoted into Open Brain records without coupling writes to `gsd_local_brain.workflow_memory`.
- `tests/open-brain-ingestion.test.cjs` asserts the exact producer-to-ingestion path `normalizeInternal(cwd) -> ingestProjectNormalizedArtifacts(...) -> ingestNormalizedArtifact(...)` writes only promotable artifacts.
  </acceptance_criteria>
  <behavior>
    - Test 1: A normalized artifact can be promoted into an Open Brain memory item with project scope and source metadata.
    - Test 2: Empty or malformed artifacts are rejected cleanly.
    - Test 3: Optional memory links can be attached without requiring them for every ingestion.
  </behavior>
  <action>
Write `tests/open-brain-ingestion.test.cjs` first. Then extend `get-stuff-done/bin/lib/open-brain.cjs` with both the low-level ingestion API and one concrete producer handoff named explicitly in code, such as `ingestProjectNormalizedArtifacts(cwd, options)`. That handoff must call `normalizeInternal(cwd)`, filter the returned artifacts through `isPromotableOpenBrainArtifact`, and route each curated artifact into `ingestNormalizedArtifact(...)`. Reuse the artifact shape already produced by `internal-normalizer.cjs` and Firecrawl normalization. Do not add a second raw-source ingestion path for v1.

Keep ingestion curated. Favor artifact summaries, validated resolutions, promoted lessons, and repeated user corrections over broad firehose import. If a light touch in `internal-normalizer.cjs` is helpful for Open Brain handoff metadata, keep it additive and do not change Second Brain’s existing ingestion behavior.

Wire the exact producer-to-ingestion handoff into `get-stuff-done/bin/lib/context.cjs` before Open Brain retrieval runs, so relevant workflow preparation can promote selected normalized artifacts into `gsd_open_brain` through the concrete path `normalizeInternal(cwd) -> ingestProjectNormalizedArtifacts(cwd, ...) -> ingestNormalizedArtifact(...)` instead of leaving ingestion as a library-only surface.
  </action>
  <verify>
    <automated>node --test tests/open-brain-ingestion.test.cjs</automated>
  </verify>
  <done>Selected normalized artifacts can be promoted into Open Brain through a dedicated sidecar ingestion path.</done>
</task>

<task type="auto" id="55-02-02" tdd="true">
  <name>Task 2: Add bounded ranking, retrieval, and recall-event feedback loops</name>
  <files>get-stuff-done/bin/lib/open-brain.cjs, get-stuff-done/bin/lib/open-brain-ranker.cjs, tests/open-brain-retrieval.test.cjs, tests/open-brain-feedback.test.cjs</files>
  <read_first>
- get-stuff-done/bin/lib/open-brain.cjs
- docs/OPEN-BRAIN-ARCHITECTURE.md
- .planning/phases/55-open-brain-v1-foundations/55-RESEARCH.md
  </read_first>
  <acceptance_criteria>
- `get-stuff-done/bin/lib/open-brain.cjs` exposes retrieval helpers such as `searchOpenBrain`, `recallForWorkflow`, and `recordRecallOutcome`.
- `get-stuff-done/bin/lib/open-brain-ranker.cjs` exists and owns ranking/curation behavior.
- Retrieval excludes archived or superseded memories unless explicitly requested.
- `tests/open-brain-retrieval.test.cjs` asserts bounded ranked retrieval behavior.
- `tests/open-brain-feedback.test.cjs` asserts recall events are recorded and feedback changes later ranking behavior.
  </acceptance_criteria>
  <behavior>
    - Test 1: Retrieval returns a bounded curated selection rather than unbounded raw query output.
    - Test 2: Ranking considers similarity, recency, reuse, and helpful/harmful outcomes.
    - Test 3: Recording helpful and harmful outcomes changes later retrieval ordering in a visible way.
  </behavior>
  <action>
Write `tests/open-brain-retrieval.test.cjs` and `tests/open-brain-feedback.test.cjs` first. Add `get-stuff-done/bin/lib/open-brain-ranker.cjs` for ranking logic, then extend `get-stuff-done/bin/lib/open-brain.cjs` with retrieval and feedback helpers. Keep the retrieval API bounded and curated. It should return selected memory candidates and recall metadata, not raw database rows intended to be copied directly into prompts.

The ranking contract for v1 must incorporate:
- vector similarity
- recency
- reuse count
- helpful count minus harmful count
- project-scope match
- explicit importance score

Do not boost ranking just because an item was retrieved. Only explicit outcome recording should improve or reduce later ranking.
  </action>
  <verify>
    <automated>node --test tests/open-brain-retrieval.test.cjs tests/open-brain-feedback.test.cjs</automated>
  </verify>
  <done>Open Brain can return bounded ranked recall and learn from measured recall outcomes.</done>
</task>

<task type="auto" id="55-02-03">
  <name>Task 3: Verify ingestion and retrieval foundations</name>
  <files>get-stuff-done/bin/lib/open-brain.cjs, get-stuff-done/bin/lib/open-brain-ranker.cjs, tests/open-brain-ingestion.test.cjs, tests/open-brain-retrieval.test.cjs, tests/open-brain-feedback.test.cjs</files>
  <read_first>
- .planning/phases/55-open-brain-v1-foundations/55-VALIDATION.md
- tests/open-brain-ingestion.test.cjs
- tests/open-brain-retrieval.test.cjs
- tests/open-brain-feedback.test.cjs
  </read_first>
  <acceptance_criteria>
- `node --check get-stuff-done/bin/lib/open-brain.cjs` exits 0.
- `node --check get-stuff-done/bin/lib/open-brain-ranker.cjs` exits 0.
- `node --test tests/open-brain-ingestion.test.cjs tests/open-brain-retrieval.test.cjs tests/open-brain-feedback.test.cjs` exits 0.
  </acceptance_criteria>
  <action>
Run focused verification after implementation:
- `node --check get-stuff-done/bin/lib/open-brain.cjs`
- `node --check get-stuff-done/bin/lib/open-brain-ranker.cjs`
- `node --test tests/open-brain-ingestion.test.cjs tests/open-brain-retrieval.test.cjs tests/open-brain-feedback.test.cjs`

Fix focused failures before considering Plan 55-02 complete.
  </action>
  <verify>
    <automated>node --check get-stuff-done/bin/lib/open-brain.cjs && node --check get-stuff-done/bin/lib/open-brain-ranker.cjs && node --test tests/open-brain-ingestion.test.cjs tests/open-brain-retrieval.test.cjs tests/open-brain-feedback.test.cjs</automated>
  </verify>
  <done>The Open Brain ingestion and retrieval core is behaviorally pinned and ready for workflow integration.</done>
</task>

</tasks>

<verification>
- `node --test tests/open-brain-ingestion.test.cjs`
- `node --test tests/open-brain-retrieval.test.cjs tests/open-brain-feedback.test.cjs`
- `node --check get-stuff-done/bin/lib/open-brain.cjs`
- `node --check get-stuff-done/bin/lib/open-brain-ranker.cjs`
</verification>

<success_criteria>
Plan 55-02 is satisfied when Open Brain can ingest selected normalized artifacts, return bounded ranked semantic recall, and persist feedback that affects future ranking.
</success_criteria>

<output>
After completion, create `.planning/phases/55-open-brain-v1-foundations/55-02-SUMMARY.md`
</output>

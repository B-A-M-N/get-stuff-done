---
phase: 55-open-brain-v1-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-stuff-done/bin/lib/open-brain.cjs
  - get-stuff-done/bin/lib/open-brain-embedder.cjs
  - get-stuff-done/bin/gsd-tools.cjs
  - scripts/init-open-brain.sql
  - tests/open-brain-schema.test.cjs
  - tests/open-brain-degraded-mode.test.cjs
autonomous: true
requirements:
  - OPEN-BRAIN-01
  - OPEN-BRAIN-02
  - OPEN-BRAIN-06
must_haves:
  truths:
    - "Open Brain storage lives in a separate `gsd_open_brain` schema and does not repurpose `gsd_local_brain` workflow-memory tables."
    - "Open Brain foundation supports local embeddings through a dedicated adapter with `fastembed` as the default implementation target."
    - "Open Brain infrastructure can be unavailable without breaking existing planner, executor, Firecrawl, or Second Brain flows."
  artifacts:
    - path: "get-stuff-done/bin/lib/open-brain.cjs"
      provides: "Canonical Open Brain schema/bootstrap helpers, degraded-mode handling, and public sidecar API."
    - path: "get-stuff-done/bin/lib/open-brain-embedder.cjs"
      provides: "Local embedding provider abstraction and default `fastembed` integration point."
    - path: "scripts/init-open-brain.sql"
      provides: "Concrete `gsd_open_brain` schema, `pgvector`, and graph-ready table bootstrap contract."
    - path: "tests/open-brain-schema.test.cjs"
      provides: "Coverage that Open Brain schema/bootstrap matches the Phase 55 contract."
    - path: "tests/open-brain-degraded-mode.test.cjs"
      provides: "Coverage that Open Brain failure degrades safely instead of breaking workflows."
  key_links:
    - from: "get-stuff-done/bin/lib/open-brain.cjs"
      to: "scripts/init-open-brain.sql"
      via: "runtime bootstrap contract between library entry points and the concrete schema initializer"
      pattern: "gsd_open_brain|memory_item|memory_link|recall_event|consolidation_job|create extension if not exists vector"
    - from: "get-stuff-done/bin/lib/open-brain.cjs"
      to: "get-stuff-done/bin/lib/open-brain-embedder.cjs"
      via: "runtime availability and embedding-provider wiring"
      pattern: "fastembed|available|vector|dimensions|provider"
    - from: "get-stuff-done/bin/lib/open-brain.cjs"
      to: "get-stuff-done/bin/lib/second-brain.cjs"
      via: "separate sidecar vs execution-truth boundary"
      pattern: "gsd_open_brain|gsd_local_brain|requirePostgres"
---

<objective>
Establish Open Brain as a separate local-first sidecar with canonical schema/bootstrap, embedding-provider abstraction, and safe degraded behavior before any ingestion or workflow integration begins.
</objective>

<execution_context>
@/home/bamn/.codex/get-shit-done/workflows/execute-plan.md
@/home/bamn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-open-brain-v1-foundations/55-CONTEXT.md
@.planning/phases/55-open-brain-v1-foundations/55-RESEARCH.md
@.planning/phases/55-open-brain-v1-foundations/55-VALIDATION.md
@docs/OPEN-BRAIN-ARCHITECTURE.md
@docs/ARCHITECTURE.md
@get-stuff-done/bin/lib/second-brain.cjs
@get-stuff-done/bin/gsd-tools.cjs
</context>

<tasks>

<task type="auto" id="55-01-01" tdd="true">
  <name>Task 1: Create the Open Brain schema/bootstrap contract and public sidecar surface</name>
  <files>get-stuff-done/bin/lib/open-brain.cjs, scripts/init-open-brain.sql, tests/open-brain-schema.test.cjs</files>
  <read_first>
- docs/OPEN-BRAIN-ARCHITECTURE.md
- .planning/phases/55-open-brain-v1-foundations/55-CONTEXT.md
- .planning/phases/55-open-brain-v1-foundations/55-RESEARCH.md
- get-stuff-done/bin/lib/second-brain.cjs
  </read_first>
  <acceptance_criteria>
- `get-stuff-done/bin/lib/open-brain.cjs` exists and owns Open Brain bootstrap/helpers rather than extending `second-brain.cjs` tables.
- `scripts/init-open-brain.sql` exists and contains `create schema if not exists gsd_open_brain`.
- `scripts/init-open-brain.sql` contains `create extension if not exists vector`.
- The schema contract includes the exact logical tables `memory_item`, `memory_link`, `recall_event`, and `consolidation_job`.
- `tests/open-brain-schema.test.cjs` asserts the bootstrap contract contains the required schema/table names and keeps Open Brain separate from `gsd_local_brain.workflow_memory`.
  </acceptance_criteria>
  <behavior>
    - Test 1: Open Brain bootstrap defines graph-ready relational tables in `gsd_open_brain`.
    - Test 2: The contract does not repurpose Second Brain workflow-memory tables for long-horizon recall.
    - Test 3: The Open Brain public library exposes explicit initialization/bootstrap entry points.
  </behavior>
  <action>
Write `tests/open-brain-schema.test.cjs` first. Pin the phase contract in tests before implementation. Then create `scripts/init-open-brain.sql` and `get-stuff-done/bin/lib/open-brain.cjs` as the canonical Open Brain sidecar surface. The library should own Open Brain initialization, schema bootstrap, and future ingestion/retrieval helpers, but should not make Open Brain the source of execution truth.

Keep the schema aligned with the architecture doc: `gsd_open_brain.memory_item`, `memory_link`, `recall_event`, and `consolidation_job`. Preserve graph-readiness through relational links now; do not introduce a graph database in this plan.
  </action>
  <verify>
    <automated>node --test tests/open-brain-schema.test.cjs</automated>
  </verify>
  <done>The Open Brain schema/bootstrap contract exists as a separate sidecar foundation and is pinned by focused tests.</done>
</task>

<task type="auto" id="55-01-02" tdd="true">
  <name>Task 2: Add a local embedding adapter and safe degraded behavior for Open Brain foundation</name>
  <files>get-stuff-done/bin/lib/open-brain.cjs, get-stuff-done/bin/lib/open-brain-embedder.cjs, get-stuff-done/bin/gsd-tools.cjs, tests/open-brain-degraded-mode.test.cjs</files>
  <read_first>
- docs/OPEN-BRAIN-ARCHITECTURE.md
- .planning/phases/55-open-brain-v1-foundations/55-RESEARCH.md
- get-stuff-done/bin/lib/second-brain.cjs
- get-stuff-done/bin/gsd-tools.cjs
  </read_first>
  <acceptance_criteria>
- `get-stuff-done/bin/lib/open-brain-embedder.cjs` exists and defines a local embedding provider abstraction with `fastembed` as the default implementation target.
- The Open Brain foundation exposes structured unavailable/degraded responses when Postgres, `pgvector`, or the embedding provider are unavailable.
- `tests/open-brain-degraded-mode.test.cjs` covers at least one storage-side failure and one embedding-side failure.
- Existing planner/executor/Firecrawl flows do not require Open Brain to be available in order to continue.
  </acceptance_criteria>
  <behavior>
    - Test 1: Missing or disabled embedding capability does not crash Open Brain callers.
    - Test 2: Missing Postgres/`pgvector` capability produces explicit unavailable/degraded metadata.
    - Test 3: Operator or helper surfaces can report Open Brain readiness without conflating it with Second Brain backend truth.
  </behavior>
  <action>
Write `tests/open-brain-degraded-mode.test.cjs` first. Add `get-stuff-done/bin/lib/open-brain-embedder.cjs` as a narrow local embedding adapter. Treat `fastembed` as the default provider target, but keep the adapter interface explicit so the implementation can report capability gaps cleanly. Extend `get-stuff-done/bin/lib/open-brain.cjs` with readiness/degraded checks and, if a minimal operator surface is needed, add the smallest truthful hook in `get-stuff-done/bin/gsd-tools.cjs`.

Do not make Open Brain a memory-critical hard dependency. The correct posture is: Open Brain unavailable means semantic recall is unavailable, not that planning/execution stops working.
  </action>
  <verify>
    <automated>node --test tests/open-brain-degraded-mode.test.cjs</automated>
  </verify>
  <done>Open Brain foundation has a local embedding adapter target and degrades safely when prerequisites are unavailable.</done>
</task>

<task type="auto" id="55-01-03">
  <name>Task 3: Verify foundation contract and syntax</name>
  <files>get-stuff-done/bin/lib/open-brain.cjs, get-stuff-done/bin/lib/open-brain-embedder.cjs, scripts/init-open-brain.sql, tests/open-brain-schema.test.cjs, tests/open-brain-degraded-mode.test.cjs</files>
  <read_first>
- .planning/phases/55-open-brain-v1-foundations/55-VALIDATION.md
- tests/open-brain-schema.test.cjs
- tests/open-brain-degraded-mode.test.cjs
  </read_first>
  <acceptance_criteria>
- `node --check get-stuff-done/bin/lib/open-brain.cjs` exits 0.
- `node --check get-stuff-done/bin/lib/open-brain-embedder.cjs` exits 0.
- `node --test tests/open-brain-schema.test.cjs tests/open-brain-degraded-mode.test.cjs` exits 0.
  </acceptance_criteria>
  <action>
Run focused verification after implementation:
- `node --check get-stuff-done/bin/lib/open-brain.cjs`
- `node --check get-stuff-done/bin/lib/open-brain-embedder.cjs`
- `node --test tests/open-brain-schema.test.cjs tests/open-brain-degraded-mode.test.cjs`

Fix focused failures before considering Plan 55-01 complete.
  </action>
  <verify>
    <automated>node --check get-stuff-done/bin/lib/open-brain.cjs && node --check get-stuff-done/bin/lib/open-brain-embedder.cjs && node --test tests/open-brain-schema.test.cjs tests/open-brain-degraded-mode.test.cjs</automated>
  </verify>
  <done>The Open Brain foundation is syntactically sound, contract-pinned, and safely optional.</done>
</task>

</tasks>

<verification>
- `node --test tests/open-brain-schema.test.cjs`
- `node --test tests/open-brain-degraded-mode.test.cjs`
- `node --check get-stuff-done/bin/lib/open-brain.cjs`
- `node --check get-stuff-done/bin/lib/open-brain-embedder.cjs`
</verification>

<success_criteria>
Plan 55-01 is satisfied when Open Brain exists as a distinct sidecar foundation with explicit schema/bootstrap, local embedding adapter posture, and safe degraded behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/55-open-brain-v1-foundations/55-01-SUMMARY.md`
</output>

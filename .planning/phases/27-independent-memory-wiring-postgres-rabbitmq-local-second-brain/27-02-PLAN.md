---
phase: 27-independent-memory-wiring-postgres-rabbitmq-local-second-brain
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified: [get-stuff-done/bin/lib/internal-normalizer.cjs, tests/second-brain.test.cjs]
autonomous: true
requirements: [BRAIN-INGEST-01, BRAIN-VERIFY-01]

must_haves:
  truths:
    - "Internal normalization automatically ingests artifacts into the Second Brain"
    - "Integration tests confirm E2E flow from normalization to Postgres and RabbitMQ"
  artifacts:
    - path: "tests/second-brain.test.cjs"
      provides: "E2E verification of memory wiring"
  key_links:
    - from: "get-stuff-done/bin/lib/internal-normalizer.cjs"
      to: "get-stuff-done/bin/lib/second-brain.cjs"
      via: "ingestArtifact call"
---

<objective>
Integrate the Second Brain ingestion into the normalization pipeline and verify the full memory wiring with a comprehensive test suite.

Purpose: To ensure that project knowledge (symbols, dependencies) is automatically captured and distributed as part of the standard workflow.
Output: Updated normalization pipeline and a new integration test suite.
</objective>

<execution_context>
@/home/bamn/.gemini/get-shit-done/workflows/execute-plan.md
@/home/bamn/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-independent-memory-wiring-postgres-rabbitmq-local-second-brain/27-01-PLAN.md
@get-stuff-done/bin/lib/internal-normalizer.cjs
@get-stuff-done/bin/lib/second-brain.cjs
@get-stuff-done/bin/lib/broker.cjs

<interfaces>
From get-stuff-done/bin/lib/second-brain.cjs (Plan 27-01):
```javascript
export function ingestArtifact(artifact: Object): Promise<void>;
export function querySymbols(name: string): Promise<Array<Object>>;
```

From get-stuff-done/bin/lib/broker.cjs (Plan 27-01):
```javascript
export function subscribe(topic: string, callback: Function): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline Integration</name>
  <files>get-stuff-done/bin/lib/internal-normalizer.cjs</files>
  <action>
    Update `normalizeInternal` in `get-stuff-done/bin/lib/internal-normalizer.cjs`:
    1. Import `ingestArtifact` from `./second-brain.cjs`.
    2. After creating and validating each `artifact` object, call `await ingestArtifact(artifact)`.
    3. Ensure the function remains compatible with existing callers (may need to make `normalizeInternal` async if it isn't already, but check call sites).
  </action>
  <verify>
    <automated>node -e "require('./get-stuff-done/bin/lib/internal-normalizer.cjs').normalizeInternal('.').then(() => process.exit(0))"</automated>
  </verify>
  <done>Normalization pipeline pushes data to the Second Brain.</done>
</task>

<task type="auto">
  <name>Task 2: E2E Verification Suite</name>
  <files>tests/second-brain.test.cjs</files>
  <action>
    Create `tests/second-brain.test.cjs`:
    1. Use a temporary test file.
    2. Run normalization on the test file.
    3. Verify that symbols and dependencies are present in the `gsd_local_brain` Postgres database.
    4. Verify that a Pulse event was received via RabbitMQ for the symbol extraction.
    5. Clean up test data after execution.
  </action>
  <verify>
    <automated>node --test tests/second-brain.test.cjs</automated>
  </verify>
  <done>E2E memory wiring is verified and working.</done>
</task>

</tasks>

<verification>
Run the full test suite and ensure all tests pass.
Check Postgres for data from the test run.
</verification>

<success_criteria>
- `internal-normalizer.cjs` calls `ingestArtifact`.
- `tests/second-brain.test.cjs` passes consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/27-independent-memory-wiring-postgres-rabbitmq-local-second-brain/27-02-SUMMARY.md`
</output>

---
phase: 27-independent-memory-wiring-postgres-rabbitmq-local-second-brain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [get-stuff-done/scripts/init-local-brain.sql, get-stuff-done/bin/lib/broker.cjs, get-stuff-done/bin/lib/second-brain.cjs, package.json]
autonomous: true
requirements: [BRAIN-INFRA-01, BRAIN-STORAGE-01]
user_setup:
  - service: postgres
    why: "Relational memory storage"
    env_vars:
      - name: PGHOST
        source: "Localhost"
      - name: PGPORT
        source: "Default 5432"
      - name: PGUSER
        source: "User credentials"
      - name: PGDATABASE
        source: "gsd_local_brain"
  - service: rabbitmq
    why: "Event distribution"
    env_vars:
      - name: AMQP_URL
        source: "Default amqp://localhost"

must_haves:
  truths:
    - "Postgres schema gsd_local_brain exists with symbols and dependencies tables"
    - "RabbitMQ broker can publish and subscribe to gsd.pulse exchange"
    - "Second Brain library can upsert symbols and record dependencies in Postgres"
  artifacts:
    - path: "get-stuff-done/scripts/init-local-brain.sql"
      provides: "Database schema definition"
    - path: "get-stuff-done/bin/lib/broker.cjs"
      provides: "RabbitMQ event distribution"
    - path: "get-stuff-done/bin/lib/second-brain.cjs"
      provides: "Postgres relational memory storage"
  key_links:
    - from: "get-stuff-done/bin/lib/second-brain.cjs"
      to: "get-stuff-done/bin/lib/broker.cjs"
      via: "event emission on symbol upsert"
---

<objective>
Implement the infrastructure foundation for the project's local memory, including the Postgres relational schema, RabbitMQ event broker, and the core memory storage library.

Purpose: To provide a persistent, relational, and event-driven foundation for "local consciousness" that tracks code symbols and dependencies.
Output: SQL schema, RabbitMQ broker library, and Postgres storage library.
</objective>

<execution_context>
@/home/bamn/.gemini/get-shit-done/workflows/execute-plan.md
@/home/bamn/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@get-stuff-done/bin/lib/context-artifact.cjs
@get-stuff-done/bin/lib/artifact-schema.cjs

<interfaces>
From get-stuff-done/bin/lib/artifact-schema.cjs:
```javascript
const contextArtifactSchema = z.object({
  id: z.string(),
  source_uri: z.string(),
  type: z.enum(['external', 'internal']),
  content_markdown: z.string(),
  analysis: z.object({
    symbols: z.array(z.object({
      name: z.string(),
      kind: z.string(),
      line: z.number()
    })).optional(),
    dependencies: z.array(z.string()).optional()
  }).optional()
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL Schema & Dependencies</name>
  <files>package.json, get-stuff-done/scripts/init-local-brain.sql</files>
  <action>
    1. Install `pg` and `amqplib` via `npm install`.
    2. Create `get-stuff-done/scripts/init-local-brain.sql`.
    3. Define schema `gsd_local_brain`.
    4. Create table `artifacts` (id PK, source_uri, content_hash, normalized_at).
    5. Create table `symbols` (id SERIAL PK, artifact_id FK, name, kind, line).
    6. Create table `dependencies` (id SERIAL PK, artifact_id FK, dependency_uri).
    7. Add indexes on `symbols(name)`, `symbols(artifact_id)`, and `dependencies(artifact_id)`.
    8. Execute schema against local Postgres (assume `gsd_local_brain` DB exists or create it).
  </action>
  <verify>
    <automated>psql -d gsd_local_brain -c "\dt gsd_local_brain.*"</automated>
  </verify>
  <done>Postgres tables exist and dependencies are installed.</done>
</task>

<task type="auto">
  <name>Task 2: RabbitMQ Broker Library</name>
  <files>get-stuff-done/bin/lib/broker.cjs</files>
  <action>
    Implement a RabbitMQ wrapper using `amqplib`:
    - `connect()`: Establish connection to RabbitMQ (use AMQP_URL env var).
    - `publish(topic, message)`: Publish JSON message to `gsd.pulse` exchange (topic-based).
    - `subscribe(topic, callback)`: Subscribe to `gsd.pulse` with a callback.
    - Ensure persistent connection with automatic reconnect or error handling.
  </action>
  <verify>
    <automated>node -e "const b = require('./get-stuff-done/bin/lib/broker.cjs'); b.connect().then(() => b.publish('test', {ok:true})).then(() => process.exit(0))"</automated>
  </verify>
  <done>Broker library can send messages to RabbitMQ.</done>
</task>

<task type="auto">
  <name>Task 3: Postgres Storage Library</name>
  <files>get-stuff-done/bin/lib/second-brain.cjs</files>
  <action>
    Implement the memory storage library using `pg`:
    - `ingestArtifact(artifact)`: Upsert artifact to `artifacts` table.
    - If `artifact.analysis` exists:
        - Insert symbols into `symbols` table.
        - Insert dependencies into `dependencies` table.
        - Emit a Pulse event `symbol.ingested` for each symbol via `broker.publish()`.
    - Provide a way to query symbols and their associated artifacts.
  </action>
  <verify>
    <automated>node -e "const s = require('./get-stuff-done/bin/lib/second-brain.cjs'); s.ingestArtifact({id:'test', source_uri:'test.js', analysis:{symbols:[{name:'foo',kind:'function',line:1}]}}).then(() => process.exit(0))"</automated>
  </verify>
  <done>Second Brain library can store artifacts and emit events.</done>
</task>

</tasks>

<verification>
Check for installed packages in package.json.
Verify Postgres tables exist.
Verify RabbitMQ exchange gsd.pulse is active.
</verification>

<success_criteria>
- `pg` and `amqplib` are in `package.json`.
- `init-local-brain.sql` creates the expected tables.
- `broker.cjs` and `second-brain.cjs` are functional.
</success_criteria>

<output>
After completion, create `.planning/phases/27-independent-memory-wiring-postgres-rabbitmq-local-second-brain/27-01-SUMMARY.md`
</output>

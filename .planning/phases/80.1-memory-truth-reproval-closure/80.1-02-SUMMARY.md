---
phase: 80.1-memory-truth-reproval-closure
plan: 02
subsystem: truth-closure-normalization
tags: [truth, degraded-memory, phase-truth, planning-path, invariants]
provides:
  - aligned planning workflow blocking with canonical model-facing memory truth
  - invariant-driven Phase 75 truth synthesis that no longer collapses expected fail-closed degraded posture into generic conditionality
key-files:
  modified:
    - get-stuff-done/bin/lib/degraded-mode.cjs
    - tests/second-brain-status.test.cjs
    - get-stuff-done/bin/lib/phase-truth.cjs
    - tests/phase-truth-hooks.test.cjs
    - .planning/phases/75-degraded-mode-enforcement/75-INVARIANTS.yaml
decisions:
  - "Planning-path blocking now resolves canonical memory loss before degraded-state workflow classification, so context-build and operator surfaces agree on the same subsystem."
  - "Phase 75 truth now uses the explicit invariant contract for same-area closure instead of generic degraded caveats and unrelated global truth posture."
  - "After bounded repair, Phase 75 truth carries exactly one blocker: the verification artifact still reports CONDITIONAL."
metrics:
  completed_at: 2026-03-28T06:08:00Z
  tasks_completed: 2
  commits:
    - 3117569
    - b7d40b9
---

# Phase 80.1 Plan 02: Memory Truth Reproval Closure Summary

**Repaired the exact logic and truth-input blockers from the audit so Phase 75 truth now converges on invariant-driven closure, with only the explicit verification artifact still preventing `VALID`.**

## Accomplishments

- Fixed degraded-mode backend resolution so authoritative `context build --workflow plan-phase --raw` and `context build --workflow execute-plan --raw` now block on `model_facing_memory` with `canonical_postgres_memory_unavailable` instead of drifting to `planning_server_unavailable`.
- Added CLI coverage in `tests/second-brain-status.test.cjs` proving both planning workflows block on the same canonical memory truth under the canonical Postgres-outage posture.
- Converted `75-INVARIANTS.yaml` into the machine-readable contract shape consumed by phase-truth invariant evaluation.
- Updated `get-stuff-done/bin/lib/phase-truth.cjs` so Phase 75 truth uses same-area invariant blockers instead of generic degraded caveats, and added regression coverage in `tests/phase-truth-hooks.test.cjs`.

## Task Commits

1. **Task 1: align planning workflow blocking with canonical memory truth** - `3117569` (feat)
2. **Task 2: synthesize Phase 75 truth from invariant closure** - `b7d40b9` (feat)

## Proof Index

```json
[
  {
    "task": 1,
    "canonical_commit": "3117569",
    "files": [
      "get-stuff-done/bin/lib/degraded-mode.cjs",
      "tests/second-brain-status.test.cjs"
    ],
    "verify": "node get-stuff-done/bin/gsd-tools.cjs context build --workflow plan-phase --raw && node get-stuff-done/bin/gsd-tools.cjs context build --workflow execute-plan --raw",
    "evidence": [
      "node get-stuff-done/bin/gsd-tools.cjs context build --workflow plan-phase --raw",
      "node get-stuff-done/bin/gsd-tools.cjs context build --workflow execute-plan --raw",
      "node --test tests/second-brain-status.test.cjs"
    ],
    "runtime_required": true,
    "runtime_proof": [
      "node --check get-stuff-done/bin/lib/degraded-mode.cjs"
    ]
  },
  {
    "task": 2,
    "canonical_commit": "b7d40b9",
    "files": [
      ".planning/phases/75-degraded-mode-enforcement/75-INVARIANTS.yaml",
      "get-stuff-done/bin/lib/phase-truth.cjs",
      "tests/phase-truth-hooks.test.cjs"
    ],
    "verify": "node --test tests/phase-truth-hooks.test.cjs && node get-stuff-done/bin/gsd-tools.cjs phase-truth generate 75 --raw",
    "evidence": [
      "node --test tests/phase-truth-hooks.test.cjs",
      "node -e \"const pt=require('./get-stuff-done/bin/lib/phase-truth.cjs'); console.log(JSON.stringify(pt.derivePhaseInvariantAudit(process.cwd(),'75').summary,null,2));\"",
      "node get-stuff-done/bin/gsd-tools.cjs phase-truth generate 75 --raw"
    ],
    "runtime_required": true,
    "runtime_proof": [
      "node --check get-stuff-done/bin/lib/phase-truth.cjs"
    ]
  }
]
```

## Resulting Closure State

- Phase 75 invariant audit: `5 PASS / 0 FAIL / 0 MISSING`
- Phase 75 truth: `CONDITIONAL`
- Remaining reason: `Phase verification artifact reports CONDITIONAL.`

## Next Plan Readiness

- Plan `80.1-03` can now focus cleanly on explicit artifact regeneration:
  - refresh `75-VERIFICATION.md` to `VALID`
  - regenerate `75-TRUTH.yaml`
  - compare outputs
  - only then attempt residual Phase 74 closure

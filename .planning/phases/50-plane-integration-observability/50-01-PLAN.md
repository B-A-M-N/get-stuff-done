---
phase: 50
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-stuff-done/bin/lib/plane-health.cjs
  - get-stuff-done/bin/lib/plane-client.cjs
  - get-stuff-done/bin/lib/second-brain.cjs
  - get-stuff-done/bin/gsd-tools.cjs
  - tests/plane-health.test.cjs
autonomous: true
requirements:
  - OBSERV-PLANE-01
  - OBSERV-PLANE-02
must_haves:
  truths:
    - "GSD exposes a Plane-specific operator status surface instead of forcing operators to infer Plane health from generic logs"
    - "The status surface reports configuration state, recent outbound action health, recent inbound webhook freshness, and recent top Plane failures"
    - "Repeated recent Plane failures open a visible circuit breaker or degraded state instead of silently thrashing repeated sync attempts"
    - "A successful Plane request closes or improves breaker state without requiring manual cleanup"
  artifacts:
    - path: "get-stuff-done/bin/lib/plane-health.cjs"
      provides: "Plane health summary assembly and circuit-breaker status helpers"
      contains: "getPlaneStatus / recordPlaneResult / shouldAllowPlaneRequest"
    - path: "get-stuff-done/bin/lib/plane-client.cjs"
      provides: "Plane request integration with breaker-aware request gating and result recording"
      contains: "_request(action, endpoint"
    - path: "get-stuff-done/bin/lib/second-brain.cjs"
      provides: "Plane-specific audit and health aggregation from the canonical audit stream"
      contains: "getPlaneHealthSummary / getPlaneAudit"
    - path: "get-stuff-done/bin/gsd-tools.cjs"
      provides: "Operator-facing Plane status and audit commands"
      contains: "case 'plane'"
    - path: "tests/plane-health.test.cjs"
      provides: "Focused coverage for Plane health summaries, breaker behavior, and CLI-facing status logic"
      contains: "plane status and breaker tests"
  key_links:
    - from: "gsd-tools.cjs:plane status"
      to: "plane-health.cjs:getPlaneStatus"
      via: "CLI route dispatch"
      pattern: "case 'plane'"
    - from: "plane-health.cjs:getPlaneStatus"
      to: "second-brain.cjs:getPlaneHealthSummary"
      via: "audit-backed summary aggregation"
      pattern: "getPlaneHealthSummary"
    - from: "plane-client.cjs:_request"
      to: "plane-health.cjs:recordPlaneResult"
      via: "success/error result recording after each outbound request"
      pattern: "recordPlaneResult"
    - from: "plane-client.cjs:_request"
      to: "plane-health.cjs:shouldAllowPlaneRequest"
      via: "breaker gate before outbound Plane calls"
      pattern: "shouldAllowPlaneRequest"
---

<objective>
Make the existing Plane integration operable by adding a deterministic operator status surface, audit-backed health summary, and a lightweight circuit breaker that prevents repeated outbound thrash during real Plane outages.
</objective>

<execution_context>
@/home/bamn/get-stuff-done/get-stuff-done/workflows/execute-plan.md
@/home/bamn/get-stuff-done/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-plane-integration-observability/50-RESEARCH.md
@.planning/phases/49-plane-webhooks-incremental-sync/49-01-SUMMARY.md
@.planning/phases/49-plane-webhooks-incremental-sync/49-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused tests for Plane status and breaker behavior</name>
  <files>
    tests/plane-health.test.cjs
  </files>
  <read_first>
    - tests/plane-client.test.cjs
    - tests/plane-webhook-sync.test.cjs
    - get-stuff-done/bin/lib/plane-client.cjs
    - get-stuff-done/bin/lib/second-brain.cjs
  </read_first>
  <action>
    Create `tests/plane-health.test.cjs` using the repo's `node:test` style.

    Cover:
    - `getPlaneStatus()` reports config flags and summarized audit health from stubbed audit rows
    - recent inbound webhook activity appears as `last_webhook_received_at` or equivalent freshness signal
    - repeated recent outbound failures open the breaker and cause `shouldAllowPlaneRequest()` to return a degraded or blocked result
    - a subsequent successful request closes or improves breaker state
    - CLI-facing output for `plane status` and optional `plane audit` does not require live Plane connectivity

    Prefer isolated module tests with stubbed `second-brain` data over network-bound integration tests.
  </action>
  <verify>
    <automated>
      test -f tests/plane-health.test.cjs
    </automated>
  </verify>
  <done>
    Phase 50 behavior is pinned by focused tests before observability code is added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Plane audit-backed health summary and breaker helpers</name>
  <files>
    get-stuff-done/bin/lib/plane-health.cjs
    get-stuff-done/bin/lib/second-brain.cjs
    get-stuff-done/bin/lib/plane-client.cjs
  </files>
  <read_first>
    - get-stuff-done/bin/lib/second-brain.cjs
    - get-stuff-done/bin/lib/plane-client.cjs
    - get-stuff-done/bin/lib/firecrawl-client.cjs
    - get-stuff-done/bin/lib/core.cjs
  </read_first>
  <action>
    Create `get-stuff-done/bin/lib/plane-health.cjs` as the narrow Plane observability module.

    Required behavior:
    - expose `shouldAllowPlaneRequest()` and `recordPlaneResult()` for lightweight in-memory breaker control
    - expose `getPlaneStatus()` that combines config state, breaker state, and audit-backed health summary
    - keep breaker scope local to the current process and current Plane host/project configuration

    Extend `get-stuff-done/bin/lib/second-brain.cjs` with:
    - `getPlaneHealthSummary()` built from the canonical `firecrawl_audit` stream filtered to `plane-*`
    - optional `getPlaneAudit(limit, filters)` helper if it simplifies a `plane audit` command
    - summary fields for recent outbound action counts, recent error rate, top failing actions, and recent webhook freshness signal

    Update `get-stuff-done/bin/lib/plane-client.cjs` so outbound Plane requests:
    - check the breaker before sending
    - record success/error outcomes after each request
    - surface degraded skip behavior clearly without crashing callers

    Keep the design intentionally lightweight. Do not add persistent breaker state or an external metrics backend in this plan.
  </action>
  <verify>
    <automated>
      test -f get-stuff-done/bin/lib/plane-health.cjs &&
      grep -q "getPlaneHealthSummary" get-stuff-done/bin/lib/second-brain.cjs &&
      grep -q "recordPlaneResult" get-stuff-done/bin/lib/plane-client.cjs
    </automated>
  </verify>
  <done>
    Plane health aggregation and breaker-aware outbound behavior exist as testable modules.
  </done>
</task>

<task type="auto">
  <name>Task 3: Expose operator-facing Plane status commands</name>
  <files>
    get-stuff-done/bin/gsd-tools.cjs
    get-stuff-done/bin/lib/plane-health.cjs
  </files>
  <read_first>
    - get-stuff-done/bin/gsd-tools.cjs
    - get-stuff-done/bin/lib/plane-health.cjs
    - get-stuff-done/bin/lib/second-brain.cjs
  </read_first>
  <action>
    Extend `get-stuff-done/bin/gsd-tools.cjs` with a new `plane` command surface.

    Required route:
    - `plane status`

    Strongly recommended in the same plan if implementation stays small:
    - `plane audit [--limit N]`

    Output requirements:
    - report whether Plane config is present (`PLANE_API_URL`, `PLANE_API_KEY`, `PLANE_PROJECT_ID`, `PLANE_WEBHOOK_TOKEN`)
    - report breaker state and whether outbound Plane sync is currently allowed
    - report recent outbound success/error shape and last inbound webhook timestamp
    - fail clearly on bad subcommands without requiring live Plane access

    Reuse the existing `firecrawl health` / `firecrawl audit` CLI shape where practical instead of inventing a new operator UI.
  </action>
  <verify>
    <automated>
      grep -q "case 'plane'" get-stuff-done/bin/gsd-tools.cjs
    </automated>
  </verify>
  <done>
    Operators can inspect Plane integration state directly from the CLI.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify syntax and focused observability behavior</name>
  <files>
    tests/plane-health.test.cjs
    get-stuff-done/bin/lib/plane-health.cjs
    get-stuff-done/bin/lib/plane-client.cjs
    get-stuff-done/bin/lib/second-brain.cjs
    get-stuff-done/bin/gsd-tools.cjs
  </files>
  <action>
    Run focused verification:
    - `node --check tests/plane-health.test.cjs`
    - `node --check get-stuff-done/bin/lib/plane-health.cjs`
    - `node --check get-stuff-done/bin/lib/plane-client.cjs`
    - `node --check get-stuff-done/bin/lib/second-brain.cjs`
    - `node --check get-stuff-done/bin/gsd-tools.cjs`
    - `node --test tests/plane-health.test.cjs`

    If a small disposable CLI smoke check is useful, run:
    - `node get-stuff-done/bin/gsd-tools.cjs plane status --raw`

    Fix failures before considering the plan complete.
  </action>
  <verify>
    <automated>
      node --check get-stuff-done/bin/lib/plane-health.cjs &&
      node --check get-stuff-done/bin/lib/plane-client.cjs &&
      node --check get-stuff-done/bin/lib/second-brain.cjs &&
      node --check get-stuff-done/bin/gsd-tools.cjs
    </automated>
  </verify>
  <done>
    The operator surface is syntactically clean and covered by passing focused tests.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/plane-health.test.cjs`
- `node get-stuff-done/bin/gsd-tools.cjs plane status --raw`
- optional `node get-stuff-done/bin/gsd-tools.cjs plane audit --limit 5 --raw`
</verification>

<success_criteria>
Phase 50 is satisfied when an operator can ask GSD for Plane status and get a truthful report of configuration, recent outbound and inbound health, and breaker state, and when repeated Plane failures stop producing silent retry thrash because the integration advertises and respects a lightweight degraded mode.
</success_criteria>

<output>
After completion, create `.planning/phases/50-plane-integration-observability/50-01-SUMMARY.md`
</output>

<!-- GSD-AUTHORITY: 50-01-1:31dc8a3e9ecf7e643cdfa10bca692acdbd73fffb8ef2d6c3019650940cc48880 -->

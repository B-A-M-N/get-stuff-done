---
phase: 77-execution-surface-governance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-stuff-done/bin/gsd-tools.cjs
  - get-stuff-done/bin/lib/context.cjs
  - get-stuff-done/bin/lib/verify.cjs
  - get-stuff-done/bin/lib/commands.cjs
  - tests/command-governance-enforcement.test.cjs
  - tests/enforcement.test.cjs
autonomous: true
requirements:
  - TRUTH-DEGRADE-01
  - TRUTH-BYPASS-01
  - TRUTH-OPS-01
  - TRUTH-CLAIM-01
must_haves:
  truths:
    - "Top-level CLI routing uses command governance to decide whether a degraded or unsafe condition should warn, block, or remain recovery-only."
    - "Recovery-only commands remain runnable even in unsafe posture."
    - "Hard-gated truth transitions still block under unsafe posture."
    - "Warn-only commands remain runnable and emit structured warnings every invocation."
    - "Subsystem fail-closed backstops remain intact even though top-level blocking narrows."
  artifacts:
    - path: "get-stuff-done/bin/gsd-tools.cjs"
      provides: "Governance-aware CLI consequence handling."
    - path: "get-stuff-done/bin/lib/context.cjs"
      provides: "Authoritative planning routes stay hard-gated."
    - path: "get-stuff-done/bin/lib/verify.cjs"
      provides: "Truth-claiming verification routes stay hard-gated."
    - path: "tests/command-governance-enforcement.test.cjs"
      provides: "Coverage for warn-only, recovery-only, and hard-gated route behavior."
  key_links:
    - from: "get-stuff-done/bin/lib/command-governance.cjs"
      to: "get-stuff-done/bin/gsd-tools.cjs"
      via: "route governance lookup before degraded consequence handling"
      pattern: "warn_only|hard_gated_state_transition|recovery_only"
    - from: "get-stuff-done/bin/gsd-tools.cjs"
      to: "tests/command-governance-enforcement.test.cjs"
      via: "governance-based CLI behavior under degraded and unsafe states"
      pattern: "warning|allowed|blocked|recovery_only"
    - from: "get-stuff-done/bin/gsd-tools.cjs"
      to: "get-stuff-done/bin/lib/context.cjs"
      via: "authoritative planning stays hard-gated even after governance narrowing"
      pattern: "plan-phase|execute-plan|UNSAFE"
---

<objective>
Wire the command-governance map into CLI routing so execution stays broadly usable while authoritative truth transitions remain strictly protected.
</objective>

<execution_context>
@/home/bamn/.codex/get-shit-done/workflows/execute-plan.md
@/home/bamn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/77-execution-surface-governance/77-CONTEXT.md
@.planning/phases/77-execution-surface-governance/77-RESEARCH.md
@.planning/policy/command-governance.yaml
@get-stuff-done/bin/lib/command-governance.cjs
@get-stuff-done/bin/lib/degraded-mode.cjs
@get-stuff-done/bin/gsd-tools.cjs
@get-stuff-done/bin/lib/context.cjs
@get-stuff-done/bin/lib/verify.cjs
</context>

## Locked Decisions

Decision: CLI narrowing changes top-level route behavior, not subsystem fail-closed safety.
Decision: recovery-only commands are always allowed.
Decision: hard-gated state transitions remain blocked under `UNSAFE`.
Decision: warn-only commands remain runnable and must emit structured warnings every invocation.
Decision: command governance reuses the Phase 75 degraded-state artifact as its policy input.

## Risks

Risk: top-level route narrowing could accidentally weaken truth safety if it bypasses existing hard-gated paths.
Guardrail: keep context and verification backstops intact and add route-behavior tests for unsafe posture.
Fallback: if a route consequence is ambiguous, prefer warning or hard-gating over silent ungated execution.

## Assumptions

Assumption: `commands.cjs` and diagnostic reader surfaces can remain available without changing subsystem safety semantics.
Assumption: route-level warning payloads can reuse existing blocked-flow structure with an explicit warning marker.
Deferred: exact operator-facing phrasing can evolve so long as structured raw output remains stable.

<tasks>

<task type="auto" id="77-02-01" tdd="true">
  <name>Task 1: apply governance-aware route consequences at the CLI layer</name>
  <files>tests/command-governance-enforcement.test.cjs, get-stuff-done/bin/gsd-tools.cjs, get-stuff-done/bin/lib/commands.cjs</files>
  <read_first>
- .planning/phases/77-execution-surface-governance/77-CONTEXT.md
- get-stuff-done/bin/gsd-tools.cjs
- get-stuff-done/bin/lib/commands.cjs
  </read_first>
  <acceptance_criteria>
- Recovery-only routes remain runnable in unsafe posture.
- Warn-only routes remain runnable and emit structured warnings.
- Hard-gated truth transitions still block when unsafe.
- CLI routing consults the canonical governance map rather than scattered inline policy.
  </acceptance_criteria>
  <behavior>
    - Test 1: a recovery command such as `drift status` still runs in unsafe posture.
    - Test 2: a warn-only route emits structured warning output while remaining allowed.
    - Test 3: a hard-gated route remains blocked under unsafe posture.
  </behavior>
  <action>
Write focused CLI governance tests first. Then wire `gsd-tools.cjs` and any necessary command readers to apply governance class before choosing block, warn, or allow.
  </action>
  <verify>
    <automated>node --test tests/command-governance-enforcement.test.cjs</automated>
  </verify>
  <done>Phase 77 CLI routing preserves flow for non-authoritative work and strict blocking for truth transitions.</done>
</task>

<task type="auto" id="77-02-02" tdd="true">
  <name>Task 2: preserve hard-gated backstops for authoritative planning and verification routes</name>
  <files>tests/enforcement.test.cjs, get-stuff-done/bin/lib/context.cjs, get-stuff-done/bin/lib/verify.cjs, get-stuff-done/bin/gsd-tools.cjs</files>
  <read_first>
- get-stuff-done/bin/lib/context.cjs
- get-stuff-done/bin/lib/verify.cjs
- get-stuff-done/bin/lib/degraded-mode.cjs
  </read_first>
  <acceptance_criteria>
- Authoritative planning context build remains hard-gated under unsafe posture.
- Current-truth verification and integrity claims remain hard-gated under unsafe posture.
- Governance narrowing does not regress the Phase 75 fail-closed boundary for subsystem helpers.
  </acceptance_criteria>
  <behavior>
    - Test 1: `context build --workflow plan-phase` remains blocked under unsafe posture.
    - Test 2: `verify integrity` remains blocked under unsafe posture.
    - Test 3: diagnostic readers remain available while these truth transitions stay blocked.
  </behavior>
  <action>
Extend enforcement coverage around planning and verification routes after governance wiring lands, ensuring the narrowed CLI surface does not weaken existing authoritative backstops.
  </action>
  <verify>
    <automated>node --test tests/command-governance-enforcement.test.cjs tests/enforcement.test.cjs</automated>
  </verify>
  <done>Phase 77 is usable at the CLI surface without weakening hard-gated truth transitions.</done>
</task>

</tasks>

<verification>
- `node --test tests/command-governance-enforcement.test.cjs`
- `node --test tests/enforcement.test.cjs`
- `node --check get-stuff-done/bin/gsd-tools.cjs`
- `node --check get-stuff-done/bin/lib/context.cjs`
- `node --check get-stuff-done/bin/lib/verify.cjs`
</verification>

<success_criteria>
Plan 77-02 is satisfied when the CLI surface preserves fast non-authoritative execution and repair, while authoritative truth transitions remain hard-gated under unsafe posture.
</success_criteria>

<output>
After completion, create `.planning/phases/77-execution-surface-governance/77-02-SUMMARY.md`
</output>

<!-- GSD-AUTHORITY: 77-01-1:2641667c053ad6f9ad5f11b91b403b591c35a1a9fe11be2f3d54e26574e64494 -->

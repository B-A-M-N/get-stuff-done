---
phase: "12"
plan: "01"
name: "Introduce Zod-backed canonical schemas for the ITL runtime surface"
wave: 1
requirements:
  - "CP-02"
  - "FR-09"
  - "TR-01"
---

# PLAN: 12-01 - Introduce Zod-backed canonical schemas for the ITL runtime surface

## Goal
Replace the lightweight ITL normalization layer with explicit Zod-backed canonical schemas that validate the existing runtime contracts without changing user-facing workflow behavior.

## Read First
- `.planning/phases/12-canonical-schema-and-adapter-layer/12-RESEARCH.md`
- `get-stuff-done/bin/lib/itl-schema.cjs`
- `get-stuff-done/bin/lib/itl-extract.cjs`
- `get-stuff-done/bin/lib/itl-ambiguity.cjs`
- `get-stuff-done/bin/lib/itl-audit.cjs`
- `get-stuff-done/bin/lib/itl.cjs`
- `tests/itl.test.cjs`

## Tasks
<task type="auto">
  <name>Define canonical Zod schemas for current ITL outputs</name>
  <files>get-stuff-done/bin/lib/itl-schema.cjs, get-stuff-done/bin/lib/itl*.cjs, tests/itl.test.cjs</files>
  <action>
    Add explicit Zod schemas for interpretation, ambiguity, lockability, audit records, and seed payloads, then use them to validate the current deterministic ITL outputs at the right module boundaries.
  </action>
  <acceptance_criteria>
    - Canonical schemas exist for the runtime data shapes Phase 2 through Phase 11 rely on.
    - Existing ITL helpers emit schema-valid payloads without breaking route or seed semantics.
    - Invalid payloads fail explicitly instead of being silently accepted.
  </acceptance_criteria>
  <verify>Focused node:test coverage demonstrates valid parses and explicit validation failures.</verify>
</task>

<task type="auto">
  <name>Preserve current workflow-facing contracts while tightening internals</name>
  <files>get-stuff-done/bin/lib/itl*.cjs, tests/itl.test.cjs, tests/dostuff.test.cjs</files>
  <action>
    Refactor only the internal normalization and validation path so the narrative-first command surfaces and seed contracts keep the same behavior externally.
  </action>
  <acceptance_criteria>
    - `dostuff` command/workflow assertions still pass.
    - Seed payloads for new-project, discuss-phase, and verify-work remain compatible.
    - Audit payloads stay readable and deterministic.
  </acceptance_criteria>
  <verify>Focused tests confirm command/workflow contracts still match the documented surface.</verify>
</task>

## Verification
- Canonical schema validation exists for the current ITL runtime surface.
- Invalid shapes fail loudly and deterministically.
- Existing narrative-first workflow contracts remain intact.

---
*Status: Planned*

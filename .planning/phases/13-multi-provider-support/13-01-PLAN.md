---
phase: "13"
plan: "01"
name: "Implement concrete provider adapters and a shared registry"
wave: 1
requirements:
  - "CP-02"
  - "FR-08"
  - "FR-10"
  - "TR-02"
---

# PLAN: 13-01 - Implement concrete provider adapters and a shared registry

## Goal
Add concrete Kimi, Gemini, and OpenAI adapters behind a shared registry so real provider integration exists without changing the canonical ITL contract.

## Read First
- `.planning/phases/13-multi-provider-support/13-RESEARCH.md`
- `get-stuff-done/bin/lib/itl-schema.cjs`
- `get-stuff-done/bin/lib/itl-adapters.cjs`
- `get-stuff-done/bin/lib/itl.cjs`
- `.planning/research/ARCHITECTURE.md`
- `.planning/research/FEATURES.md`
- `.planning/research/PITFALLS.md`

## Tasks
<task type="auto">
  <name>Build a provider registry and concrete adapters for Kimi, Gemini, and OpenAI</name>
  <files>get-stuff-done/bin/lib/itl*.cjs, tests/itl.test.cjs</files>
  <action>
    Introduce a provider registry plus concrete adapters that prepare provider-native request payloads, parse provider responses, and normalize them back into the canonical ITL schema.
  </action>
  <acceptance_criteria>
    - Kimi, Gemini, and OpenAI adapters exist behind a shared registry.
    - Provider output is normalized through the canonical schema before leaving the adapter layer.
    - The deterministic internal adapter remains available as a fallback/offline path.
  </acceptance_criteria>
  <verify>Deterministic fixture tests cover valid provider normalization and malformed payload rejection for each adapter.</verify>
</task>

<task type="auto">
  <name>Keep provider-specific logic out of workflow-facing runtime code</name>
  <files>get-stuff-done/bin/lib/itl*.cjs, tests/itl.test.cjs</files>
  <action>
    Refactor provider selection so `itl.cjs` and workflow surfaces consume a registry/adapter contract instead of branching per provider.
  </action>
  <acceptance_criteria>
    - Provider lookup is centralized.
    - Workflow-facing code continues to consume canonical ITL data only.
    - Unsupported provider selection fails clearly.
  </acceptance_criteria>
  <verify>Tests demonstrate provider selection, fallback behavior, and explicit unknown-provider failures.</verify>
</task>

## Verification
- Concrete provider adapters exist behind a shared registry.
- Provider-specific payloads are normalized back into canonical ITL data.
- Workflow-facing runtime code stays provider-agnostic.

---
*Status: Planned*

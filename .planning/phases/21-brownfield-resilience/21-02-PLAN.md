---
phase: 21
plan: "02"
type: standard
wave: 1
depends_on: ["21-01"]
files_modified:
  - get-stuff-done/workflows/resume-project.md
  - tests/brownfield-mega-audit.test.cjs
autonomous: true
must_haves:
  truths:
    - resume-project provides recovery path for orphaned blocked states
    - Brownfield Mega Audit covers deadlock recovery
  artifacts:
    - path: tests/brownfield-mega-audit.test.cjs
      exists: true
---

# Plan 21-02: Deadlock Detection & State Repair

**Goal:** Prevent systemic deadlocks when legacy projects have "ghost" blocked states without artifacts.

<task type="auto">
<name>Update resume-project.md with State Repair Path</name>
<read_first>
- get-stuff-done/workflows/resume-project.md
</read_first>
<files>
- get-stuff-done/workflows/resume-project.md
</files>
<acceptance_criteria>
- If `CLARIFICATION_STATUS` is `blocked` but `CHECKPOINT_PATH` is missing or invalid, the user is offered a "Force Unblock" or "Re-run Interpretation" path.
- The workflow does not exit without providing a recovery command.
</acceptance_criteria>
<action>
Modify the `check_checkpoint_artifact` step in `resume-project.md`. 
Add logic to detect if the block is "orphaned" (no file).
If orphaned, display a "DEADLOCK DETECTED" warning and provide the `gsd-tools state record-session --clarification-status none` command as a recovery path.
</action>
<verify>
Manual inspection of `resume-project.md` logic.
</verify>
<done>
State repair path added to resume workflow.
</done>
</task>

<task type="auto">
<name>Add Deadlock Resilience Test to Mega Audit</name>
<read_first>
- tests/brownfield-mega-audit.test.cjs
- get-stuff-done/bin/gsd-tools.cjs
</read_first>
<files>
- tests/brownfield-mega-audit.test.cjs
</files>
<acceptance_criteria>
- New test simulates an orphaned blocked state.
- Test verifies that `gsd-tools init resume` (or equivalent check) identifies the anomaly.
</acceptance_criteria>
<action>
Update `tests/brownfield-mega-audit.test.cjs`.
Add a test case "Deadlock Resilience: Orphaned Blocked State".
Setup: `STATE.md` with `clarification_status: blocked` but no `CHECKPOINT.md`.
Assert: The system correctly reports the status so the workflow can trigger the repair path.
</action>
<verify>
`node --test tests/brownfield-mega-audit.test.cjs`
</verify>
<done>
Deadlock test added and passing.
</done>
</task>

## Verification Criteria
- [ ] `tests/brownfield-mega-audit.test.cjs` passes all tests.
- [ ] `resume-project.md` contains the recovery path logic for missing artifacts.

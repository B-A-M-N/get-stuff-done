---
phase: 21
plan: "01"
type: standard
wave: 1
depends_on: []
files_modified:
  - get-stuff-done/bin/lib/artifact-schema.cjs
  - get-stuff-done/bin/lib/verify.cjs
  - tests/brownfield-mega-audit.test.cjs
autonomous: true
must_haves:
  truths:
    - executionSummarySchema supports legacy Phase 1-14 summaries
    - cmdVerifySummary downgrades legacy schema errors to warnings
  artifacts:
    - path: tests/brownfield-mega-audit.test.cjs
      exists: true
---

# Plan 21-01: Schema Resilience & Legacy Fixtures

**Goal:** Ensure Zod schemas handle legacy GSD data without crashing and provide descriptive error routing.

<task type="auto">
<name>Refactor executionSummarySchema for Legacy Support</name>
<read_first>
- get-stuff-done/bin/lib/artifact-schema.cjs
</read_first>
<files>
- get-stuff-done/bin/lib/artifact-schema.cjs
</files>
<acceptance_criteria>
- Schema allows `name` as an alias for `subsystem` for phases < 15.
- Mandatory v0.2.0 fields (`duration`, `completed`, `tags`, `provides`) are optional for legacy phases.
</acceptance_criteria>
<action>
Update `executionSummarySchema` in `artifact-schema.cjs` to use Zod's `.superRefine()` or similar logic to handle legacy field mappings and conditional requirements based on the `phase` number.
</action>
<verify>
`node --test tests/artifact-schema.test.cjs`
</verify>
<done>
Legacy fields supported in schema.
</done>
</task>

<task type="auto">
<name>Create Brownfield Mega Audit Test Suite</name>
<read_first>
- tests/helpers.cjs
</read_first>
<files>
- tests/brownfield-mega-audit.test.cjs
</files>
<acceptance_criteria>
- `tests/brownfield-mega-audit.test.cjs` exists.
- It iterates over real legacy summaries from `.planning/phases/01-14/`.
- It asserts that `cmdVerifySummary` does not throw unhandled exceptions.
</acceptance_criteria>
<action>
Implement `tests/brownfield-mega-audit.test.cjs`. 
Include a "Legacy Ingestion" test that feeds actual Phase 1-14 markdown files through the verification gate.
</action>
<verify>
`node --test tests/brownfield-mega-audit.test.cjs`
</verify>
<done>
Test suite implemented and passing for legacy summaries.
</done>
</task>

<task type="auto">
<name>Harden cmdVerifySummary Error Routing</name>
<read_first>
- get-stuff-done/bin/lib/verify.cjs
</read_first>
<files>
- get-stuff-done/bin/lib/verify.cjs
</files>
<acceptance_criteria>
- Verification failures for legacy files return `valid: true` with a `legacy: true` warning flag, OR clear error messages that don't halt the core loop.
</acceptance_criteria>
<action>
Modify `cmdVerifySummary` in `verify.cjs` to catch schema errors and, if the phase is legacy, downgrade them to warnings.
</action>
<verify>
`node --test tests/brownfield-mega-audit.test.cjs`
</verify>
<done>
Error routing hardened for legacy data.
</done>
</task>

## Verification Criteria
- [ ] All 21-01 tests in `tests/brownfield-mega-audit.test.cjs` pass.
- [ ] All regression tests in `tests/verify.test.cjs` pass.
- [ ] Manual verification confirms legacy summaries pass with warnings.

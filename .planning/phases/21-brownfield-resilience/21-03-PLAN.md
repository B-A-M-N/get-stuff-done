---
phase: 21
plan: "03"
type: standard
wave: 1
depends_on: ["21-02"]
files_modified:
  - get-stuff-done/bin/lib/state.cjs
  - tests/brownfield-mega-audit.test.cjs
autonomous: true
must_haves:
  truths:
    - Context harvesting handles massive STATE.md files without overflow
    - Truncation logic preserves most recent decisions
  artifacts:
    - path: tests/brownfield-mega-audit.test.cjs
      exists: true
---

# Plan 21-03: Large-Scale Context Harvesting & Truncation

**Goal:** Ensure pre-flight context enrichment stays within model context limits for massive legacy projects.

<task type="auto">
<name>Implement Token-Aware Truncation in harvestAmbientContext</name>
<read_first>
- get-stuff-done/bin/lib/state.cjs
</read_first>
<files>
- get-stuff-done/bin/lib/state.cjs
</files>
<acceptance_criteria>
- `phase_decisions` are limited to the most recent 20 items.
- Project goals and constraints are truncated if they exceed 5,000 characters total.
- A `truncated: true` flag is added to the harvest JSON if any section was shortened.
</acceptance_criteria>
<action>
Modify `harvestAmbientContext` in `state.cjs`.
Add slicing to `phase_decisions` (limit to 20).
Add character-based truncation to large text blocks from `PROJECT.md`.
Ensure the returned object includes a `truncated` status.
</action>
<verify>
Unit test with a simulated massive project.
</verify>
<done>
Truncation logic implemented in context harvesting.
</done>
</task>

<task type="auto">
<name>Add Scale Test to Mega Audit</name>
<read_first>
- tests/brownfield-mega-audit.test.cjs
</read_first>
<files>
- tests/brownfield-mega-audit.test.cjs
</files>
<acceptance_criteria>
- Test simulates a 2,000-line `STATE.md` and `PROJECT.md`.
- Test verifies that `harvestAmbientContext` returns a valid, non-crashing object with `truncated: true`.
</acceptance_criteria>
<action>
Update `tests/brownfield-mega-audit.test.cjs`.
Add "Scale Test: Massive Context Ingestion".
Assert the `truncated` flag and data structure integrity.
</action>
<verify>
`node --test tests/brownfield-mega-audit.test.cjs`
</verify>
<done>
Scale test added and passing.
</done>
</task>

## Verification Criteria
- [ ] `tests/brownfield-mega-audit.test.cjs` passes all tests.
- [ ] `gsd-tools state harvest-context --phase 21` reports `truncated: true` when fed with artificial bloat.

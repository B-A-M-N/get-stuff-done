---
phase: 21
plan: "04"
type: standard
wave: 1
depends_on: ["21-03"]
files_modified:
  - get-stuff-done/workflows/
  - tests/brownfield-mega-audit.test.cjs
autonomous: true
must_haves:
  truths:
    - Workflow library move did not break internal references
    - Library files are accessible to orchestrator
  artifacts:
    - path: tests/brownfield-mega-audit.test.cjs
      exists: true
---

# Plan 21-04: Workflow Library Backward Compatibility

**Goal:** Ensure the internal library extraction (Phase 19) is robust and backward-compatible.

<task type="auto">
<name>Verify Internal Workflow References</name>
<read_first>
- get-stuff-done/workflows/
</read_first>
<files>
- get-stuff-done/workflows/
</files>
<acceptance_criteria>
- No dangling references to `workflows/discovery-phase.md` or other moved files exist in the `workflows/` directory.
- All references point to `workflows/lib/`.
</acceptance_criteria>
<action>
Run `grep -r "workflows/" get-stuff-done/workflows/`.
Identify any files still using the old path.
Update them if found.
</action>
<verify>
`grep -r "workflows/discovery-phase.md" get-stuff-done/workflows/` returns 0 results outside `lib/`.
</verify>
<done>
Internal references verified and fixed.
</done>
</task>

<task type="auto">
<name>Add Library Access Test to Mega Audit</name>
<read_first>
- tests/brownfield-mega-audit.test.cjs
</read_first>
<files>
- tests/brownfield-mega-audit.test.cjs
</files>
<acceptance_criteria>
- Test verifies that the orchestrator can read a file from `workflows/lib/`.
</acceptance_criteria>
<action>
Update `tests/brownfield-mega-audit.test.cjs`.
Add "Integration Test: Library Accessibility".
Assert that `workflows/lib/diagnose-issues.md` is readable and contains the expected purpose.
</action>
<verify>
`node --test tests/brownfield-mega-audit.test.cjs`
</verify>
<done>
Library accessibility test added and passing.
</done>
</task>

## Verification Criteria
- [ ] `tests/brownfield-mega-audit.test.cjs` passes all tests.
- [ ] `grep` confirms no old paths remain.

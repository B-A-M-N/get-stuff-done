---
phase: 24-research-canonical-identity-storage
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified: [get-stuff-done/bin/gsd-tools.cjs, get-stuff-done/bin/lib/context.cjs]
autonomous: true
requirements: [CLI-INTEGRATION]

must_haves:
  truths:
    - "context read bundles multiple artifacts into a single markdown stream"
    - "context build can normalize and store a new context artifact"
    - "CLI provides clear error messages for missing artifacts"
  artifacts:
    - path: "get-stuff-done/bin/lib/context.cjs"
      provides: "cmdContextRead, cmdContextNormalize"
  key_links:
    - from: "get-stuff-done/bin/gsd-tools.cjs"
      to: "get-stuff-done/bin/lib/context.cjs"
      via: "cmdContextRead, cmdContextNormalize"
---

<objective>
Integrate the canonical identity and storage system into the GSD CLI. This allows agents and users to normalize new context pieces and retrieve bundled context for execution.

Purpose: Provide CLI interfaces for context normalization and retrieval.
Output: Updated gsd-tools.cjs and context.cjs.
</objective>

<execution_context>
@get-stuff-done/workflows/execute-plan.md
@get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-research-canonical-identity-storage/24-RESEARCH.md
@get-stuff-done/bin/gsd-tools.cjs
@get-stuff-done/bin/lib/context.cjs
@get-stuff-done/bin/lib/context-store.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Context CLI Logic</name>
  <files>get-stuff-done/bin/lib/context.cjs</files>
  <action>
    Implement cmdContextRead(cwd, ids) and cmdContextNormalize(cwd, sourceUri, contentPath, options) in context.cjs.
    
    cmdContextRead:
    - Map ids to artifacts using context-store.get().
    - Filter out nulls.
    - Format as a bundle with YAML separators as shown in RESEARCH.md.
    - Write to stdout.

    cmdContextNormalize:
    - Read content from contentPath.
    - Generate artifact object (using generateArtifactId from context-artifact.cjs).
    - Store using context-store.put().
    - Output the generated ID.
  </action>
  <verify>
    <automated>node -e 'const context = require("./get-stuff-done/bin/lib/context.cjs"); console.log(typeof context.cmdContextRead)' | grep "function"</automated>
  </verify>
  <done>context.cjs exports cmdContextRead and cmdContextNormalize.</done>
</task>

<task type="auto">
  <name>Task 2: Update GSD Tools Routing</name>
  <files>get-stuff-done/bin/gsd-tools.cjs</files>
  <action>
    Update the 'context' case in gsd-tools.cjs to handle 'read' and 'normalize' subcommands.
    
    'context read':
    - Collect all positional arguments after 'read' as IDs.
    - Call context.cmdContextRead(cwd, ids).
    
    'context normalize':
    - Parse --source and --file (or --content).
    - Call context.cmdContextNormalize(cwd, source, file).
    
    Ensure 'context build' still works for workflow snapshots.
  </action>
  <verify>
    <automated>node ./get-stuff-done/bin/gsd-tools.cjs context --help 2>&1 | grep "read"</automated>
  </verify>
  <done>gsd-tools.cjs correctly routes context read and context normalize commands.</done>
</task>

</tasks>

<verification>
Verify that 'context normalize' stores an artifact and 'context read' can retrieve it using the ID returned.
</verification>

<success_criteria>
- node gsd-tools.cjs context normalize --source "test" --file "README.md" returns an ID.
- node gsd-tools.cjs context read <ID> returns the formatted markdown bundle.
- Existing context build commands remain functional.
</success_criteria>

<output>
After completion, create `.planning/phases/24-research-canonical-identity-storage/24-03-SUMMARY.md`
</output>

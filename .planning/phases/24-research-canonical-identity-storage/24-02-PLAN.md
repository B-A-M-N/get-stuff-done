---
phase: 24-research-canonical-identity-storage
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified: [get-stuff-done/bin/lib/context-store.cjs, tests/context-store.test.cjs]
autonomous: true
requirements: [STORE-FILE-BACKED]

must_haves:
  truths:
    - "Artifacts are persisted as JSON files in .planning/context/artifacts/"
    - "Artifacts can be retrieved by ID"
    - "All artifacts in the store can be listed"
    - "Store ensures artifact integrity by re-validating on get/put"
  artifacts:
    - path: "get-stuff-done/bin/lib/context-store.cjs"
      provides: "put, get, list, findBySource"
    - path: "tests/context-store.test.cjs"
      provides: "E2E storage verification"
  key_links:
    - from: "get-stuff-done/bin/lib/context-store.cjs"
      to: "get-stuff-done/bin/lib/artifact-schema.cjs"
      via: "parseContextArtifact"
---

<objective>
Implement a minimal file-backed store for context artifacts. This provides the persistent layer where normalized context is cached and retrieved.

Purpose: Provide a reliable, file-based repository for project context artifacts.
Output: context-store.cjs and context-store.test.cjs.
</objective>

<execution_context>
@get-stuff-done/workflows/execute-plan.md
@get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-research-canonical-identity-storage/24-RESEARCH.md
@get-stuff-done/bin/lib/artifact-schema.cjs
@get-stuff-done/bin/lib/context-artifact.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Context Store Library</name>
  <files>get-stuff-done/bin/lib/context-store.cjs</files>
  <action>
    Create get-stuff-done/bin/lib/context-store.cjs.
    Implement:
    - getStoreDir(cwd): returns path.join(cwd, '.planning', 'context', 'artifacts')
    - put(cwd, artifact): Validates via parseContextArtifact, ensures dir exists, saves to {id}.json.
    - get(cwd, id): Reads {id}.json, parses and validates, returns artifact or null.
    - list(cwd): Returns array of all artifact IDs (filenames minus .json).
    - findBySource(cwd, uri): Lists all and filters by source_uri (for deduplication).
    Use fs and path built-ins. Use parseContextArtifact from artifact-schema.cjs.
  </action>
  <verify>
    <automated>node -e 'const store = require("./get-stuff-done/bin/lib/context-store.cjs"); console.log(typeof store.put)' | grep "function"</automated>
  </verify>
  <done>context-store.cjs exports put, get, list, findBySource functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Context Store Tests</name>
  <files>tests/context-store.test.cjs</files>
  <behavior>
    - Should put an artifact and retrieve it with the same data.
    - Should list multiple saved artifacts.
    - Should find artifacts by source URI.
    - Should generate deterministic IDs when integrated with context-artifact.cjs.
    - Should handle missing files gracefully in get().
  </behavior>
  <action>
    Create tests/context-store.test.cjs.
    Test the full lifecycle: generation of ID -> put to store -> get from store -> list.
    Use a temporary directory for tests if possible, or clean up after.
  </action>
  <verify>
    <automated>node --test tests/context-store.test.cjs</automated>
  </verify>
  <done>Tests pass for context storage and identity integration.</done>
</task>

</tasks>

<verification>
Ensure tests clean up any created test artifacts or use a dedicated test directory.
</verification>

<success_criteria>
- context-store.cjs is fully implemented.
- node --test tests/context-store.test.cjs passes.
- Artifacts are correctly stored in the .planning/context/artifacts/ directory structure.
</success_criteria>

<output>
After completion, create `.planning/phases/24-research-canonical-identity-storage/24-02-SUMMARY.md`
</output>

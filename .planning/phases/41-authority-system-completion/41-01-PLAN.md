---
phase: 41-authority-system-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-stuff-done/bin/lib/core.cjs
  - get-stuff-done/bin/lib/authority.cjs
  - get-stuff-done/bin/lib/state.cjs
  - get-stuff-done/bin/lib/roadmap.cjs
  - get-stuff-done/bin/lib/phase.cjs
  - get-stuff-done/bin/lib/commands.cjs
  - get-stuff-done/bin/lib/planning-server.cjs
  - tests/authority-read-enforce.test.cjs
  - tests/planning-server-auth.test.cjs
  - tests/authority-secret-fallback.test.cjs
  - tests/audit-authority-failure.test.cjs
  - scripts/check-direct-writes.cjs
autonomous: true
requirements:
  - AUTH-COMPLETE-01
  - AUTH-COMPLETE-02
  - AUTH-COMPLETE-03
must_haves:
  truths:
    - "All system writes to .planning/ files include a valid authority envelope with phase-plan-wave"
    - "Reading a .planning/ file with missing or invalid envelope causes process exit 13 and logs an audit entry"
    - "No hardcoded fallback secret is used by authority.getAuthoritySecret"
    - "Sandbox module load failure results in denial (allowed: false) for restricted paths"
    - "Planning server /v1/extract uses safeReadFile and returns 404 on missing file, exits on signature failure"
    - "All four new test files exist and pass"
  artifacts:
    - path: "get-stuff-done/bin/lib/core.cjs"
      provides: "safeReadFile with audit logging and strict sandbox fallback"
    - path: "get-stuff-done/bin/lib/authority.cjs"
      provides: "getAuthoritySecret without hardcoded fallback"
    - path: "tests/authority-read-enforce.test.cjs"
      provides: "Exit code 13 verification test"
    - path: "tests/planning-server-auth.test.cjs"
      provides: "Planning server authority enforcement test"
    - path: "tests/authority-secret-fallback.test.cjs"
      provides: "Fallback secret generation test"
    - path: "tests/audit-authority-failure.test.cjs"
      provides: "Audit entry creation test"
  key_links:
    - from: "safeReadFile"
      to: "authority.verifySignature"
      via: "verification and exit on failure"
      pattern: "authority.verifySignature(content)"
    - from: "safeWriteFile"
      to: "authority.generateSignature"
      via: "signing envelope for writes"
      pattern: "authority.generateSignature(content, options.phase, options.plan, options.wave)"
    - from: "planning-server /v1/extract"
      to: "safeReadFile"
      via: "reading and verifying target file"
      pattern: "safeReadFile(targetPath)"
---

<objective>
Complete authority system enforcement for Phase 41: ensure all writes to system-managed files are signed with phase/plan/wave, all reads verify envelopes and fail securely, fallback secret is non-persistent, and audit logging captures violations.

Purpose: Fulfill guarantees that all modifications are traceable and tampering is detected and logged.

Output: Modified core library files, updated command implementations, and comprehensive test suite verifying the guarantees.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-authority-system-completion/RESEARCH.md

# Key interfaces (executor should use directly):

From get-stuff-done/bin/lib/authority.cjs:
- generateSignature(content, phase, plan, wave)
- verifySignature(content) → {valid, reason, ...}
- signFile(filePath, phase, plan, wave)

From get-stuff-done/bin/lib/core.cjs:
- safeReadFile(filePath) — will be updated to exit on invalid signature
- safeWriteFile(filePath, content, options = {}) — options = {phase, plan, wave} or string "phase:plan:wave"
- safeFs (object with fs methods)

From get-stuff-done/bin/lib/audit.cjs:
- recordAuditEntry(cwd, entry) — call with impact/context

</context>

<tasks>

<task type="auto">
  <name>AUTH-COMPLETE-01: Enforce authority envelope signing for all system writes</name>
  <files>
    get-stuff-done/bin/lib/state.cjs
    get-stuff-done/bin/lib/roadmap.cjs
    get-stuff-done/bin/lib/phase.cjs
    get-stuff-done/bin/lib/commands.cjs
  </files>
  <read_first>
    state.cjs: writeStateMd and its callers.
    roadmap.cjs: cmdRoadmapUpdatePlanProgress and any roadmap writes.
    phase.cjs: Functions that write ROADMAP updates and .gitkeep.
    commands.cjs: cmdCheckpointWrite and other commands that write CHECKPOINT or other .planning files.
  </read_first>
  <action>
    Goal: Ensure every write to `.planning/` system files via safeWriteFile includes explicit `phase`, `plan`, and `wave` options.

    Detailed changes:

    1. state.cjs:
       - Modify writeStateMd signature (around line 912):
         From: `function writeStateMd(statePath, content, cwd)`
         To: `function writeStateMd(statePath, content, cwd, options = {})`
       - Change the safeWriteFile call (line ~914):
         From: `safeWriteFile(statePath, normalizeMd(synced));`
         To: `safeWriteFile(statePath, normalizeMd(synced), options);`
       - Update all internal calls to writeStateMd to include a fourth argument:
         * In cmdStateCheckpoint (line ~954): change `writeStateMd(statePath, content, cwd);` to `writeStateMd(statePath, content, cwd, { phase: options.phase, plan: options.plan, wave: options.task || options.wave || '1' });`
         * In cmdStateBeginPhase (line ~1033): adjust to pass options through. This function signature is `cmdStateBeginPhase(cwd, phaseNumber, phaseName, planCount, raw)`. It does not receive options. Instead, derive phase from phaseNumber and plan from the phase's first plan? Not possible. Instead, modify cmdStateBeginPhase signature to accept an options argument: `(cwd, phaseNumber, phaseName, planCount, options, raw)` and use `{ phase: phaseNumber, plan: options.plan, wave: options.wave || '1' }`. Need to adjust callers as well.
         * Similarly for cmdStateAdvancePlan, cmdStateUpdateProgress, cmdStateAddDecision, cmdStateAddBlocker, cmdStateResolveBlocker, cmdStateRecordSession, cmdStateRecordMetric: ensure they receive or construct appropriate envelope options and forward them to writeStateMd.
         * For simplicity, many of these functions already accept an `options` parameter; ensure they use it to build the envelope object.

    2. roadmap.cjs:
       - Update cmdRoadmapUpdatePlanProgress:
         * Change signature from `function cmdRoadmapUpdatePlanProgress(cwd, phaseNum, raw)` to `function cmdRoadmapUpdatePlanProgress(cwd, phaseNum, options, raw)`.
         * At line 290, change `safeWriteFile(roadmapPath, roadmapContent);` to `safeWriteFile(roadmapPath, roadmapContent, { phase: phaseNum, plan: options.plan, wave: options.wave || '1' });`
         * Add early validation: if (!options.plan) error('--plan required'); (Or the orchestrator will always provide it.)

    3. phase.cjs:
       - For each safeWriteFile that writes to `.planning/` (e.g., in cmdPhaseCreate, cmdPhaseArchive, cmdPhasePromote, cmdPhaseDemote, cmdPhaseAdd), replace the call to include envelope options.
       - Examples:
         * At line ~341 (cmdPhaseAdd): `safeWriteFile(path.join(dirPath, '.gitkeep'), '');` → `safeWriteFile(path.join(dirPath, '.gitkeep'), '', { phase: paddedNum, plan: 'infrastructure', wave: '1' });`
         * At line ~355 (likely cmdPhaseCreate or similar): `safeWriteFile(roadmapPath, updatedContent, 'utf-8');` → `safeWriteFile(roadmapPath, updatedContent, { phase: phase, plan: options.plan, wave: options.wave || '1' });` Ensure the function's signature includes options.
         * Apply similar changes at lines 437, 668, 756, and any other safeWriteFile targeting .planning/.
       - These functions typically have parameters `(cwd, phase, options, raw)`; use `options.plan` and `options.wave`. If options not present, add to signature and propagate from callers.

    4. commands.cjs:
       - In cmdCheckpointWrite (line ~1324): currently `safeWriteFile(checkpointPath, content, 'utf-8');` → change to `safeWriteFile(checkpointPath, content, { phase: options.phase, plan: options.plan, wave: options.task || '1' });` But cmdCheckpointWrite receives `(cwd, phase, options, raw)`. It has phase as separate arg and options contains task. It does not have plan. So we need to add plan handling: either require `options.plan` (modify callers) or derive plan from phase directory. For simplicity, add `if (!options.plan) error('--plan required');` and then use `{ phase: phase, plan: options.plan, wave: options.task || '1' }`.
       - Verify other safeWriteFile calls in commands.cjs that target .planning/ (search for safeWriteFile) and ensure they include envelope options.

    Note: For functions that previously passed 'utf-8' as third argument, that string is now replaced by an options object; safeWriteFile already uses fs.writeFileSync with default utf-8 encoding, so no need to specify encoding.

    After changes, run a grep check: `grep -rn "safeWriteFile.*\.planning" get-stuff-done/bin/lib/ | grep -v "phase:"` should find no matches.
  </action>
  <verify>
    - Write a small script: node -e "const fs=require('fs'); const pattern=/\.planning\//; const api=require('fs').readdirSync; // not real" but easier: grep the repo.
    - Verify all safeWriteFile calls on .planning files include an object with keys phase, plan, wave (could be spread from env but at least object structure).
    - Inspect a generated artifact (e.g., after task execution) to confirm envelope exists.
  </verify>
  <done>
    All critical write operations on .planning/ files now include authority envelope with explicit phase/plan/wave. System can trace each modification to a specific execution wave.
  </done>
</task>

<task type="auto">
  <name>AUTH-COMPLETE-02: Enforce verification for all reads of system files</name>
  <files>
    get-stuff-done/bin/lib/core.cjs
    get-stuff-done/bin/lib/planning-server.cjs
  </files>
  <read_first>
    core.cjs: lines 134-162 (safeReadFile), and functions getMilestoneInfo, getMilestonePhaseFilter.
    planning-server.cjs: /v1/extract route (lines 55-132).
  </read_first>
  <action>
    Goal: Replace all raw fs.readFileSync for .planning/ with safeReadFile and ensure verification is enforced.

    Part 1 — core.cjs:
      A. In getMilestoneInfo (around line 755):
         Locate: `const roadmap = fs.readFileSync(path.join(cwd, '.planning', 'ROADMAP.md'), 'utf-8');`
         Replace with:
           `const roadmap = safeReadFile(path.join(cwd, '.planning', 'ROADMAP.md'));`
           `if (!roadmap) return { version: 'v1.0', name: 'milestone' };`
      B. In getMilestonePhaseFilter (around line 796):
         Locate: `const roadmap = stripShippedMilestones(fs.readFileSync(path.join(cwd, '.planning', 'ROADMAP.md'), 'utf-8'));`
         Replace with:
           `const raw = safeReadFile(path.join(cwd, '.planning', 'ROADMAP.md'));`
           `if (!raw) { const passAll = () => true; passAll.phaseCount = 0; return passAll; }`
           `const roadmap = stripShippedMilestones(raw);`

    Part 2 — planning-server.cjs:
      - Add import at top: `const { safeReadFile } = require('./core.cjs');`
      - In the /v1/extract handler (line ~103), replace the block:
           `const rawContent = fs.readFileSync(targetPath, 'utf-8');`
        with:
           `const rawContent = safeReadFile(targetPath);`
           `if (!rawContent) {`
           `  res.statusCode = 404;`
           `  res.setHeader('Content-Type', 'application/json');`
           `  res.end(JSON.stringify({ error: 'File not found or unreadable' }));`
           `  return;`
           `}`
        (Note: safeReadFile returns null on any error, including verification failure? Actually verification failure calls exit(13), which terminates the process. So if the file exists but signature invalid, the server will exit; the 404 path is only for missing file or other read errors. That's correct.)

    After these changes, all internal library reads of .planning/ will be subject to sandbox check and authority verification.
  </action>
  <verify>
    - `grep` in core.cjs: no more `fs.readFileSync(... '.planning' ...)` lines.
    - `grep` in planning-server.cjs: no direct `fs.readFileSync` for .planning files; safeReadFile is used.
    - Run unit test that tries to read a .planning/ file with invalid signature; expect exit 13.
  </verify>
  <done>
    All internal reads of system files now pass through safeReadFile, guaranteeing mandatory authority verification.
  </done>
</task>

<task type="auto">
  <name>AUTH-COMPLETE-03: Secure fallback and audit logging</name>
  <files>
    get-stuff-done/bin/lib/authority.cjs
    get-stuff-done/bin/lib/core.cjs
  </files>
  <read_first>
    authority.cjs: entire file, especially getAuthoritySecret (lines 16-33).
    core.cjs: safeReadFile (lines 134-162).
  </read_first>
  <action>
    Goal: Remove insecure defaults, tighten sandbox error handling, and add audit trail for authority violations.

    Part A — authority.cjs (remove hardcoded fallback):
      In getAuthoritySecret, modify the catch block (lines 26-32):
        Replace:
          `console.error(...); return 'gsd-authority-fallback-secret-stable-v1';`
        With:
          `const sessionSecret = crypto.randomBytes(64).toString('hex');`
          `console.error('[Authority] Using volatile session secret – persist failed:', err.message);`
          `return sessionSecret;`

    Part B — core.cjs (sandbox fallback):
      In safeReadFile, inside the inner try-catch around sandbox.checkPath (line ~143):
        Change `decision = { allowed: true };` to `decision = { allowed: false };`

    Part C — core.cjs (audit logging before exit):
      Inside safeReadFile, in the verification failure block after `if (!authResult.valid)` (line ~152), before `process.exit(13);`, insert:
        ```javascript
        try {
          const audit = require('./audit.cjs');
          audit.recordAuditEntry(process.cwd(), {
            impact: 'severe',
            context: { action: 'authority_verification_failed', path: filePath, reason: authResult.reason },
            narrative_ref: 'none'
          });
        } catch (e) {
          // Intentionally ignore secondary errors to preserve exit behavior
        }
        ```

    Part D — planning-server already uses safeReadFile from Task 2, so it inherits audit logging; no further changes needed.
  </action>
  <verify>
    - Search authority.cjs for the hardcoded string 'gsd-authority-fallback-secret-stable-v1' – it must not appear.
    - In core.cjs, confirm `decision = { allowed: false }` in the sandbox catch.
    - In core.cjs, confirm a call to `audit.recordAuditEntry` exists immediately before `process.exit(13)`.
  </verify>
  <done>
    Fallback secret is now per-process random, sandbox errors deny access, and every authority violation is audit-logged before termination.
  </done>
</task>

<task type="auto">
  <name>Test Coverage and Regression Validation</name>
  <files>
    tests/authority-read-enforce.test.cjs
    tests/planning-server-auth.test.cjs
    tests/authority-secret-fallback.test.cjs
    tests/audit-authority-failure.test.cjs
    scripts/check-direct-writes.cjs
  </files>
  <read_first>
    tests/authority.test.cjs, tests/core-safeWriteFile.test.cjs, tests/sandbox.test.cjs for style patterns.
    Also review helpers.cjs for test utilities.
  </read_first>
  <action>
    Goal: Create the required test files and ensure the whole suite passes.

    Step 1 — Create tests/authority-read-enforce.test.cjs:
      - Use Node's `test` module (node:test) or `assert`.
      - Create a temporary directory with a mock restricted file (.planning/foo.md) containing content without envelope. safeReadFile should exit 13.
      - Also create a file with a valid envelope and verify safeReadFile returns content.
      - Ensure process exit codes are checked (use `try { safeReadFile(...); } catch (e) {}`? Actually safeReadFile calls process.exit which terminates the whole test process. Use a child process to test exit behavior. Example: spawn node -e "const { safeReadFile } = require('../lib/core.cjs'); safeReadFile('path')". Or use mock of process.exit.
      - Follow patterns in tests/core-safeWriteFile.test.cjs.

    Step 2 — Create tests/planning-server-auth.test.cjs:
      - Start server in a separate process or use http module.
      - Create a valid file under .planning/ (use safeWriteFile to create with envelope).
      - GET /v1/extract?path=... → expect 200 and correct hash.
      - Tamper file (edit and remove envelope line), request again; expect connection closed or server exit. Also check audit ledger for entry.
      - Clean up: kill server, delete temp files.

    Step 3 — Create tests/authority-secret-fallback.test.cjs:
      - Setup: make .gemini_security/authority.key unreadable (chmod 000) or point to invalid location via env?
      - Require authority.cjs fresh (delete require cache) and call getAuthoritySecret().
      - Assert that returned secret is a 128-char hex string and not equal to 'gsd-authority-fallback-secret-stable-v1'.
      - Call again; should return same secret within same process (cached? Actually getAuthoritySecret reads secretPath each call but caches? No, it reads file each call. In case of error, it generates new each call? That would be a problem. In the new code, if reading fails, it generates a new random secret each call? In the catch, we generate sessionSecret and return it. That would be a new secret on each call. But maybe we should cache it in a closure? The current getAuthoritySecret function defines secretPath and returns value; if error occurs, it returns a new random each call. That would break signatures because they'd vary. So we need to cache the secret in a module-level variable. In the research code example they had: `const sessionSecret = crypto.randomBytes(64).toString('hex'); return sessionSecret;` but that would generate a new one on every call when failing. That's wrong. We need to ensure the secret is consistent within a process. The original fallback was stable across calls (hardcoded). We should generate once and cache.
      Let's check getAuthoritySecret implementation:
      ```
      function getAuthoritySecret() {
        const secretPath = ...;
        try {
          if (safeFs.existsSync(secretPath)) {
            return safeFs.readFileSync(secretPath, 'utf-8').trim();
          }
          // ... generate and write if not exists
        } catch (err) {
          // fallback
        }
      }
      ```
      In the current version, if reading the secretFile fails after it was created (e.g., permission error), it falls back to hardcoded. If we change to generate random each time, that's unstable. So we should generate a random secret once and cache in a module variable. We can modify getAuthoritySecret to have a `let cachedSecret = null;` at top, and if cachedSecret exists return it. In catch block, if cachedSecret is null, generate and set cachedSecret, then return it. That ensures consistency.
      Therefore, Task 3 should also include adding caching to authority.cjs. I'll update Part A to include module-level cache and fallback to volatile but consistent secret.
      Document: Add at top: `let volatileSecret = null;`. In catch: `if (!volatileSecret) { volatileSecret = crypto.randomBytes(64).toString('hex'); } return volatileSecret;`.
      We'll include that in the changes for authority.cjs.

      Test: verify that two successive getAuthoritySecret calls in same process return the same volatile secret when persistence fails.

    Step 4 — Create tests/audit-authority-failure.test.cjs:
      - Force safeReadFile to exit 13 by creating a restricted file with invalid envelope.
      - Because safeReadFile exits, we need to capture audit before exit: we can override process.exit and audit.recordAuditEntry using stubs, or run in child process and inspect ledger file after it exits.
      - Simpler: spawn a child node process that calls safeReadFile on the bad file; after it exits, read .planning/audit/ledger.jsonl and verify an entry with impact='severe' and context.action='authority_verification_failed' exists.
      - Use tmp dir for isolation.

    Step 5 — Create scripts/check-direct-writes.cjs:
      - Walk get-stuff-done/bin/lib/; for each .cjs file, regex find calls to fs.writeFileSync or safeFs.writeFileSync where the file path includes '.planning'. Also check for fs.readFileSync on .planning. Output report. Exit 0 if none, 1 if any.
      - Example use: `node scripts/check-direct-writes.cjs`.

    Step 6 — Run full regression:
      - Execute `npm test` from project root.
      - All tests must pass, including the four new ones.
  </action>
  <verify>
    - All four test files exist and are non-empty.
    - scripts/check-direct-writes.cjs exists and is executable.
    - `npm test` completes with exit code 0.
  </verify>
  <done>
    Test coverage for authority enforcement, fallback, and audit is complete. Regression suite passes with no breakage.
  </done>
</task>

</tasks>

<verification>
  After all tasks complete:
  - Run `node scripts/check-direct-writes.cjs`: should report no direct writes to .planning/ without safeWriteFile, and no direct reads without safeReadFile.
  - Create a test plan file using the system and verify it contains a valid GSD-AUTHORITY envelope (regex match).
  - Tamper with a signed file's content and confirm that reading it triggers exit code 13 and an audit entry in .planning/audit/ledger.jsonl.
  - Check that .gemini_security/authority.key contains random hex and not the old fallback string.
</verification>

<success_criteria>
  - All three requirements (AUTH-COMPLETE-01, -02, -03) implemented and verified.
  - All system-managed writes signed with phase-plan-wave.
  - All reads of restricted files enforce verification and exit on failure.
  - Hardcoded fallback eliminated, sandbox fails secure, audit logging in place.
  - New tests added and passing; no regressions.
</success_criteria>

<output>
  After completion, create `.planning/phases/41-authority-system-completion/41-01-SUMMARY.md`
</output>

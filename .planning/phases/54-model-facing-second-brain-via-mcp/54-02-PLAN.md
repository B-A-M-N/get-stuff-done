---
phase: 54-model-facing-second-brain-via-mcp
plan: 02
type: execute
wave: 2
depends_on:
  - 54-01
files_modified:
  - get-stuff-done/bin/lib/context.cjs
  - get-stuff-done/templates/planner-subagent-prompt.md
  - get-stuff-done/bin/lib/second-brain.cjs
  - get-stuff-done/bin/gsd-tools.cjs
  - tests/context-memory-pack.test.cjs
  - tests/firecrawl-memory-boundary.test.cjs
  - tests/executor-memory-writeback.test.cjs
autonomous: true
requirements:
  - MEMORY-MCP-01
  - MEMORY-MCP-02
must_haves:
  truths:
    - "Planner receives bounded curated internal memory through the sanctioned Phase 54 contract."
    - "Executor checkpoint and summary lifecycle events write back through the sanctioned memory contract rather than ad hoc direct database coupling."
    - "External-context retrieval still routes through Firecrawl and does not move to the toolbox memory path."
  artifacts:
    - path: "get-stuff-done/bin/lib/context.cjs"
      provides: "Bounded memory-pack assembly for plan-phase and execute-plan workflows."
    - path: "tests/context-memory-pack.test.cjs"
      provides: "Coverage for bounded memory-pack integration into workflow context."
    - path: "tests/firecrawl-memory-boundary.test.cjs"
      provides: "Coverage that external context is still routed through Firecrawl."
    - path: "tests/executor-memory-writeback.test.cjs"
      provides: "Coverage that checkpoint/summary lifecycle writeback uses sanctioned memory helpers/tool contract."
  key_links:
    - from: "get-stuff-done/bin/lib/context.cjs"
      to: "get-stuff-done/bin/lib/second-brain.cjs"
      via: "bounded workflow memory retrieval and lifecycle writeback"
      pattern: "buildMemoryPack|workflow_memory|checkpoint|summary"
    - from: "tests/firecrawl-memory-boundary.test.cjs"
      to: "get-stuff-done/bin/lib/firecrawl-client.cjs"
      via: "external-context boundary assertion"
      pattern: "Firecrawl|memory pack|toolbox"
---

<objective>
Wire the Phase 54 foundation into real planner and executor workflow lifecycles so read access and writeback both use the sanctioned contract without weakening the Firecrawl boundary.
</objective>

<execution_context>
@/home/bamn/.codex/get-shit-done/workflows/execute-plan.md
@/home/bamn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-model-facing-second-brain-via-mcp/54-CONTEXT.md
@.planning/phases/54-model-facing-second-brain-via-mcp/54-RESEARCH.md
@.planning/phases/54-model-facing-second-brain-via-mcp/54-VALIDATION.md
@.planning/phases/54-model-facing-second-brain-via-mcp/54-01-PLAN.md
@get-stuff-done/bin/lib/context.cjs
@get-stuff-done/bin/lib/second-brain.cjs
@get-stuff-done/bin/lib/firecrawl-client.cjs
@get-stuff-done/bin/gsd-tools.cjs
</context>

<tasks>

<task type="auto" id="54-02-01" tdd="true">
  <name>Task 1: Integrate a bounded internal memory pack into planning and execution context assembly</name>
  <files>get-stuff-done/bin/lib/context.cjs, get-stuff-done/templates/planner-subagent-prompt.md, tests/context-memory-pack.test.cjs, tests/firecrawl-memory-boundary.test.cjs</files>
  <read_first>
- get-stuff-done/bin/lib/context.cjs
- get-stuff-done/templates/planner-subagent-prompt.md
- get-stuff-done/bin/lib/firecrawl-client.cjs
- .planning/phases/54-model-facing-second-brain-via-mcp/54-RESEARCH.md
- .planning/phases/54-model-facing-second-brain-via-mcp/54-CONTEXT.md
- .planning/phases/54-model-facing-second-brain-via-mcp/54-01-PLAN.md
  </read_first>
  <acceptance_criteria>
- `get-stuff-done/bin/lib/context.cjs` contains a bounded memory-pack builder such as `buildMemoryPack` or `memory_pack`.
- The plan-phase and execute-plan context payloads include a structured memory field when Postgres-backed model memory is available.
- `tests/context-memory-pack.test.cjs` asserts the memory pack is bounded and contains curated entries rather than raw audit output.
- `tests/firecrawl-memory-boundary.test.cjs` asserts external URLs or Firecrawl-derived content are not retrieved through the toolbox memory path.
- No prompt/template change instructs agents to fetch arbitrary web or external-doc context from Second Brain memory.
  </acceptance_criteria>
  <behavior>
    - Test 1: Context assembly includes recent relevant decisions, summaries, and blockers from curated workflow memory.
    - Test 2: The returned memory pack is bounded in size and shape.
    - Test 3: External-context retrieval remains Firecrawl-only.
  </behavior>
  <action>
Add a single bounded internal memory-pack step to `get-stuff-done/bin/lib/context.cjs` for the `plan-phase` and `execute-plan` workflows. The pack should contain curated internal memory such as recent decisions, prior summaries, known pitfalls, and unresolved blockers. Keep the output structured and bounded; do not dump raw SQL rows or full audit logs into prompt context.

If the current planner prompt template or related context-injection surface needs to acknowledge the new memory pack, update `get-stuff-done/templates/planner-subagent-prompt.md` with the concrete field name and how it should be consumed. Keep the change narrow: the template should treat the memory pack as additional internal execution memory, not as a replacement for Firecrawl context.

Write `tests/context-memory-pack.test.cjs` and `tests/firecrawl-memory-boundary.test.cjs` first. Assert that the memory pack is present only for internal curated memory and that external-context retrieval still routes through Firecrawl-facing code paths rather than the toolbox memory contract.
  </action>
  <verify>
    <automated>node --test tests/context-memory-pack.test.cjs tests/firecrawl-memory-boundary.test.cjs</automated>
  </verify>
  <done>Planning and execution workflows receive bounded curated internal memory without weakening the Firecrawl external-context boundary.</done>
</task>

<task type="auto" id="54-02-02" tdd="true">
  <name>Task 2: Wire executor checkpoint and summary lifecycle events to sanctioned memory writeback</name>
  <files>get-stuff-done/bin/lib/context.cjs, get-stuff-done/bin/lib/second-brain.cjs, get-stuff-done/bin/gsd-tools.cjs, tests/executor-memory-writeback.test.cjs</files>
  <read_first>
- get-stuff-done/bin/lib/context.cjs
- get-stuff-done/bin/lib/second-brain.cjs
- get-stuff-done/bin/gsd-tools.cjs
- .planning/phases/54-model-facing-second-brain-via-mcp/toolbox/tools.yaml
- .planning/phases/54-model-facing-second-brain-via-mcp/54-RESEARCH.md
- .planning/phases/54-model-facing-second-brain-via-mcp/54-01-PLAN.md
  </read_first>
  <acceptance_criteria>
- A code path exists that writes executor checkpoint memory through the sanctioned Phase 54 writeback helper or tool contract rather than an ad hoc raw DB path.
- A code path exists that writes executor summary memory through the sanctioned Phase 54 writeback helper or tool contract rather than an ad hoc raw DB path.
- `tests/executor-memory-writeback.test.cjs` asserts both checkpoint and summary lifecycle writeback behavior.
- The writeback path is append-only and does not expose generic destructive operations.
  </acceptance_criteria>
  <behavior>
    - Test 1: Creating an executor checkpoint causes a curated checkpoint memory entry to be written through the sanctioned helper/contract.
    - Test 2: Completing a plan/summary causes a curated summary memory entry to be written through the sanctioned helper/contract.
    - Test 3: The implementation does not require planners to have write access.
  </behavior>
  <action>
Connect real executor lifecycle events to the sanctioned Phase 54 writeback path. Use the helper and toolbox contract established in Plan 54-01 so checkpoint and summary memory writes happen through the same curated append-only semantics. The concrete milestones to wire are:
1. executor checkpoint creation writes a `memory_kind` of `checkpoint`;
2. executor completion/summary writes a `memory_kind` of `summary`.

Keep planner access read-only. Do not add a generic write surface to planner-facing prompts, configs, or commands. If the current lifecycle hook point lives in `context.cjs`, `gsd-tools.cjs`, or a nearby workflow helper, wire the sanctioned helper there explicitly and use concrete operation names such as `memory_write_checkpoint` and `memory_write_summary` if needed.

Write `tests/executor-memory-writeback.test.cjs` first. Assert end-to-end that checkpoint and summary lifecycle events use the sanctioned writeback path and that the resulting entries are curated, append-only workflow memory rather than raw audit writes.
  </action>
  <verify>
    <automated>node --test tests/executor-memory-writeback.test.cjs</automated>
  </verify>
  <done>Executor lifecycle events now use the sanctioned Phase 54 writeback path for curated checkpoint and summary memory.</done>
</task>

<task type="auto" id="54-02-03">
  <name>Task 3: Verify workflow integration behavior</name>
  <files>get-stuff-done/bin/lib/context.cjs, get-stuff-done/bin/lib/second-brain.cjs, tests/context-memory-pack.test.cjs, tests/firecrawl-memory-boundary.test.cjs, tests/executor-memory-writeback.test.cjs</files>
  <read_first>
- tests/context-memory-pack.test.cjs
- tests/firecrawl-memory-boundary.test.cjs
- tests/executor-memory-writeback.test.cjs
- .planning/phases/54-model-facing-second-brain-via-mcp/54-VALIDATION.md
  </read_first>
  <acceptance_criteria>
- `node --check get-stuff-done/bin/lib/context.cjs` exits 0.
- `node --check get-stuff-done/bin/lib/second-brain.cjs` exits 0.
- `node --test tests/context-memory-pack.test.cjs tests/firecrawl-memory-boundary.test.cjs tests/executor-memory-writeback.test.cjs` exits 0.
  </acceptance_criteria>
  <action>
Run focused integration verification after implementation:
- `node --check get-stuff-done/bin/lib/context.cjs`
- `node --check get-stuff-done/bin/lib/second-brain.cjs`
- `node --test tests/context-memory-pack.test.cjs tests/firecrawl-memory-boundary.test.cjs tests/executor-memory-writeback.test.cjs`

Fix focused failures before considering Plan 54-02 complete.
  </action>
  <verify>
    <automated>node --check get-stuff-done/bin/lib/context.cjs && node --check get-stuff-done/bin/lib/second-brain.cjs && node --test tests/context-memory-pack.test.cjs tests/firecrawl-memory-boundary.test.cjs tests/executor-memory-writeback.test.cjs</automated>
  </verify>
  <done>Phase 54 workflow integration is proven by focused tests and keeps the Firecrawl boundary intact.</done>
</task>

</tasks>

<verification>
- `node --test tests/context-memory-pack.test.cjs tests/firecrawl-memory-boundary.test.cjs`
- `node --test tests/executor-memory-writeback.test.cjs`
- `node --check get-stuff-done/bin/lib/context.cjs`
- `node --check get-stuff-done/bin/lib/second-brain.cjs`
</verification>

<success_criteria>
Plan 54-02 is satisfied when planner context reads and executor lifecycle writeback both use the sanctioned Phase 54 contract and external-context retrieval remains Firecrawl-only.
</success_criteria>

<output>
After completion, create `.planning/phases/54-model-facing-second-brain-via-mcp/54-02-SUMMARY.md`
</output>

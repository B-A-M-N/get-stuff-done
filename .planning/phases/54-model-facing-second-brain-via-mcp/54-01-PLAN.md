---
phase: 54-model-facing-second-brain-via-mcp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-stuff-done/bin/lib/second-brain.cjs
  - get-stuff-done/bin/lib/brain-manager.cjs
  - get-stuff-done/bin/gsd-tools.cjs
  - .planning/phases/54-model-facing-second-brain-via-mcp/54-VALIDATION.md
  - .planning/phases/54-model-facing-second-brain-via-mcp/toolbox/tools.yaml
  - tests/second-brain-mcp-tools.test.cjs
  - tests/brain-mcp-degraded-mode.test.cjs
autonomous: true
requirements:
  - MEMORY-MCP-01
  - MEMORY-MCP-02
must_haves:
  truths:
    - "Planner and executor workflows access Second Brain through a sanctioned MCP/toolbox contract instead of prompt-level direct database coupling."
    - "Planner memory access is read-only, executor writeback is narrowly scoped to curated append-only memory operations."
    - "Model-facing memory is unavailable or blocked when Second Brain is degraded away from Postgres, rather than silently changing semantics."
    - "Planning and execution context assembly includes a bounded internal memory pack while Firecrawl remains the sole external-context boundary."
    - "The local GenAI toolbox target at `/home/bamn/FireCageMCPGateway/genai-toolbox` is reflected in checked-in configuration or integration docs."
  artifacts:
    - path: "get-stuff-done/bin/lib/second-brain.cjs"
      provides: "Curated workflow-memory storage helpers and Postgres-required guardrails for MCP-facing memory."
    - path: ".planning/phases/54-model-facing-second-brain-via-mcp/toolbox/tools.yaml"
      provides: "Concrete MCP Toolbox source and toolset contract for planner read-only and executor writeback access."
    - path: "get-stuff-done/bin/gsd-tools.cjs"
      provides: "Operator-facing inspection or helper routes for Phase 54 memory/toolbox integration."
    - path: "tests/second-brain-mcp-tools.test.cjs"
      provides: "Coverage for toolbox contract shape and planner/executor permission split."
    - path: "tests/context-memory-pack.test.cjs"
      provides: "Coverage for bounded memory-pack integration into workflow context."
    - path: "tests/brain-mcp-degraded-mode.test.cjs"
      provides: "Coverage for explicit degraded-mode blocking of model-facing memory."
  key_links:
    - from: ".planning/phases/54-model-facing-second-brain-via-mcp/toolbox/tools.yaml"
      to: "/home/bamn/FireCageMCPGateway/genai-toolbox"
      via: "local MCP Toolbox integration target"
      pattern: "planner_memory_readonly|executor_memory_rw|memory_search|memory_write_checkpoint"
    - from: "get-stuff-done/bin/lib/brain-manager.cjs"
      to: "get-stuff-done/bin/lib/second-brain.cjs"
      via: "backend-truth/degraded-mode enforcement"
      pattern: "getBackendState|requirePostgres"
---

<objective>
Make Second Brain directly usable by planner and executor workflows through a sanctioned MCP Toolbox contract, while preserving Firecrawl as the only external-context boundary and failing closed when Postgres-backed model memory is unavailable.
</objective>

<execution_context>
@/home/bamn/.codex/get-shit-done/workflows/execute-plan.md
@/home/bamn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-model-facing-second-brain-via-mcp/54-CONTEXT.md
@.planning/phases/54-model-facing-second-brain-via-mcp/54-RESEARCH.md
@.planning/phases/54-model-facing-second-brain-via-mcp/54-VALIDATION.md
@.planning/phases/53-second-brain-connection-fallback-hardening/53-CONTEXT.md
@.planning/phases/53-second-brain-connection-fallback-hardening/53-01-SUMMARY.md
@get-stuff-done/bin/lib/second-brain.cjs
@get-stuff-done/bin/lib/brain-manager.cjs
@get-stuff-done/bin/lib/firecrawl-client.cjs
@get-stuff-done/bin/gsd-tools.cjs

<interfaces>
Locked local MCP integration target from `54-CONTEXT.md`:

```text
/home/bamn/FireCageMCPGateway/genai-toolbox
```

Existing backend-truth contract from Phase 53:

```json
{
  "configured_backend": "postgres",
  "active_backend": "sqlite",
  "degraded": true,
  "degraded_reason": "postgres_auth_failed",
  "warning_emitted": true,
  "memory_critical_blocked": false
}
```

Required Phase 54 architecture split from `54-RESEARCH.md`:

```text
External context -> Firecrawl only
Internal execution memory -> MCP Toolbox custom tools over Postgres
```
 </interfaces>
</context>

<tasks>

<task type="auto" id="54-01-01" tdd="true">
  <name>Task 1: Add curated workflow-memory storage and checked-in MCP Toolbox contract</name>
  <files>get-stuff-done/bin/lib/second-brain.cjs, .planning/phases/54-model-facing-second-brain-via-mcp/toolbox/tools.yaml, tests/second-brain-mcp-tools.test.cjs</files>
  <read_first>
- get-stuff-done/bin/lib/second-brain.cjs
- .planning/phases/54-model-facing-second-brain-via-mcp/54-RESEARCH.md
- .planning/phases/54-model-facing-second-brain-via-mcp/54-CONTEXT.md
- .planning/phases/53-second-brain-connection-fallback-hardening/53-01-SUMMARY.md
- /home/bamn/FireCageMCPGateway/genai-toolbox
  </read_first>
  <acceptance_criteria>
- `get-stuff-done/bin/lib/second-brain.cjs` contains helpers named `upsertWorkflowMemory`, `listWorkflowMemory`, and `requirePostgres`.
- `get-stuff-done/bin/lib/second-brain.cjs` contains the literal table name `gsd_local_brain.workflow_memory` or SQLite equivalent `workflow_memory`.
- `.planning/phases/54-model-facing-second-brain-via-mcp/toolbox/tools.yaml` exists and contains the exact toolset names `planner_memory_readonly` and `executor_memory_rw`.
- `.planning/phases/54-model-facing-second-brain-via-mcp/toolbox/tools.yaml` contains the exact tool names `memory_search` and `memory_write_checkpoint`.
- `tests/second-brain-mcp-tools.test.cjs` asserts the toolbox config does not expose `execute_sql` to planner-facing tools.
  </acceptance_criteria>
  <behavior>
    - Test 1: Curated workflow-memory records can be listed by project, phase, plan, and memory kind without exposing raw audit rows.
    - Test 2: The checked-in toolbox config describes a Postgres source and separate planner/executor toolsets.
    - Test 3: Planner-facing tools are read-only while executor-facing tools include limited append-only writeback.
  </behavior>
  <action>
Create a curated workflow-memory layer in `get-stuff-done/bin/lib/second-brain.cjs` instead of sending planner/executor prompts directly to raw `artifacts`, `symbols`, `dependencies`, or `firecrawl_audit`. Add concrete helpers named `upsertWorkflowMemory(entry)`, `listWorkflowMemory(filters = {})`, and any supporting schema bootstrap needed for a `workflow_memory` table with these exact conceptual fields: `project_id`, `phase`, `plan`, `memory_kind`, `title`, `body_markdown`, `source_ref`, `created_by`, and `importance`. Keep writes append-oriented for checkpoint/summary/decision memory.

Create `.planning/phases/54-model-facing-second-brain-via-mcp/toolbox/tools.yaml` as the checked-in Phase 54 contract for the local MCP Toolbox target at `/home/bamn/FireCageMCPGateway/genai-toolbox`. Define a Postgres source plus custom named tools. Include exact toolset names `planner_memory_readonly` and `executor_memory_rw`. Include exact tool names `memory_search` and `memory_write_checkpoint`. Do not include generic `execute_sql`, migration, or destructive delete tools in the planner-facing surface.

Write `tests/second-brain-mcp-tools.test.cjs` first. Assert the toolbox config file exists, contains the required toolsets and tool names, and does not expose unrestricted SQL execution to the planner toolset. Assert the storage helper API in `second-brain.cjs` matches the plan contract.
  </action>
  <verify>
    <automated>node --test tests/second-brain-mcp-tools.test.cjs</automated>
  </verify>
  <done>Curated workflow-memory storage exists and the Phase 54 MCP Toolbox contract is explicit, checked in, and least-privilege.</done>
</task>

<task type="auto" id="54-01-02" tdd="true">
  <name>Task 2: Enforce degraded-mode and Postgres-required behavior for model-facing memory</name>
  <files>get-stuff-done/bin/lib/second-brain.cjs, get-stuff-done/bin/lib/brain-manager.cjs, get-stuff-done/bin/gsd-tools.cjs, tests/brain-mcp-degraded-mode.test.cjs</files>
  <read_first>
- get-stuff-done/bin/lib/second-brain.cjs
- get-stuff-done/bin/lib/brain-manager.cjs
- get-stuff-done/bin/gsd-tools.cjs
- .planning/phases/53-second-brain-connection-fallback-hardening/53-CONTEXT.md
- .planning/phases/54-model-facing-second-brain-via-mcp/54-RESEARCH.md
  </read_first>
  <acceptance_criteria>
- `get-stuff-done/bin/lib/second-brain.cjs` contains a model-memory-specific guard that calls `requirePostgres(` or an equivalently named Postgres-required helper before MCP-facing memory reads or writes.
- `tests/brain-mcp-degraded-mode.test.cjs` covers both read and write attempts while `active_backend` is not `postgres`.
- `get-stuff-done/bin/lib/brain-manager.cjs` or `get-stuff-done/bin/gsd-tools.cjs` exposes a status/help field that mentions model-facing memory is unavailable while degraded.
- No code path silently switches planner/executor model memory to a SQLite-only contract.
  </acceptance_criteria>
  <behavior>
    - Test 1: Planner memory retrieval returns an explicit blocked or unavailable result when Second Brain is degraded to SQLite.
    - Test 2: Executor writeback returns an explicit blocked or unavailable result when Second Brain is degraded to SQLite.
    - Test 3: Operator/status output makes the degraded state visible instead of hiding it behind silent fallback.
  </behavior>
  <action>
Extend the Phase 53 backend-truth contract into the model-facing memory layer. Add a concrete guard in `get-stuff-done/bin/lib/second-brain.cjs` for MCP-facing memory operations so planner and executor memory access requires `active_backend === 'postgres'`. Use the existing `requirePostgres(operationName)` posture rather than inventing a second degraded-state system. Do not silently allow model-facing memory to keep working on SQLite.

Update `get-stuff-done/bin/lib/brain-manager.cjs` and `get-stuff-done/bin/gsd-tools.cjs` only as much as needed to make the operator story explicit. For example, expose a status/help field that reports model-facing memory is blocked while the backend is degraded. Keep this aligned with Phase 53's truthful operator-surface pattern rather than creating a separate subsystem UI.

Write `tests/brain-mcp-degraded-mode.test.cjs` first. Simulate a degraded backend state and assert that both read-style and write-style model-memory calls fail clearly with structured blocked/unavailable behavior instead of querying or writing to SQLite.
  </action>
  <verify>
    <automated>node --test tests/brain-mcp-degraded-mode.test.cjs</automated>
  </verify>
  <done>Model-facing memory obeys Phase 53's truth contract and fails closed when canonical Postgres-backed memory is unavailable.</done>
</task>

<task type="auto" id="54-01-04">
  <name>Task 3: Verify syntax, config shape, and focused foundation behavior</name>
  <files>get-stuff-done/bin/lib/second-brain.cjs, get-stuff-done/bin/lib/brain-manager.cjs, get-stuff-done/bin/gsd-tools.cjs, .planning/phases/54-model-facing-second-brain-via-mcp/toolbox/tools.yaml, tests/second-brain-mcp-tools.test.cjs, tests/brain-mcp-degraded-mode.test.cjs</files>
  <read_first>
- .planning/phases/54-model-facing-second-brain-via-mcp/54-VALIDATION.md
- .planning/phases/54-model-facing-second-brain-via-mcp/toolbox/tools.yaml
- tests/second-brain-mcp-tools.test.cjs
- tests/brain-mcp-degraded-mode.test.cjs
  </read_first>
  <acceptance_criteria>
- `node --check get-stuff-done/bin/lib/second-brain.cjs` exits 0.
- `node --check get-stuff-done/bin/lib/brain-manager.cjs` exits 0.
- `node --check get-stuff-done/bin/gsd-tools.cjs` exits 0.
- `node --test tests/second-brain-mcp-tools.test.cjs tests/brain-mcp-degraded-mode.test.cjs` exits 0.
  </acceptance_criteria>
  <behavior>
    - Test 1: All syntax checks pass.
    - Test 2: The focused foundation suite passes without requiring unrelated full-project green status.
    - Test 3: The checked-in toolbox config remains consistent with the code/test contract.
  </behavior>
  <action>
Run focused verification after implementation:
- `node --check get-stuff-done/bin/lib/second-brain.cjs`
- `node --check get-stuff-done/bin/lib/brain-manager.cjs`
- `node --check get-stuff-done/bin/gsd-tools.cjs`
- `node --test tests/second-brain-mcp-tools.test.cjs tests/brain-mcp-degraded-mode.test.cjs`

If a small CLI smoke check is useful and non-flaky, run one operator/status command that surfaces the new memory/toolbox state without requiring live external services. Fix focused failures before considering the plan complete.
  </action>
  <verify>
    <automated>node --check get-stuff-done/bin/lib/second-brain.cjs && node --check get-stuff-done/bin/lib/brain-manager.cjs && node --check get-stuff-done/bin/gsd-tools.cjs && node --test tests/second-brain-mcp-tools.test.cjs tests/brain-mcp-degraded-mode.test.cjs</automated>
  </verify>
  <done>The Phase 54 foundation is syntactically clean, behaviorally pinned by focused tests, and consistent with the checked-in toolbox contract.</done>
</task>

</tasks>

<verification>
- `node --test tests/second-brain-mcp-tools.test.cjs`
- `node --test tests/brain-mcp-degraded-mode.test.cjs`
- `node --check get-stuff-done/bin/lib/second-brain.cjs`
- `node --check get-stuff-done/bin/lib/brain-manager.cjs`
- `node --check get-stuff-done/bin/gsd-tools.cjs`
</verification>

<success_criteria>
Plan 54-01 is satisfied when the sanctioned MCP Toolbox contract, curated workflow-memory storage, and degraded-mode enforcement exist as a safe foundation for the later workflow integration plan.
</success_criteria>

<output>
After completion, create `.planning/phases/54-model-facing-second-brain-via-mcp/54-01-SUMMARY.md`
</output>

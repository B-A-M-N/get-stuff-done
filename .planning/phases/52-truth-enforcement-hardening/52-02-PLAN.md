---
phase: 52
plan: 02
title: SafeLogger Secret Leakage Prevention
slug: safelogger-secret-prevention
wave: 2
depends_on:
  - 52-01
files_modified:
  - packages/gsd-tools/src/logging/SafeLogger.js
  - packages/gsd-tools/src/controlplane/**/*.js
  - packages/gsd-tools/src/executor.js
  - packages/gsd-tools/src/decision-composer.js
  - packages/gsd-tools/src/finalizer.js
  - packages/gsd-tools/src/verifier.js
  - packages/gsd-tools/src/contract/**/*.js
  - tests/phase-52/unit/safelogger-secrets.test.cjs
  - tests/phase-52/integration/safelogger-end-to-end.test.cjs
  - scripts/validate-secret-scan.js
requirements:
  - QUALITY-02
  - QUALITY-04
must_haves:
  truths:
    - "No claim exists without verifiable evidence"
    - "Secret leakage prevention is structural, not aspirational — if a secret can leak, it will"
    - "Sanitization must occur at write boundary, not scattered across codebase"
  artifacts:
    - "SafeLogger.js with sanitize() covering all secret patterns and <1ms latency"
    - "tests/phase-52/unit/safelogger-secrets.test.cjs with ≥10 test cases"
    - "tests/phase-52/integration/safelogger-end-to-end.test.cjs proving end-to-end redaction"
    - "scripts/validate-secret-scan.js returning exit 0 on clean codebase"
  key_links:
    - ".planning/phases/52-truth-enforcement-hardening/52-CONTEXT.md → QUALITY-02 secret definition and detection surface"
    - "docs/ARCHITECTURE-V0.4.0.md → logging infrastructure boundaries"
    - ".planning/phases/52-truth-enforcement-hardening/52-VALIDATION.md → test infrastructure"
status: planned
autonomous: true
---

<task id="52-02-01" title="Implement SafeLogger.sanitize() and Unit Tests">
<read_first>
- Read .planning/phases/52-truth-enforcement-hardening/52-CONTEXT.md section on Secret Leakage Prevention
- Check if packages/gsd-tools/src/logging/SafeLogger.js exists; if not, create
- Read existing logging usage in controlplane and executor to understand call patterns
- Read .planning/phases/52-truth-enforcement-hardening/52-VALIDATION.md for test conventions
</read_first>

<acceptance_criteria>
1. **SafeLogger.sanitize(input) exists:** Accepts string/buffer, returns sanitized string with secret patterns replaced by [REDACTED]
2. **Pattern coverage:** Minimum: API keys (sk-*, AKIA*, ghp_*), Bearer tokens, JWTs, Private keys, Database URLs, high-entropy strings (≥32 char alphanumeric)
3. **Performance:** sanitize() <1ms average on 1000 calls
4. **Unit tests pass:** tests/phase-52/unit/safelogger-secrets.test.cjs covers each pattern, multiple secrets, non-string inputs, normal passthrough (≥10 cases)
5. **Deterministic:** same input → same output
</acceptance_criteria>

<action>
Step 1: Create/update packages/gsd-tools/src/logging/SafeLogger.js with static sanitize(input)
Step 2: Define #secretPatterns array with regex for each pattern; add entropy fallback
Step 3: Write unit test file tests/phase-52/unit/safelogger-secrets.test.cjs with pattern tests + edge cases
Step 4: Ensure patterns are anchored appropriately
Step 5: Measure performance; optimize if >1ms (precompile regex, avoid loops)
</action>

<verify>
- SafeLogger.js exists with sanitize method and required patterns
- node --test tests/phase-52/unit/safelogger-secrets.test.cjs exits 0
- Benchmark 1000 calls average <1ms
- Patterns validated with examples
</verify>

<done>SafeLogger.sanitize() implemented and unit tested with performance constraints met</done>
</task>

<task id="52-02-02" title="Replace Console Calls and Intercept Direct Writes">
<read_first>
- Read packages/gsd-tools/src/logging/SafeLogger.js
- Grep for console. and logger. calls in source tree (excluding src/validation/)
- Grep for fs.appendFileSync/writeFileSync in source (excluding tests)
- Identify files needing modification
</read_first>

<acceptance_criteria>
1. **No raw console calls:** Every console.error/warn/log/info in application code (excluding tests) replaced with SafeLogger methods
2. **Logger wrappers:** Custom logger instances wrapped to sanitize before emitting
3. **Direct writes sanitized:** fs.writeFileSync/appendFileSync content passed through SafeLogger.sanitize()
4. **Process.stdout/err sanitized:** Direct writes with dynamic data use SafeLogger.sanitize()
5. **Integration test:** tests/phase-52/integration/safelogger-end-to-end.test.cjs verifies redaction end-to-end
6. **Kill test:** Disabling sanitize causes integration test to fail (proves effectiveness)
</acceptance_criteria>

<action>
Step 1: Write integration test: tests/phase-52/integration/safelogger-end-to-end.test.cjs captures output and asserts [REDACTED] present, raw secret absent
Step 2: Search src/ (excluding validation/) for console.* and logger.*; replace with SafeLogger.error/warn/log/info
Step 3: Wrap fs.writeFileSync/appendFileSync content with SafeLogger.sanitize()
Step 4: Wrap process.stdout.write/process.stderr.write as needed
Step 5: Run integration test; iterate until it passes
Step 6: Add kill test scenario (stub sanitize to return input) that makes integration test fail
</action>

<verify>
- grep -rn "console\\.[a-z]*" packages/gsd-tools/src/ | grep -v test returns empty (or only in test files)
- All application logging calls use SafeLogger
- Integration test passes; kill test variant fails as expected
- No raw secrets in logs when errors occur
</verify>

<done>All logging call sites sanitized; integration and kill tests verify redaction</done>
</task>

<task id="52-02-03" title="Create Validation Script and Perform Final Secret Scan">
<read_first>
- Read existing scripts directory (scripts/) for convention
- Read .planning/phases/52-truth-enforcement-hardening/52-VALIDATION.md for CI integration
</read_first>

<acceptance_criteria>
1. **Validation script exists:** scripts/validate-secret-scan.js uses same patterns as SafeLogger to scan source and logs
2. **Clean exit:** Running node scripts/validate-secret-scan.js on current codebase exits 0 (no secrets)
3. **CI gate:** npm script "validate:secrets" added; fails if scan finds anything
4. **Audit evidence:** .planning/phases/52-truth-enforcement-hardening/secret-scan-clean.txt saved with timestamp and "PASS"
</acceptance_criteria>

<action>
Step 1: Create scripts/validate-secret-scan.js; mirror SafeLogger patterns; recursively scan packages/gsd-tools/src/ (excluding tests) and logs/
Step 2: Run script; if finds matches, sanitize or document exceptions
Step 3: Add npm script "validate:secrets": "node scripts/validate-secret-scan.js"
Step 4: Save scan result to secret-scan-clean.txt
Step 5: Verify exit code 0
</action>

<verify>
- scripts/validate-secret-scan.js exists and is runnable
- node scripts/validate-secret-scan.js exits 0
- secret-scan-clean.txt exists with PASS message
- npm run validate:secrets exits 0
- No raw secrets detected in application code
</verify>

<done>Secret validation script operational; final scan clean; CI gate configured</done>
</task>

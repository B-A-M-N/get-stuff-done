---
phase: 52
plan: 01
title: Coverage Enforcement for Critical Modules
slug: coverage-enforcement
wave: 1
depends_on: []
files_modified:
  - .nycrc
  - package.json
  - tests/phase-52/unit/coverage-criticalpaths.test.cjs
  - tests/phase-52/unit/coverage-edgecases.test.cjs
  - coverage/phase-52-coverage.json
requirements:
  - QUALITY-01
  - QUALITY-04
must_haves:
  truths:
    - "No claim exists without verifiable evidence"
    - "Coverage is evidence, not a metric — must prove execution of critical paths"
    - "Deterministic test results are prerequisite for reproducibility"
  artifacts:
    - "coverage/phase-52-coverage.json with ≥85% line coverage for all critical modules"
    - "tests/phase-52/unit/coverage-criticalpaths.test.cjs covering error branches"
    - "tests/phase-52/unit/coverage-edgecases.test.cjs covering boundary conditions"
    - ".planning/phases/52-truth-enforcement-hardening/coverage-gaps.json gap analysis"
  key_links:
    - ".planning/phases/52-truth-enforcement-hardening/52-CONTEXT.md → QUALITY-01 critical modules definition"
    - "docs/ARCHITECTURE-V0.4.0.md → module dependency mapping"
    - ".planning/phases/52-truth-enforcement-hardening/52-VALIDATION.md → test infrastructure standards"
status: planned
autonomous: true
---

<task id="52-01-01" title="Configure Coverage Thresholds and Identify Gaps">
<read_first>
- Read docs/ARCHITECTURE-V0.4.0.md to identify critical modules (ControlPlane, Executor, DecisionComposer, Finalizer, Verifier)
- Read package.json to understand existing test scripts
- Read .nycrc (if exists) for current coverage configuration
- Read packages/gsd-tools/src/executor.js and other critical modules to understand branch points
- Read .planning/phases/52-truth-enforcement-hardening/52-CONTEXT.md for phase context
</read_first>

<acceptance_criteria>
1. **Coverage configured:** .nycrc sets `check-coverage: true`, `lines: 85`, `branches: 80`, `statements: 85` for critical modules only
2. **Gap analysis complete:** Running `npm test -- --coverage --reporter=text-summary` produces coverage-gaps.json with all files below threshold and uncovered branch line numbers
3. **Exclusions documented:** Any module excluded from threshold is listed in coverage-exceptions.yaml with explicit justification (external, generated, or trivial adapter)
</acceptance_criteria>

<action>
Step 1: Create/update .nycrc with strict thresholds for packages/gsd-tools/src/controlplane/, executor.js, decision-composer.js, finalizer.js, verifier.js
Step 2: Run `npm test -- --coverage --reporter=json-summary` to capture current baseline
Step 3: Parse coverage output, filter to critical modules, identify files with line <85% or branch <80%
Step 4: Write coverage-gaps.json as array of objects: {file: path, lines_pct: N, branches_pct: M, uncovered_branches: [line numbers]}
Step 5: Review gaps with architecture doc to confirm critical module list is complete
</action>

<verify>
- .nycrc exists with correct thresholds
- coverage-gaps.json exists and contains at least one entry for each undercovered critical module
- Running `npm test` exits with non-zero if any critical module fails threshold
</verify>

<done>Coverage thresholds configured; gap analysis JSON written and reviewed</done>
</task>

<task id="52-01-02" title="Write Tests for Critical Paths and Error Branches">
<read_first>
- Read coverage-gaps.json to see which files need tests
- Read target source files to identify uncovered branches
- Read existing unit tests in tests/unit/ to follow project test patterns
- Read .planning/phases/52-truth-enforcement-hardening/52-VALIDATION.md for test framework conventions
</read_first>

<acceptance_criteria>
1. **Branch coverage added:** For each gap, tests explicitly exercise all try/catch, conditional rejection, and error handling branches
2. **Edge cases covered:** Empty inputs, malformed data, oversized payloads, null values, boundary numbers (0, -1, MAX_INT) have dedicated test cases
3. **Test files placed:** New tests in tests/phase-52/unit/ mirroring source structure
4. **Property-based tests included:** At least 2 files use fast-check for edge generation
5. **All new tests pass:** node --test tests/phase-52/unit/*.test.cjs exits 0
</acceptance_criteria>

<action>
Step 1: For each file in coverage-gaps.json, read source to identify uncovered branches
Step 2: Write unit tests targeting those branches with assertions verifying error conditions
Step 3: Write separate test blocks for edge cases with descriptive names
Step 4: For two critical modules, add property-based tests using fast-check generating random inputs and asserting invariants (e.g., "executor never crashes", "validator deterministic")
Step 5: Run tests repeatedly to ensure determinism; fix any flaky tests
</action>

<verify>
- npm test -- --coverage shows increased line/branch coverage for previously gap files
- All new test files exist in tests/phase-52/unit/ with .test.cjs extension
- node --test tests/phase-52/unit/*.test.cjs runs in <5 seconds
- No test uses external network calls or flaky timing
</verify>

<done>All critical path gaps filled with deterministic unit and property-based tests</done>
</task>

<task id="52-01-03" title="Validate Coverage Threshold and Generate Audit Report">
<read_first>
- Read .nycrc to confirm thresholds
- Run full coverage collection
- Read .planning/phases/52-truth-enforcement-hardening/52-VALIDATION.md for expected report format
</read_first>

<acceptance_criteria>
1. **Coverage threshold met:** npm test -- --coverage --reporter=text-summary shows ≥85% lines for every critical module
2. **Report generated:** coverage/phase-52-coverage.json exists with structure {module_name: {lines: {pct: N}, branches: {pct: M}}}
3. **Gate check passes:** CI would accept this coverage (no threshold violations)
4. **Audit artifact produced:** JSON report is deterministic and can be diffed across runs
</acceptance_criteria>

<action>
Step 1: Run `npm test -- --coverage --reporter=json --reporter=json-summary`
Step 2: Extract from coverage/coverage-final.json only critical modules and their line/branch pct
Step 3: Write coverage/phase-52-coverage.json with simplified structure
Step 4: Validate all critical modules ≥85%. If any fail, exit non-zero and print failing modules
Step 5: Ensure package.json has script "test:coverage:phase52"
</action>

<verify>
- coverage/phase-52-coverage.json exists with all critical modules ≥85
- npm run test:coverage:phase52 exits 0
- Running coverage twice produces identical JSON
- File paths in report match source locations in packages/gsd-tools/src/
</verify>

<done>Coverage threshold met; audit-ready JSON report generated; CI gate script in place</done>
</task>

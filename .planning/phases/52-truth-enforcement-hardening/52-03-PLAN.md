---
phase: 52
plan: 03
title: Validation Proof Harness
slug: validation-proof-harness
wave: 2
depends_on:
  - 52-01
files_modified:
  - packages/gsd-tools/src/validation/ProofHarness.js
  - packages/gsd-tools/src/validation/**/*.js
  - tests/phase-52/unit/proof-harness.test.cjs
  - tests/phase-52/integration/adversarial-scenarios.test.cjs
  - .planning/phases/52-truth-enforcement-hardening/proofs/
  - .planning/phases/52-truth-enforcement-hardening/schemas/proof-schema.json
requirements:
  - QUALITY-03
  - QUALITY-04
must_haves:
  truths:
    - "No claim exists without verifiable evidence"
    - "Validity claims without adversarial evidence are faith, not fact — prove the negative"
    - "Proofs must be deterministic and tamper-evident"
  artifacts:
    - "ProofHarness.js with generateProof(validatorName, testResults) producing deterministic proofs"
    - "schemas/proof-schema.json validating required fields and constraints"
    - "proofs/*.proof.json files for all validators with false_negatives=0 and false_positives=0"
    - "tests/phase-52/integration/adversarial-scenarios.test.cjs covering tampering, bypass, and kill tests"
  key_links:
    - ".planning/phases/52-truth-enforcement-hardening/52-CONTEXT.md → QUALITY-03 proof definition and adversarial requirements"
    - "docs/ARCHITECTURE-V0.4.0.md → validator module boundaries"
    - ".planning/phases/52-truth-enforcement-hardening/52-VALIDATION.md → test infrastructure"
status: planned
autonomous: true
---

<task id="52-03-01" title="Create ProofHarness and Proof Schema">
<read_first>
- Read packages/gsd-tools/src/validation/ to see existing validators
- Read .planning/phases/52-truth-enforcement-hardening/52-RESEARCH.md section 3 (Proof Harness Architecture)
- Read .planning/phases/52-truth-enforcement-hardening/52-CONTEXT.md for QUALITY-03
- Check if contracts/ directory exists for validator contracts
</read_first>

<acceptance_criteria>
1. **ProofHarness class exists:** packages/gsd-tools/src/validation/ProofHarness.js exports { ProofHarness } with static generateProof(validatorName, testResults)
2. **Proof schema defined:** .planning/phases/52-truth-enforcement-hardening/schemas/proof-schema.json validates required fields (version, validator, timestamp, total_cases, passed_valid, rejected_invalid, false_negatives, false_positives, contract_hash, signature)
3. **Proof structure matches schema:** generateProof returns object that validates against schema (using ajv)
4. **Deterministic contract hash:** contract_hash = SHA256 of validator's contract file (contracts/{validator}.yaml) — same input → same hash
5. **Signature computed:** signature = SHA256(JSON.stringify({validatorName, testResults})) — tamper-evident
6. **Unit test proves:** ProofHarness generates valid proof with 0 false negatives/positives and matches schema
</acceptance_criteria>

<action>
Step 1: Write ProofHarness.js implementing generateProof, #computeContractHash, #signProof, #validateProof using ajv
Step 2: Create schemas/proof-schema.json with strict constraints (false_negatives/positives maximum 0)
Step 3: Write unit test tests/phase-52/unit/proof-harness.test.cjs verifying proof generation, schema validation, and deterministic outputs
Step 4: Ensure ajv is available (add to devDependencies if missing)
</action>

<verify>
- ProofHarness.js exists with generateProof method
- schemas/proof-schema.json exists and validates proofs
- node --test tests/phase-52/unit/proof-harness.test.cjs exits 0
- Running generateProof twice with same inputs yields identical proof object (except timestamp)
- contract_hash pattern matches ^[a-f0-9]{64}$
</verify>

<done>ProofHarness and schema implemented; unit test verifies deterministic proof generation</done>
</task>

<task id="52-03-02" title="Build Proof Generator and Adversarial Test Suite">
<read_first>
- Read ProofHarness.js and unit test
- Read existing validators in packages/gsd-tools/src/validation/ to enumerate validator names
- Read .planning/phases/52-truth-enforcement-hardening/52-CONTEXT.md for adversarial scenarios
</read_first>

<acceptance_criteria>
1. **Validator inventory complete:** All validators (json_validator, config_validator, phase_input_validator, contract_validator) identified
2. **Input sets created:** For each validator, valid/invalid input sets stored in .planning/phases/52-truth-enforcement-hardening/proofs/inputs/{name}.valid.json and {name}.invalid.json (≥40 each)
3. **Proof generation script:** scripts/generate-all-proofs.js runs each validator through its input sets, collects testResults, calls ProofHarness.generateProof, writes proofs/inputs to proofs/{validator}.proof.json
4. **Adversarial integration tests:** tests/phase-52/integration/adversarial-scenarios.test.cjs covers:
   - Tampered contract hash detection (proof invalidated if hash mismatch)
   - False negative detection (proof rejects if falseNegatives > 0)
   - Invalid signature detection (proof invalidated)
   - Validator removal (kill test) — missing validator causes proof generation to fail
5. **All proofs have 0 false negatives/positives:** Generating proofs from real validators on their test sets produces exactly 0 errors
6. **Deterministic proofs:** Running generator twice produces identical proof files (excluding timestamp)
</acceptance_criteria>

<action>
Step 1: Create scripts/generate-all-proofs.js that enumerates validators, loads input sets, runs validation loops, aggregates results, calls ProofHarness.generateProof, writes proof JSON to proofs/ directory
Step 2: Create input JSON files for each validator (start with at least 50 valid and 50 invalid synthetic inputs)
Step 3: Write adversarial tests in tests/phase-52/integration/adversarial-scenarios.test.cjs that simulate tampering, forgery, and removal
Step 4: Run generator; verify all proofs have false_negatives=0, false_positives=0; if not, adjust validators or inputs
Step 5: Ensure proofs are deterministic (compare two runs)
Step 6: Update package.json to add script: "test:proofs:phase52": "node scripts/generate-all-proofs.js"
</action>

<verify>
- scripts/generate-all-proofs.js exists and runs without error
- proofs/*.proof.json exist for all validators (at least 3)
- Each proof contains required fields and passes AJV schema validation
- All proofs have false_negatives: 0, false_positives: 0
- node --test tests/phase-52/integration/adversarial-scenarios.test.cjs exits 0
- Proof files identical across two generator runs (ignore timestamp)
- npm run test:proofs:phase52 -- --dry-run succeeds
- package.json contains "test:proofs:phase52"
</verify>

<done>Proof generator and adversarial test suite complete; all proofs validate with zero errors</done>
</task>

---
phase: 74-state-reconciliation-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-stuff-done/bin/lib/state.cjs
  - get-stuff-done/bin/lib/roadmap.cjs
  - get-stuff-done/bin/lib/drift-reconcile.cjs
  - get-stuff-done/bin/lib/drift-reconcile-adapter.cjs
  - get-stuff-done/bin/gsd-tools.cjs
  - tests/drift-reconcile.test.cjs
  - tests/state.test.cjs
autonomous: true
requirements:
  - TRUTH-DRIFT-02
  - TRUTH-CLAIM-01
must_haves:
  truths:
    - "Phase 74 consumes Phase 73 drift reports and applies deterministic status downgrade rules through sanctioned interfaces."
    - "Reconciliation marks affected truth surfaces as invalid, conditional, degraded, or re-verification-required without inventing new evidence."
    - "Historical-only drift remains visible but non-blocking for current status mutation."
    - "Applied reconciliation outcomes are recorded in a machine-readable artifact that attributes every consequence to the source drift report."
    - "A thin adapter layer normalizes Phase 73 report output before reconciliation rules run."
  artifacts:
    - path: "get-stuff-done/bin/lib/drift-reconcile.cjs"
      provides: "Deterministic reconciliation rules from drift report to affected status surfaces."
    - path: "get-stuff-done/bin/lib/drift-reconcile-adapter.cjs"
      provides: "Thin adapter that normalizes Phase 73 report data into reconciliation-ready inputs."
    - path: "get-stuff-done/bin/lib/state.cjs"
      provides: "Sanctioned state-surface mutation points for applied reconciliation outcomes."
    - path: "get-stuff-done/bin/lib/roadmap.cjs"
      provides: "Roadmap/phase-status mutation support for reconciliation results where in scope."
    - path: "get-stuff-done/bin/gsd-tools.cjs"
      provides: "CLI entrypoint for reconciliation operations and reporting."
    - path: ".planning/drift/latest-reconciliation.json"
      provides: "Machine-readable audit trail of applied consequences, attribution, and re-verification requirements."
    - path: "tests/drift-reconcile.test.cjs"
      provides: "Rule coverage for downgrade, conditionality, historical handling, and re-verification triggers."
  key_links:
    - from: "get-stuff-done/bin/lib/drift-reconcile.cjs"
      to: "get-stuff-done/bin/lib/state.cjs"
      via: "sanctioned state mutation from predicted drift effects"
      pattern: "INVALID|CONDITIONAL|reverification_required|degraded"
    - from: "get-stuff-done/bin/lib/drift-reconcile.cjs"
      to: "get-stuff-done/bin/lib/roadmap.cjs"
      via: "phase/roadmap status downgrade"
      pattern: "phase_status|roadmap_status|BLOCKED|AT_RISK|INFO"
    - from: "get-stuff-done/bin/lib/drift-reconcile-adapter.cjs"
      to: "get-stuff-done/bin/lib/drift-reconcile.cjs"
      via: "normalized Phase 73 report input"
      pattern: "normalized_drift|source_report|affected|predicted_effect"
    - from: "get-stuff-done/bin/lib/drift-reconcile.cjs"
      to: "tests/drift-reconcile.test.cjs"
      via: "reconciliation rule contract"
      pattern: "historical|CRITICAL|MAJOR|MINOR|reverification|BLOCKED|AT_RISK|HEALTHY_WITH_WARNINGS"
---

<objective>
Implement the Phase 74 reconciliation layer so active drift findings from Phase 73 produce deterministic status downgrade, conditionality, degraded operator truth, and re-verification requirements through sanctioned interfaces.
</objective>

<execution_context>
@/home/bamn/.codex/get-shit-done/workflows/execute-plan.md
@/home/bamn/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v0.7.0-DECISIONS.md
@.planning/phases/74-state-reconciliation-layer/74-CONTEXT.md
@.planning/phases/73-drift-detection-engine/73-CONTEXT.md
@get-stuff-done/bin/lib/state.cjs
@get-stuff-done/bin/lib/roadmap.cjs
@get-stuff-done/bin/lib/verify.cjs
</context>

## Locked Decisions

Decision: Phase 74 consumes the Phase 73 drift report and applies consequences rather than recomputing drift.
Decision: Reconciliation may mutate sanctioned truth surfaces, but it must not invent evidence or rewrite history.
Decision: Historical-only drift is visible but non-blocking for current-state downgrade.
Decision: Re-verification requirements must be machine-readable and attributable to the drift report that triggered them.
Decision: Reconciliation must emit a central machine artifact that records source-report identity, applied changes, unchanged surfaces, and attribution metadata.
Decision: Downgrade results are fixed by severity: `CRITICAL -> INVALID/INVALID/BLOCKED/UNHEALTHY`, `MAJOR -> CONDITIONAL/CONDITIONAL/AT_RISK/DEGRADED`, `MINOR -> VALID/VALID/INFO/HEALTHY_WITH_WARNINGS`.
Decision: `drift reconcile` is the only mutation entrypoint; `drift preview` is the dry-run view.
Decision: Reconciliation must normalize Phase 73 inputs through a thin adapter and must not depend directly on raw Phase 73 report shape.

## Risks

Risk: reconciliation can mutate too broadly and create a second source of truth.
Guardrail: mutations must flow through one deterministic reconciliation layer and sanctioned state/roadmap interfaces.
Fallback: if a downgrade target is ambiguous, record a re-verification-required or conditional outcome rather than silently mutating an uncertain surface.

## Assumptions

Assumption: Phase 74 consumes the Phase 73 report fields `affected` and `predicted_effect` as reconciliation inputs rather than recomputing detection logic.
Deferred: if the Phase 73 report shape changes during implementation, update the reconciliation adapter layer rather than broadening Phase 74 back into a second detection engine.
Assumption: the canonical reconciliation artifact path is `.planning/drift/latest-reconciliation.json`, and targeted state mutations remain separate from the audit artifact.

<tasks>

<task type="auto" id="74-01-01" tdd="true">
  <name>Task 1: Implement deterministic reconciliation rules and adapter normalization from Phase 73 report to affected truth surfaces</name>
  <files>tests/drift-reconcile.test.cjs, get-stuff-done/bin/lib/drift-reconcile-adapter.cjs, get-stuff-done/bin/lib/drift-reconcile.cjs</files>
  <read_first>
- .planning/phases/74-state-reconciliation-layer/74-CONTEXT.md
- .planning/phases/73-drift-detection-engine/73-CONTEXT.md
- .planning/v0.7.0-DECISIONS.md
  </read_first>
  <acceptance_criteria>
- `CRITICAL`, `MAJOR`, and `MINOR` drift map deterministically to reconciliation outcomes.
- The severity matrix is fixed to `INVALID/INVALID/BLOCKED/UNHEALTHY`, `CONDITIONAL/CONDITIONAL/AT_RISK/DEGRADED`, and `VALID/VALID/INFO/HEALTHY_WITH_WARNINGS`.
- Historical-only findings do not downgrade current truth surfaces.
- Reconciliation output includes explicit re-verification requirements where applicable.
- A thin adapter normalizes Phase 73 report inputs before rule evaluation.
- Reconciliation remains pure with respect to rule evaluation before mutation is applied.
  </acceptance_criteria>
  <behavior>
    - Test 1: `CRITICAL` active drift invalidates affected truth surfaces.
    - Test 2: `MAJOR` drift downgrades to conditional/degraded and marks re-verification required.
    - Test 3: historical-only drift produces no current downgrade.
  </behavior>
  <action>
Write focused rule tests first in `tests/drift-reconcile.test.cjs`. Then implement `get-stuff-done/bin/lib/drift-reconcile-adapter.cjs` plus `get-stuff-done/bin/lib/drift-reconcile.cjs` so Phase 74 converts Phase 73 report findings into reconciliation decisions without directly mutating files.
  </action>
  <verify>
    <automated>node --test tests/drift-reconcile.test.cjs</automated>
  </verify>
  <done>Phase 74 has a deterministic, test-pinned reconciliation rule engine.</done>
</task>

<task type="auto" id="74-01-02" tdd="true">
  <name>Task 2: Apply reconciliation outcomes through sanctioned state and roadmap mutation surfaces</name>
  <files>tests/state.test.cjs, get-stuff-done/bin/lib/state.cjs, get-stuff-done/bin/lib/roadmap.cjs, get-stuff-done/bin/lib/drift-reconcile.cjs</files>
  <read_first>
- get-stuff-done/bin/lib/state.cjs
- get-stuff-done/bin/lib/roadmap.cjs
- get-stuff-done/bin/lib/drift-reconcile.cjs
  </read_first>
  <acceptance_criteria>
- Reconciliation applies status changes only through sanctioned state and roadmap interfaces.
- Applied outcomes remain attributable to the input drift report.
- Re-verification requirements are persisted in machine-readable form.
- A central reconciliation artifact records source report identity or hash, applied timestamp, mutated surfaces, and unchanged surfaces.
- Mutation scope stays inside `STATE.md`, phase metadata markers, operator health markers, and re-verification markers.
- No ad hoc or silent direct writes occur outside the sanctioned mutation path.
  </acceptance_criteria>
  <behavior>
    - Test 1: applying reconciliation mutates state/roadmap surfaces through sanctioned helpers.
    - Test 2: applied output records source report identity and affected surfaces.
    - Test 3: ambiguous or unsupported targets do not silently mutate unrelated state.
  </behavior>
  <action>
Add focused mutation tests first. Then wire the reconciliation engine into `state.cjs` and `roadmap.cjs` via a sanctioned path that applies downgrade and re-verification markers while preserving attribution to the source drift report, without mutating roadmap structure, requirements, or historical artifacts.
  </action>
  <verify>
    <automated>node --test tests/state.test.cjs tests/drift-reconcile.test.cjs</automated>
  </verify>
  <done>Reconciliation outcomes now reach state-bearing artifacts through sanctioned deterministic mutation surfaces.</done>
</task>

<task type="auto" id="74-01-03">
  <name>Task 3: Expose preview and reconcile through the CLI and verify end-to-end downgrade behavior</name>
  <files>get-stuff-done/bin/gsd-tools.cjs, get-stuff-done/bin/lib/drift-reconcile-adapter.cjs, get-stuff-done/bin/lib/drift-reconcile.cjs, get-stuff-done/bin/lib/state.cjs, get-stuff-done/bin/lib/roadmap.cjs, tests/drift-reconcile.test.cjs, tests/state.test.cjs</files>
  <read_first>
- tests/drift-reconcile.test.cjs
- tests/state.test.cjs
- get-stuff-done/bin/gsd-tools.cjs
- get-stuff-done/bin/lib/drift-reconcile.cjs
  </read_first>
  <acceptance_criteria>
- `node --check get-stuff-done/bin/lib/drift-reconcile.cjs` exits `0`.
- `node --check get-stuff-done/bin/lib/drift-reconcile-adapter.cjs` exits `0`.
- `node --check get-stuff-done/bin/gsd-tools.cjs` exits `0`.
- `node --test tests/drift-reconcile.test.cjs tests/state.test.cjs` exits `0`.
- CLI `drift preview` shows the deterministic dry-run outcome and `drift reconcile` is the only mutation entrypoint.
- CLI reconciliation applies deterministic downgrade and re-verification requirements from a Phase 73 report.
- The applied reconciliation artifact is machine-readable and attributable to the exact source report consumed.
  </acceptance_criteria>
  <action>
Expose sanctioned `drift preview` and `drift reconcile` entrypoints in `gsd-tools.cjs`, then run focused verification on rule evaluation and mutation behavior. If helpful, use a temp drift report to confirm the full flow from machine report to applied status changes is deterministic and attributable.
  </action>
  <verify>
    <automated>node --check get-stuff-done/bin/lib/drift-reconcile.cjs && node --check get-stuff-done/bin/gsd-tools.cjs && node --test tests/drift-reconcile.test.cjs tests/state.test.cjs</automated>
  </verify>
  <done>Phase 74 can apply drift consequences deterministically through sanctioned interfaces without inventing new evidence.</done>
</task>

</tasks>

<verification>
- `node --test tests/drift-reconcile.test.cjs`
- `node --test tests/state.test.cjs`
- `node --check get-stuff-done/bin/lib/drift-reconcile-adapter.cjs`
- `node --check get-stuff-done/bin/lib/drift-reconcile.cjs`
- `node --check get-stuff-done/bin/gsd-tools.cjs`
</verification>

<success_criteria>
Plan 74-01 is satisfied when active drift can be reconciled into deterministic status downgrade and re-verification requirements through sanctioned mutation surfaces, while historical-only drift remains non-blocking.
</success_criteria>

<output>
After completion, create `.planning/phases/74-state-reconciliation-layer/74-01-SUMMARY.md`
</output>

<!-- GSD-AUTHORITY: 74-01-1:27cacd65bb71ca9f162133359ad0f54cb03bbfca3fdfbcf6461b21f9f8ca77f0 -->

---
phase: 47
plan: 02
type: execute
wave: 2
depends_on:
  - "47-01"
files_modified:
  - tests/plane-client.test.cjs
  - tests/roadmap-plane-sync.test.cjs
  - get-stuff-done/bin/lib/roadmap.cjs
  - get-stuff-done/bin/gsd-tools.cjs
autonomous: true
requirements:
  - PLANE-VISIBILITY-02
  - PLANE-SYNC-02
must_haves:
  truths:
    - "Unit tests verify plane-client methods call _request with correct endpoints and payloads"
    - "Unit tests verify syncFullRoadmap implements idempotent upsert via custom field lookup"
    - "Unit tests verify drift detection logs differences without overwriting unless force=true"
    - "Unit tests verify data enrichment fields (gsd_last_synced_at, labels) are included"
    - "Unit tests verify error continuation (batch continues when individual operation fails)"
    - "Unit tests verify dry-run mode counts without making API calls"
    - "Unit tests verify notifyRoadmapChange fire-and-forget pattern swallows errors"
    - "gsd-tools exposes `roadmap sync` command that executes syncFullRoadmap"
    - "cmdRoadmapUpdatePlanProgress triggers opportunistic sync via notifyRoadmapChange"
  artifacts:
    - path: tests/plane-client.test.cjs
      provides: Tests for new plane-client methods with correct mocking
      min_lines: 20
    - path: tests/roadmap-plane-sync.test.cjs
      provides: Comprehensive unit tests for sync module including upsert, drift, force, enrichment
      min_lines: 120
    - path: get-stuff-done/bin/lib/roadmap.cjs
      provides: cmdRoadmapSync command and write hook integration (notifyRoadmapChange)
      min_lines: 20
    - path: get-stuff-done/bin/gsd-tools.cjs
      provides: Command routing for 'roadmap sync'
      min_lines: 5
  key_links:
    - from: get-stuff-done/bin/gsd-tools.cjs
      to: get-stuff-done/bin/lib/roadmap.cjs
      via: calls roadmap.cmdRoadmapSync
      pattern: roadmap\.cmdRoadmapSync
    - from: get-stuff-done/bin/lib/roadmap.cjs
      to: get-stuff-done/bin/lib/roadmap-plane-sync.cjs
      via: imports syncFullRoadmap/notifyRoadmapChange
      pattern: require.*roadmap-plane-sync.*\n.*(syncFullRoadmap|notifyRoadmapChange)
    - from: get-stuff-done/bin/lib/roadmap.cjs::cmdRoadmapUpdatePlanProgress
      to: get-stuff-done/bin/lib/roadmap-plane-sync.cjs::notifyRoadmapChange
      via: fire-and-forget call after safeWriteFile
      pattern: notifyRoadmapChange\\(cwd, roadmapPath\\)\\.catch
---

<objective>
Add unit tests for the Plane client methods and the enhanced roadmap-plane-sync module (with upsert, drift detection, force, enrichment), and integrate sync into GSD command surface with opportunistic write hooks.

**Purpose:** Verify correctness of the implementation and make the feature available to operators via `gsd-tools roadmap sync` and automatic ROADMAP write triggers.

**Output:** Extended test files with comprehensive coverage; command registration; ROADMAP write hook.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-plane-roadmap-sync/47-CONTEXT.md
@.planning/phases/47-plane-roadmap-sync/47-RESEARCH.md
@.planning/phases/47-plane-roadmap-sync/47-VALIDATION.md

# Reference implementations and patterns:
@get-stuff-done/tests/plane-client.test.cjs (testing style, mocking _makeRequest)
@get-stuff-done/tests/state-plane-sync.test.cjs (fire-and-forget testing)
@get-stuff-done/bin/lib/state-plane-sync.cjs (hook pattern)
@get-stuff-done/bin/gsd-tools.cjs (command registration pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for plane-client and roadmap-plane-sync with full coverage</name>
  <files>
    tests/plane-client.test.cjs
    tests/roadmap-plane-sync.test.cjs
  </files>
  <read_first>
    - tests/plane-client.test.cjs (existing tests to extend)
    - tests/state-plane-sync.test.cjs (similar sync module tests pattern)
    - get-stuff-done/bin/lib/plane-client.cjs
    - get-stuff-done/bin/lib/roadmap-plane-sync.cjs (from Plan 01 output)
  </read_first>
  <action>
    Part A: Extend tests/plane-client.test.cjs with tests for new methods.

    For each method (createMilestone, createIssue, updateIssue, linkIssueParent):
    - Mock planeClient._makeRequest (or spy on it)
    - Call the method with sample payload
    - Assert that _makeRequest was called with correct endpoint, method 'POST', and body
    - Verify returned value matches _makeRequest's result

    Use node:test and assert.

    Part B: Create new file tests/roadmap-plane-sync.test.cjs with comprehensive tests covering:

    - Smoke test: module exports syncFullRoadmap and notifyRoadmapChange
    - Status mapping: test mapGSDStatusToPlane for all statuses (Planned→Todo, Research→Backlog, Discussed→Todo, In Progress→In Progress, Blocked→Blocked, Complete/Verified→Done)
    - Idempotent upsert: mock parseRoadmap to return one milestone; mock planeClient methods; call syncFullRoadmap twice; second call should either update (if force) or skip; verify counts (created on first, updated/unchanged on second)
    - Phase + plan hierarchy: mock parseRoadmap for one phase with one plan file; mock findPhaseInternal; mock planeClient; verify createIssue called for phase and then for child plan with parent_id set
    - Drift detection: set up existing milestone with different name; run sync without force; verify drift logged, unchanged count incremented, update not called; then run with force=true; verify update called
    - Data enrichment: verify all payloads include gsd_last_synced_at (recent timestamp), labels include "Synced from GSD", and `discussed` label appears when phase status is Discussed-derived
    - Error continuation: mock createMilestone to throw for second milestone; verify still processes rest and logs warning; no uncaught exception
    - Dry-run mode: set dryRun:true; mock planeClient; assert no planeClient methods called but counts incremented appropriately
    - Force mode: mock existing item with drift; call with force=true; assert update method called
    - notifyRoadmapChange fire-and-forget: mock syncFullRoadmap to throw; call notifyRoadmapChange; verify no exception propagates and logWarn called

    Use environment isolation (require cache clearing) and clean up mocks.

    Run final test suite: `node --test tests/roadmap-plane-sync.test.cjs tests/plane-client.test.cjs` should exit 0.
  </action>
  <verify>
    <automated>node --test tests/roadmap-plane-sync.test.cjs tests/plane-client.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/roadmap-plane-sync.test.cjs exists and contains at least 10 test blocks covering the behaviors listed
    - tests/plane-client.test.cjs contains at least 4 new test blocks for the new methods
    - node --test exits with code 0
  </acceptance_criteria>
  <done>Unit tests verify plane-client methods and roadmap-plane-sync behavior including idempotent upsert, drift detection, force, enrichment, error continuation, dry-run, and fire-and-forget.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate command and ROADMAP write hook</name>
  <files>
    get-stuff-done/bin/lib/roadmap.cjs
    get-stuff-done/bin/gsd-tools.cjs
  </files>
  <read_first>
    - get-stuff-done/bin/lib/roadmap.cjs (existing command functions)
    - get-stuff-done/bin/gsd-tools.cjs (command routing)
    - get-stuff-done/bin/lib/roadmap-plane-sync.cjs
  </read_first>
  <action>
    Part A: Modify get-stuff-done/bin/lib/roadmap.cjs
      - At top, after other requires, add:
        `const roadmapPlaneSync = require('./roadmap-plane-sync.cjs');`
      - Add new command function:
        ```javascript
        function cmdRoadmapSync(cwd, options, raw) {
          const result = roadmapPlaneSync.syncFullRoadmap(cwd, {
            dryRun: options && options['dry-run'],
            force: options && options.force
          });
          output(result, raw);
        }
        ```
      - Modify cmdRoadmapUpdatePlanProgress: after the `safeWriteFile(roadmapPath, roadmapContent, { ... });` line, add fire-and-forget hook:
        ```javascript
        if (process.env.PLANE_SYNC_ENABLED !== 'false') {
          roadmapPlaneSync.notifyRoadmapChange(cwd, roadmapPath).catch(() => {});
        }
        ```
      - Ensure exports include cmdRoadmapSync: add to module.exports:
        `module.exports = { cmdRoadmapGetPhase, cmdRoadmapAnalyze, cmdRoadmapUpdatePlanProgress, cmdRoadmapSync };`

    Part B: Modify get-stuff-done/bin/gsd-tools.cjs
      - Locate the 'roadmap' switch case (around line 1051-1060).
      - Add a new case for 'sync':
        ```javascript
        case 'sync':
          roadmap.cmdRoadmapSync(cwd, args[3], raw);
          break;
        ```
      - Update help text to include 'sync' as available subcommand (optional).

    Part C: Verify command `./get-stuff-done/bin/gsd-tools.cjs roadmap sync` runs and outputs valid JSON.
  </action>
  <verify>
    <automated>
      # Check command registration
      grep -q "cmdRoadmapSync" get-stuff-done/bin/lib/roadmap.cjs &&
      grep -q "case 'sync':" get-stuff-done/bin/gsd-tools.cjs &&
      # Run command dry-run (should succeed even without Plane configured)
      node ./get-stuff-done/bin/gsd-tools.cjs roadmap sync
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -q "function cmdRoadmapSync" get-stuff-done/bin/lib/roadmap.cjs
    - grep -q "notifyRoadmapChange" get-stuff-done/bin/lib/roadmap.cjs
    - grep -q "case 'sync':" get-stuff-done/bin/gsd-tools.cjs
    - Running `node ./get-stuff-done/bin/gsd-tools.cjs roadmap sync` exits 0 and prints valid JSON containing "synced" or "reason"
  </acceptance_criteria>
  <done>Command `gsd-tools roadmap sync` available and ROADMAP updates trigger opportunistic Plane sync.</done>
</task>

</tasks>

<verification>
Phase 47 Plan 02 completion checks:
- All automated tests pass (plane-client + roadmap-plane-sync with full coverage)
- Command registration present in both roadmap.cjs and gsd-tools.cjs
- Hook call present in cmdRoadmapUpdatePlanProgress
- `gsd-tools roadmap sync` executes without error
</verification>

<success_criteria>
Both tasks completed; tests green (≥130 total test lines); command working; sync integrated with write hook; all behaviors verified including upsert, drift detection, force, enrichment, error continuation.
</success_criteria>

<output>
After completion, create `.planning/phases/47-plane-roadmap-sync/47-02-SUMMARY.md`
</output>

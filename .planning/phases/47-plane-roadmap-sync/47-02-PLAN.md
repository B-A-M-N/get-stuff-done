---
phase: 47
plan: 02
type: execute
wave: 2
depends_on:
  - "47-01"
files_modified:
  - tests/plane-client.test.cjs
  - tests/roadmap-plane-sync.test.cjs
  - get-stuff-done/bin/lib/roadmap.cjs
  - get-stuff-done/bin/gsd-tools.cjs
autonomous: true
requirements:
  - PLANE-VISIBILITY-02
  - PLANE-SYNC-02
must_haves:
  truths:
    - "tests/plane-client.test.cjs includes tests for createMilestone, createIssue, updateIssue, linkIssueParent verifying _request calls and payloads"
    - "tests/roadmap-plane-sync.test.cjs exists and covers milestone upsert, phase+plan hierarchy creation, status mapping, drift detection, dry-run, force, error continuation, and notifyRoadmapChange fire-and-forget"
    - "All tests pass with node --test"
    - "gsd-tools.cjs routes 'roadmap sync' subcommand to roadmap.cjs cmdRoadmapSync"
    - "roadmap.cjs contains cmdRoadmapSync function that calls syncFullRoadmap(cwd, { dryRun, force }) and outputs result"
    - "cmdRoadmapUpdatePlanProgress includes fire-and-forget call to notifyRoadmapChange(cwd, roadmapPath) after safeWriteFile (when PLANE_SYNC_ENABLED !== 'false')"
    - "Manual command `gsd-tools roadmap sync` produces JSON output and exits 0"
  artifacts:
    - path: tests/roadmap-plane-sync.test.cjs
      provides: Comprehensive unit tests for sync module
      min_lines: 100
    - path: tests/plane-client.test.cjs
      provides: Tests for new plane-client methods (additions)
      min_lines: 20
    - path: get-stuff-done/bin/lib/roadmap.cjs
      provides: cmdRoadmapSync command and write hook integration
      min_lines: 20
    - path: get-stuff-done/bin/gsd-tools.cjs
      provides: Command routing for 'roadmap sync'
      min_lines: 5
  key_links:
    - from: get-stuff-done/bin/gsd-tools.cjs
      to: get-stuff-done/bin/lib/roadmap.cjs
      via: calls roadmap.cmdRoadmapSync
      pattern: roadmap\.cmdRoadmapSync
    - from: get-stuff-done/bin/lib/roadmap.cjs
      to: get-stuff-done/bin/lib/roadmap-plane-sync.cjs
      via: imports syncFullRoadmap/notifyRoadmapChange
      pattern: require.*roadmap-plane-sync.*\n.*(syncFullRoadmap|notifyRoadmapChange)
    - from: get-stuff-done/bin/lib/roadmap.cjs::cmdRoadmapUpdatePlanProgress
      to: get-stuff-done/bin/lib/roadmap-plane-sync.cjs::notifyRoadmapChange
      via: fire-and-forget call after safeWriteFile
      pattern: notifyRoadmapChange\\(cwd, roadmapPath\\)\\.catch
---

<objective>
Add unit tests for the new Plane API methods and sync module, and integrate sync into GSD command surface with opportunistic write hooks.

**Purpose:** Verify correctness of the implementation and make the feature available to operators via `gsd-tools roadmap sync` and automatic ROADMAP write triggers.

**Output:** Updated test files, command registration, and ROADMAP write hook.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-plane-roadmap-sync/47-CONTEXT.md
@.planning/phases/47-plane-roadmap-sync/47-RESEARCH.md
@.planning/phases/47-plane-roadmap-sync/47-VALIDATION.md

# Reference implementations and patterns:
@get-stuff-done/tests/plane-client.test.cjs (testing style, mocking _makeRequest)
@get-stuff-done/tests/state-plane-sync.test.cjs (fire-and-forget testing)
@get-stuff-done/bin/lib/state-plane-sync.cjs (hook pattern)
@get-stuff-done/bin/gsd-tools.cjs (command registration pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for plane-client and roadmap-plane-sync</name>
  <files>
    tests/plane-client.test.cjs
    tests/roadmap-plane-sync.test.cjs
  </files>
  <read_first>
    - tests/plane-client.test.cjs (existing tests)
    - tests/state-plane-sync.test.cjs (similar sync module tests)
    - get-stuff-done/bin/lib/plane-client.cjs
    - get-stuff-done/bin/lib/roadmap-plane-sync.cjs
  </read_first>
  <action>
    Part A: Extend tests/plane-client.test.cjs with tests for new methods:

    For each method (createMilestone, createIssue, updateIssue, linkIssueParent):
    - Mock planeClient._makeRequest (or spy on it)
    - Call the method with sample payload
    - Assert that _makeRequest was called with correct endpoint, method 'POST', and body
    - Verify returned value matches _makeRequest's result

    Example:
    ```javascript
    test('createMilestone calls _request with correct params', async () => {
      const client = require('../get-stuff-done/bin/lib/plane-client.cjs');
      const mockRequest = async (action, endpoint, body) => ({ statusCode: 200, data: { id: 'm1' } });
      client._request = mockRequest;
      const result = await client.createMilestone({ name: 'v0.4.0', gsd_milestone_version: 'v0.4.0' });
      assert.ok(result);
    });
    ```

    Part B: Create new file tests/roadmap-plane-sync.test.cjs with comprehensive tests:

    - smoke: module exports functions (syncFullRoadmap, notifyRoadmapChange)
    - test('syncFullRoadmap creates milestones using parseRoadmap': mock parseRoadmap to return one milestone; mock planeClient methods; call syncFullRoadmap; assert createMilestone called with correct name and gsd_milestone_version)
    - test('syncFullRoadmap creates phase and plan issues with parent linking': mock parseRoadmap for one phase, mock findPhaseInternal to return phaseInfo with one plan file; mock planeClient methods; assert createIssue called for phase and then for child plan with parent_id set)
    - test('status mapping maps GSD disk status to Plane states': directly test mapGSDStatusToPlane with various inputs to ensure correct mapping)
    - test('syncFullRoadmap handles errors gracefully (continues on failure)': mock createMilestone to throw for second milestone; verify still processes rest and logs warning)
    - test('syncFullRoadmap dry-run mode counts without calling planeClient': set dryRun:true; mock planeClient; assert no planeClient methods called but counts incremented)
    - test('syncFullRoadmap force mode updates protected fields'): optional if force logic implemented.
    - test('notifyRoadmapChange fire-and-forget swallows errors': mock syncFullRoadmap to throw; call notifyRoadmapChange; verify no exception propagates and logWarn called)

    Use node:test, assert, and mocking via spies or simple function replacement.
    Ensure tests clean up after themselves (restore mocks).

    Run the full test suite after adding tests: `node --test tests/roadmap-plane-sync.test.cjs tests/plane-client.test.cjs` should exit 0.
  </action>
  <verify>
    <automated>node --test tests/roadmap-plane-sync.test.cjs tests/plane-client.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/roadmap-plane-sync.test.cjs exists and contains at least 6 test blocks covering the behaviors listed
    - tests/plane-client.test.cjs contains at least 4 new test blocks for the new methods
    - node --test exits with code 0
  </acceptance_criteria>
  <done>Unit tests verify core Plane API methods and roadmap-plane-sync behavior; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate command and ROADMAP write hook</name>
  <files>
    get-stuff-done/bin/lib/roadmap.cjs
    get-stuff-done/bin/gsd-tools.cjs
  </files>
  <read_first>
    - get-stuff-done/bin/lib/roadmap.cjs (existing command functions)
    - get-stuff-done/bin/gsd-tools.cjs (command routing)
    - get-stuff-done/bin/lib/roadmap-plane-sync.cjs (exports)
  </read_first>
  <action>
    Part A: Modify get-stuff-done/bin/lib/roadmap.cjs
      - At top, after other requires, add:
        `const roadmapPlaneSync = require('./roadmap-plane-sync.cjs');`
      - Add new command function:
        ```javascript
        function cmdRoadmapSync(cwd, options, raw) {
          const result = roadmapPlaneSync.syncFullRoadmap(cwd, {
            dryRun: options && options['dry-run'],
            force: options && options.force
          });
          output(result, raw);
        }
        ```
        (If output expects raw boolean and rawValue, call `output(result, raw, JSON.stringify(result, null, 2))` or simply `output(result, raw)` as other commands do.)
      - Modify cmdRoadmapUpdatePlanProgress: after the `safeWriteFile(roadmapPath, roadmapContent, { ... });` line (around line 293), add fire-and-forget hook:
        ```javascript
        if (process.env.PLANE_SYNC_ENABLED !== 'false') {
          roadmapPlaneSync.notifyRoadmapChange(cwd, roadmapPath).catch(() => {});
        }
        ```
      - Ensure exports include cmdRoadmapSync: add to module.exports:
        `module.exports = { cmdRoadmapGetPhase, cmdRoadmapAnalyze, cmdRoadmapUpdatePlanProgress, cmdRoadmapSync };`

    Part B: Modify get-stuff-done/bin/gsd-tools.cjs
      - Locate the 'roadmap' switch case (around line 1051-1060).
      - Add a new case for 'sync':
        ```javascript
        case 'sync':
          roadmap.cmdRoadmapSync(cwd, args[3], raw);
          break;
        ```
      - Update the error message to list 'sync' as an available subcommand (optional but good).

    Part C: Verify command `./get-stuff-done/bin/gsd-tools.cjs roadmap sync` runs and outputs JSON.
  </action>
  <verify>
    <automated>
      # Check command registration
      grep -q "cmdRoadmapSync" get-stuff-done/bin/lib/roadmap.cjs &&
      grep -q "case 'sync':" get-stuff-done/bin/gsd-tools.cjs &&
      # Run command dry-run (should succeed even without Plane configured)
      node ./get-stuff-done/bin/gsd-tools.cjs roadmap sync
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -q "function cmdRoadmapSync" get-stuff-done/bin/lib/roadmap.cjs
    - grep -q "notifyRoadmapChange" get-stuff-done/bin/lib/roadmap.cjs
    - grep -q "case 'sync':" get-stuff-done/bin/gsd-tools.cjs
    - Running `node ./get-stuff-done/bin/gsd-tools.cjs roadmap sync` exits 0 and prints valid JSON containing "synced" or "reason"
  </acceptance_criteria>
  <done>Command `gsd-tools roadmap sync` available and ROADMAP updates trigger opportunistic Plane sync.</done>
</task>

</tasks>

<verification>
Phase 47 Plan 02 completion checks:
- All automated tests pass (plane-client + roadmap-plane-sync)
- Command registration present in both files
- Hook call present in cmdRoadmapUpdatePlanProgress
- `gsd-tools roadmap sync` executes without error
</verification>

<success_criteria>
Both tasks completed; tests green; command working; sync integrated.
</success_criteria>

<output>
After completion, create `.planning/phases/47-plane-roadmap-sync/47-02-SUMMARY.md`
</output>

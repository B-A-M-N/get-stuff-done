---
phase: 26-research-implement-ast-aware-normalization-tree-sitter
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified: [get-stuff-done/bin/lib/internal-normalizer.cjs, get-stuff-done/bin/lib/firecrawl-normalizer.cjs, tests/ast-normalization.test.cjs]
autonomous: true
requirements: [AST-INTERNAL-01, AST-EXTERNAL-01, AST-VERIFY-01]

must_haves:
  truths:
    - "Internal JS/TS files in .planning/ are parsed for symbols and dependencies"
    - "Fenced JS/TS code blocks in Firecrawl documentation are parsed for symbols"
    - "Symbols are persisted in the analysis field of ContextArtifacts"
  artifacts:
    - path: "tests/ast-normalization.test.cjs"
      provides: "E2E verification of AST-aware normalization"
  key_links:
    - from: "get-stuff-done/bin/lib/internal-normalizer.cjs"
      to: "get-stuff-done/bin/lib/ast-parser.cjs"
      via: "require"
    - from: "get-stuff-done/bin/lib/firecrawl-normalizer.cjs"
      to: "get-stuff-done/bin/lib/ast-parser.cjs"
      via: "require"
---

<objective>
Integrate the AST parser into the normalization pipeline for both internal and external sources, and verify symbol extraction through comprehensive tests.

Purpose: Automate the extraction of code symbols to build a richer second brain context.
Output: AST-integrated normalizers and a new test suite.
</objective>

<execution_context>
@/home/bamn/.gemini/get-shit-done/workflows/execute-plan.md
@/home/bamn/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@get-stuff-done/bin/lib/internal-normalizer.cjs
@get-stuff-done/bin/lib/firecrawl-normalizer.cjs

<interfaces>
From get-stuff-done/bin/lib/ast-parser.cjs (Plan 01):
```javascript
/**
 * @param {string} code
 * @param {string} language
 * @returns {{ symbols: Array<{name: string, kind: string, line: number}>, dependencies: string[] }}
 */
function parseCode(code, language = 'javascript');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate AST parser into internal-normalizer.cjs</name>
  <files>get-stuff-done/bin/lib/internal-normalizer.cjs</files>
  <action>
    Update normalizeInternal to detect if a file is .js or .ts.
    If so, call parseCode from ast-parser.cjs on the raw content.
    Populate the analysis field in the artifact before validation and return.
  </action>
  <verify>
    <automated>node -e "const { normalizeInternal } = require('./get-stuff-done/bin/lib/internal-normalizer.cjs'); const res = normalizeInternal('.'); console.log(JSON.stringify(res, null, 2))"</automated>
  </verify>
  <done>Internal normalizer populates analysis field for code files.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate AST parser into firecrawl-normalizer.cjs</name>
  <files>get-stuff-done/bin/lib/firecrawl-normalizer.cjs</files>
  <action>
    Update normalizeFirecrawl to scan content_markdown for fenced code blocks with 'javascript', 'js', 'typescript', or 'ts' language identifiers.
    Extract the code and call parseCode for each block.
    Merge symbols and dependencies from all blocks into the analysis field of the resulting artifact.
  </action>
  <verify>
    <automated>node -e "const { normalizeFirecrawl } = require('./get-stuff-done/bin/lib/firecrawl-normalizer.cjs'); const res = normalizeFirecrawl({ success: true, data: { metadata: { sourceURL: 'test' }, markdown: '\`\`\`js\nfunction test(){}\n\`\`\`' } }); console.log(res.analysis)"</automated>
  </verify>
  <done>Firecrawl normalizer populates analysis field from markdown code blocks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/ast-normalization.test.cjs</name>
  <files>tests/ast-normalization.test.cjs</files>
  <behavior>
    - Test: Internal normalizer extracts symbols from a mock .js file.
    - Test: Firecrawl normalizer extracts symbols from markdown code blocks.
    - Test: Symbols found in code blocks include correct kind and line relative to block start or absolute? (Absolute or block-relative depends on implementation choice - let's aim for relative to block start or just documented line).
    - Test: Parity check between internal and external extraction quality.
  </behavior>
  <action>
    Implement behavioral tests for the AST normalization logic.
    Use mocks for file system and Firecrawl responses.
  </action>
  <verify>
    <automated>node tests/ast-normalization.test.cjs</automated>
  </verify>
  <done>Comprehensive test suite confirms AST parsing integration works as expected.</done>
</task>

</tasks>

<verification>
Run all tests and verify analysis fields are present in normalized outputs.
</verification>

<success_criteria>
- Internal normalizer extracts AST symbols.
- Firecrawl normalizer extracts AST symbols from markdown.
- Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/26-research-implement-ast-aware-normalization-tree-sitter/26-02-SUMMARY.md`
</output>

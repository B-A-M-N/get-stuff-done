---
phase: 25
plan: "02"
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - get-stuff-done/bin/lib/context.cjs
  - tests/normalization-parity.test.cjs
autonomous: true
requirements: [NORMAL-PARITY]
must_haves:
  truths:
    - "The context build command now reads from the artifact store, ensuring full normalization of project context"
    - "Internal and external artifacts are validated against the same contract and share deterministic identity logic"
  artifacts:
    - path: "get-stuff-done/bin/lib/context.cjs"
      provides: "Pipeline integration in CLI"
    - path: "tests/normalization-parity.test.cjs"
      provides: "Parity and idempotency verification"
  key_links:
    - from: "get-stuff-done/bin/lib/context.cjs"
      to: "get-stuff-done/bin/lib/internal-normalizer.cjs"
      via: "require"
    - from: "tests/normalization-parity.test.cjs"
      to: "get-stuff-done/bin/lib/internal-normalizer.cjs"
      via: "require"
    - from: "tests/normalization-parity.test.cjs"
      to: "get-stuff-done/bin/lib/firecrawl-normalizer.cjs"
      via: "require"
---

# Plan 25-02: Pipeline Integration & Parity Testing

## Goal
Integrate normalizers into the `context build` CLI command and verify structural parity between internal and external artifacts.

<objective>
Ensure the context build process is fully decoupled from raw source documents, forcing use of normalized artifacts.
</objective>

<execution_context>
@/home/bamn/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-research-unified-normalization-pipeline/25-RESEARCH.md
@.planning/phases/25-research-unified-normalization-pipeline/25-01-SUMMARY.md
@get-stuff-done/bin/lib/context.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update context.cjs integration</name>
  <files>get-stuff-done/bin/lib/context.cjs</files>
  <action>
Modify the `context build` command to run the normalization pipeline. Ensure the workflow state is built from normalized artifacts in the `artifacts/` store rather than reading raw files from `.planning/`.
  </action>
  <verify>
    <automated>node get-stuff-done/bin/lib/context.cjs --help | grep "build"</automated>
  </verify>
  <done>Context build command leverages normalized artifact store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add normalization parity tests</name>
  <files>tests/normalization-parity.test.cjs</files>
  <behavior>
    - Idempotency: Normalizing the same file twice results in the same Artifact ID.
    - Contract Parity: Internal MD artifacts and Firecrawl JSON artifacts share same keys and valid structure.
  </behavior>
  <action>
Implement `tests/normalization-parity.test.cjs`. Use a sample `.planning/*.md` file and a sample Firecrawl JSON structure. Verify both produce valid artifacts that share structural parity.
  </action>
  <verify>
    <automated>node --test tests/normalization-parity.test.cjs</automated>
  </verify>
  <done>Parity and idempotency tests pass.</done>
</task>

</tasks>

<success_criteria>
- `context build` successfully uses the normalization pipeline.
- Parity tests pass, proving that internal and external data are handled uniformly.
</success_criteria>

<output>
After completion, create `.planning/phases/25-research-unified-normalization-pipeline/25-02-SUMMARY.md`
</output>

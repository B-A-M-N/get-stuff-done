---
phase: 25
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - get-stuff-done/bin/lib/internal-normalizer.cjs
  - get-stuff-done/bin/lib/firecrawl-normalizer.cjs
autonomous: true
requirements: [NORMAL-INTERNAL, NORMAL-FIRE]
must_haves:
  truths:
    - "Internal planning files are correctly transformed into validated ContextArtifact objects"
    - "External Firecrawl documentation is correctly transformed into validated ContextArtifact objects"
  artifacts:
    - path: "get-stuff-done/bin/lib/internal-normalizer.cjs"
      provides: "Internal document normalization"
    - path: "get-stuff-done/bin/lib/firecrawl-normalizer.cjs"
      provides: "Firecrawl JSON normalization"
  key_links:
    - from: "get-stuff-done/bin/lib/internal-normalizer.cjs"
      to: "get-stuff-done/bin/lib/artifact-schema.cjs"
      via: "require"
    - from: "get-stuff-done/bin/lib/firecrawl-normalizer.cjs"
      to: "get-stuff-done/bin/lib/artifact-schema.cjs"
      via: "require"
---

# Plan 25-01: Implementation of Normalization Adapters

## Goal
Create the core adapters that transform raw source data into validated `ContextArtifact` objects.

<objective>
Implement internal and external normalizers to provide a unified stream of validated ContextArtifacts.
</objective>

<execution_context>
@/home/bamn/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-research-unified-normalization-pipeline/25-RESEARCH.md
@get-stuff-done/bin/lib/artifact-schema.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create internal-normalizer</name>
  <files>get-stuff-done/bin/lib/internal-normalizer.cjs</files>
  <action>
Implement `internal-normalizer.cjs` to glob `.planning/*.md` files, extract metadata/content, and use `normalizeMd` (from core.cjs) and `generateArtifactId` (from identity.cjs) to produce `ContextArtifact` objects. Ensure it validates output against `ContextArtifactSchema`.
  </action>
  <verify>
    <automated>node -e "const m = require('./get-stuff-done/bin/lib/internal-normalizer.cjs'); if (typeof m.normalizeInternal !== 'function') throw new Error('normalizeInternal export missing');"</automated>
  </verify>
  <done>Module exports normalizeInternal function that produces valid ContextArtifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Create firecrawl-normalizer</name>
  <files>get-stuff-done/bin/lib/firecrawl-normalizer.cjs</files>
  <action>
Implement `firecrawl-normalizer.cjs` as a wrapper for external Firecrawl JSON results. It should map the Firecrawl fields (markdown, metadata) to the `ContextArtifact` schema and use the deterministic ID generation logic.
  </action>
  <verify>
    <automated>node -e "const m = require('./get-stuff-done/bin/lib/firecrawl-normalizer.cjs'); if (typeof m.normalizeFirecrawl !== 'function') throw new Error('normalizeFirecrawl export missing');"</automated>
  </verify>
  <done>Module exports normalizeFirecrawl function that maps external data to ContextArtifacts.</done>
</task>

</tasks>

<success_criteria>
- Both normalizers are implemented and export their respective normalization functions.
- Output from both normalizers conforms to the ContextArtifact Zod schema.
</success_criteria>

<output>
After completion, create `.planning/phases/25-research-unified-normalization-pipeline/25-01-SUMMARY.md`
</output>

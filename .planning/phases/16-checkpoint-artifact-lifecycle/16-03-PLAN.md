---
phase: 16-checkpoint-artifact-lifecycle
plan: "03"
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - get-stuff-done/workflows/execute-plan.md
  - get-stuff-done/workflows/execute-phase.md
autonomous: true
requirements:
  - CHECKPOINT-01
  - CHECKPOINT-03
must_haves:
  truths:
    - "execute-plan.md §checkpoint_return_for_orchestrator instructs the executor to write CHECKPOINT.md before returning structured state"
    - "The CHECKPOINT.md write instruction specifies YAML frontmatter with all required fields: status pending, type, why_blocked, what_is_uncertain, choices, allow_freeform, resume_condition, resolved_at ~"
    - "execute-plan.md §checkpoint_return_for_orchestrator includes a commit step for the written CHECKPOINT.md"
    - "execute-phase.md §checkpoint_handling instructs the orchestrator to call `state checkpoint --status awaiting-response` after presenting to user, and to call `state checkpoint --status resolved --path \"\"` and `frontmatter set status resolved` + `frontmatter set resolved_at` after continuation completes"
    - "Both execute-phase.md transitions include a commit step pairing CHECKPOINT.md and STATE.md together"
  artifacts:
    - path: "get-stuff-done/workflows/execute-plan.md"
      provides: "CHECKPOINT.md write step in checkpoint_return_for_orchestrator"
      contains: "CHECKPOINT.md"
    - path: "get-stuff-done/workflows/execute-phase.md"
      provides: "awaiting-response and resolved status transition steps in checkpoint_handling"
      contains: "awaiting-response"
  key_links:
    - from: "get-stuff-done/workflows/execute-plan.md"
      to: ".planning/phases/{phase}/CHECKPOINT.md"
      via: "Write tool — YAML frontmatter with status: pending"
      pattern: "CHECKPOINT\\.md"
    - from: "get-stuff-done/workflows/execute-phase.md"
      to: "gsd-tools.cjs state checkpoint"
      via: "node gsd-tools.cjs state checkpoint --status awaiting-response --path {path}"
      pattern: "state checkpoint"
---

<objective>
Add the CHECKPOINT.md write step to execute-plan.md (executor writes on block — CHECKPOINT-01) and add the awaiting-response and resolved status transition steps to execute-phase.md (orchestrator lifecycle management — CHECKPOINT-03 transitions).

Purpose: These workflow file changes make checkpoints persistent across session boundaries. Without them, checkpoint payloads are validated in-flight and discarded — the v0.2.0 brownfield audit finding that this phase fixes.

Output: execute-plan.md has a CHECKPOINT.md write + commit step before checkpoint_return_for_orchestrator; execute-phase.md §checkpoint_handling has awaiting-response transition after presenting to user and resolved transition after continuation completes.
</objective>

<execution_context>
@/home/bamn/.claude/get-stuff-done/workflows/execute-plan.md
@/home/bamn/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-checkpoint-artifact-lifecycle/16-CONTEXT.md

<interfaces>
<!-- Exact content blocks to add — executor copies these verbatim -->

CHECKPOINT.md file format (from RESEARCH.md — validated against checkpointArtifactSchema):
```
---
status: pending
type: {checkpoint_type}
why_blocked: "{why_blocked}"
what_is_uncertain: "{what_is_uncertain}"
choices: "{choices}"
allow_freeform: {allow_freeform}
resume_condition: "{resume_condition}"
resolved_at: ~
---

## Checkpoint Details

**Type:** {checkpoint_type}
**Blocked at:** Task {task_number} — {task_name}

[Human-readable summary of what caused the block and what the user needs to do]
```

gsd-tools commands available (from 16-01 plan):
- `node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" state checkpoint --status awaiting-response --path "{phase_dir}/CHECKPOINT.md"`
- `node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" state checkpoint --status resolved --path ""`
- `node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" frontmatter set "{phase_dir}/CHECKPOINT.md" --field status --value '"awaiting-response"'`
- `node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" frontmatter set "{phase_dir}/CHECKPOINT.md" --field status --value '"resolved"'`
- `node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" frontmatter set "{phase_dir}/CHECKPOINT.md" --field resolved_at --value '"$RESOLVED_AT"'`
- `node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" commit "chore({phase}-checkpoint): ..." --files {files}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CHECKPOINT.md write step to execute-plan.md §checkpoint_return_for_orchestrator</name>
  <files>get-stuff-done/workflows/execute-plan.md</files>
  <read_first>
    - get-stuff-done/workflows/execute-plan.md (full file — locate `<step name="checkpoint_return_for_orchestrator">` and read its current content carefully; the write step is inserted BEFORE the "Required return:" section)
    - get-stuff-done/bin/lib/artifact-schema.cjs (confirm exact field names for CHECKPOINT.md frontmatter: status, type, why_blocked, what_is_uncertain, choices, allow_freeform, resume_condition, resolved_at)
  </read_first>
  <action>
In `get-stuff-done/workflows/execute-plan.md`, find `<step name="checkpoint_return_for_orchestrator">`. Inside that step, insert a new section BEFORE the existing "**Required return:**" line. The new content to add:

```markdown
**Before returning checkpoint state, write CHECKPOINT.md to the phase directory:**

Write `{phase_dir}/CHECKPOINT.md` using the Write tool with the following YAML frontmatter + markdown body. Populate all fields from the checkpoint payload:

```
---
status: pending
type: {checkpoint_type}
why_blocked: "{why_blocked}"
what_is_uncertain: "{what_is_uncertain}"
choices: "{choices}"
allow_freeform: {allow_freeform}
resume_condition: "{resume_condition}"
resolved_at: ~
---

## Checkpoint Details

**Type:** {checkpoint_type}
**Blocked at:** Task {task_number} — {task_name}

[Human-readable summary of what caused the block and what the user needs to do]
```

Then commit the artifact:
```bash
node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" commit \
  "chore({phase}-checkpoint): write checkpoint artifact [task {task_number}]" \
  --files "{phase_dir}/CHECKPOINT.md"
```

Note: If a CHECKPOINT.md already exists in this phase directory, overwrite it — one active checkpoint per phase at a time; prior checkpoints are preserved in git history.

```

The existing "**Required return:**" content remains unchanged after this insertion. The structural order becomes:
1. New: Write CHECKPOINT.md using Write tool
2. New: Commit CHECKPOINT.md
3. Existing: Required return: 1) Completed Tasks table ... 2) Current Task ... etc.
  </action>
  <verify>
    <automated>grep -n "CHECKPOINT.md" get-stuff-done/workflows/execute-plan.md | head -10</automated>
  </verify>
  <acceptance_criteria>
    - get-stuff-done/workflows/execute-plan.md contains `Write \`{phase_dir}/CHECKPOINT.md\`` (or equivalent phrasing about writing to phase_dir/CHECKPOINT.md)
    - get-stuff-done/workflows/execute-plan.md contains `status: pending` in the CHECKPOINT.md frontmatter template
    - get-stuff-done/workflows/execute-plan.md contains `why_blocked:` in the CHECKPOINT.md frontmatter template
    - get-stuff-done/workflows/execute-plan.md contains `what_is_uncertain:` in the CHECKPOINT.md frontmatter template
    - get-stuff-done/workflows/execute-plan.md contains `resolved_at: ~` in the template
    - get-stuff-done/workflows/execute-plan.md contains `chore({phase}-checkpoint): write checkpoint artifact` commit message
    - grep for "CHECKPOINT.md" in execute-plan.md returns at least 3 matches (write instruction + frontmatter template + commit)
  </acceptance_criteria>
  <done>execute-plan.md §checkpoint_return_for_orchestrator instructs executors to write CHECKPOINT.md with status: pending before returning structured state, and to commit the artifact</done>
</task>

<task type="auto">
  <name>Task 2: Add awaiting-response and resolved transitions to execute-phase.md §checkpoint_handling</name>
  <files>get-stuff-done/workflows/execute-phase.md</files>
  <read_first>
    - get-stuff-done/workflows/execute-phase.md (full file — locate `<step name="checkpoint_handling">` and read steps 3.5, 4, 5, 6, 7 carefully; transitions insert after step 4 "Present to user" and after step 7 "Continuation agent verifies previous commits")
  </read_first>
  <action>
In `get-stuff-done/workflows/execute-phase.md`, find `<step name="checkpoint_handling">`. Make two additions:

**Addition 1 — awaiting-response transition** (insert after step 4 "Present to user", before step 5 "User responds"):

Add a new step 4.5:

```markdown
4.5. **Transition CHECKPOINT.md and STATE.md to awaiting-response** (after presenting to user, before waiting for response):
   ```bash
   # Update STATE.md checkpoint lifecycle fields atomically
   node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" state checkpoint \
     --status awaiting-response \
     --path "{phase_dir}/CHECKPOINT.md"

   # Update CHECKPOINT.md status field
   node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" frontmatter set \
     "{phase_dir}/CHECKPOINT.md" --field status --value '"awaiting-response"'

   # Commit both files together
   node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" commit \
     "chore({phase}-checkpoint): update checkpoint to awaiting-response" \
     --files "{phase_dir}/CHECKPOINT.md" .planning/STATE.md
   ```
```

**Addition 2 — resolved transition** (insert after step 8 "Repeat until plan completes or user stops", or at the end of the standard flow block, before "Why fresh agent, not resume"):

Add a new step 8.5:

```markdown
8.5. **Transition CHECKPOINT.md and STATE.md to resolved** (after continuation agent completes successfully):
   ```bash
   RESOLVED_AT=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

   # Mark CHECKPOINT.md resolved with timestamp
   node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" frontmatter set \
     "{phase_dir}/CHECKPOINT.md" --field status --value '"resolved"'
   node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" frontmatter set \
     "{phase_dir}/CHECKPOINT.md" --field resolved_at --value "\"$RESOLVED_AT\""

   # Clear checkpoint fields from STATE.md (empty values excluded from frontmatter by falsy guard)
   node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" state checkpoint \
     --status "" --path ""

   # Commit both files together
   node "$HOME/.claude/get-stuff-done/bin/gsd-tools.cjs" commit \
     "chore({phase}-checkpoint): resolve checkpoint" \
     --files "{phase_dir}/CHECKPOINT.md" .planning/STATE.md
   ```

   Note: CHECKPOINT.md is kept in the phase directory as a resolved audit artifact (status: resolved, resolved_at set). It does not interfere with future checkpoints because resume-project checks the status field, not file presence.
```
  </action>
  <verify>
    <automated>grep -n "awaiting-response\|resolved_at\|state checkpoint" get-stuff-done/workflows/execute-phase.md | head -15</automated>
  </verify>
  <acceptance_criteria>
    - get-stuff-done/workflows/execute-phase.md contains `state checkpoint` (the CLI subcommand call)
    - get-stuff-done/workflows/execute-phase.md contains `--status awaiting-response`
    - get-stuff-done/workflows/execute-phase.md contains `--status resolved`  (wait — clear uses `--status ""`, not resolved; check: the resolved transition uses `--status ""` to clear. The frontmatter set uses `'"resolved"'`. Both strings must appear)
    - get-stuff-done/workflows/execute-phase.md contains `frontmatter set` with `--field status --value '"awaiting-response"'`
    - get-stuff-done/workflows/execute-phase.md contains `frontmatter set` with `--field resolved_at`
    - get-stuff-done/workflows/execute-phase.md contains `chore({phase}-checkpoint): update checkpoint to awaiting-response`
    - get-stuff-done/workflows/execute-phase.md contains `chore({phase}-checkpoint): resolve checkpoint`
    - get-stuff-done/workflows/execute-phase.md contains `RESOLVED_AT=$(date -u`
  </acceptance_criteria>
  <done>execute-phase.md §checkpoint_handling has both lifecycle transitions: awaiting-response written after presenting to user; resolved written after continuation agent completes; both transitions commit CHECKPOINT.md and STATE.md together</done>
</task>

</tasks>

<verification>
```bash
# Verify execute-plan.md write step is present
grep -c "CHECKPOINT.md" get-stuff-done/workflows/execute-plan.md
# Expect >= 3

# Verify execute-phase.md transitions are present
grep -c "state checkpoint\|awaiting-response\|resolved_at" get-stuff-done/workflows/execute-phase.md
# Expect >= 3

# Run test suite — no regressions from workflow doc changes
node scripts/run-tests.cjs 2>&1 | tail -10
```
</verification>

<success_criteria>
- execute-plan.md §checkpoint_return_for_orchestrator has CHECKPOINT.md write step with all required frontmatter fields and a commit
- execute-phase.md §checkpoint_handling has awaiting-response transition (after present, before user response) and resolved transition (after continuation completes)
- Both transitions commit paired CHECKPOINT.md + STATE.md
- Existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-checkpoint-artifact-lifecycle/16-03-SUMMARY.md`
</output>

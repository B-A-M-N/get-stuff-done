---
phase: 16-checkpoint-artifact-lifecycle
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/checkpoint-lifecycle.test.cjs
autonomous: true
requirements:
  - CHECKPOINT-01
  - CHECKPOINT-02
  - CHECKPOINT-03
must_haves:
  truths:
    - "tests/checkpoint-lifecycle.test.cjs exists and runs without syntax errors"
    - "The test file has describe blocks covering: cmdStateCheckpoint atomicity, buildStateFrontmatter checkpoint field extraction, resume routing for missing/invalid/valid/resolved artifact, and checkpointArtifactSchema rejection of malformed input"
    - "All tests are FAILING (stubs return TODO errors) — Wave 0 Nyquist compliance: tests exist before implementation"
  artifacts:
    - path: "tests/checkpoint-lifecycle.test.cjs"
      provides: "Test scaffold for CHECKPOINT-01/02/03 behavioral coverage"
      contains: "cmdStateCheckpoint"
  key_links:
    - from: "tests/checkpoint-lifecycle.test.cjs"
      to: "get-stuff-done/bin/lib/state.cjs"
      via: "require('../get-stuff-done/bin/lib/state.cjs')"
      pattern: "require.*state\\.cjs"
    - from: "tests/checkpoint-lifecycle.test.cjs"
      to: "get-stuff-done/bin/lib/artifact-schema.cjs"
      via: "require('../get-stuff-done/bin/lib/artifact-schema.cjs')"
      pattern: "require.*artifact-schema"
---

<objective>
Create `tests/checkpoint-lifecycle.test.cjs` with a complete test scaffold covering all three checkpoint lifecycle requirements (CHECKPOINT-01/02/03). Tests are written BEFORE implementation completes — they must fail now and pass after plans 16-03 and 16-04 complete.

Purpose: Nyquist rule compliance — no 3 consecutive implementation tasks without an automated verify command. This scaffold establishes the verification baseline that subsequent plans must make pass.

Output: `tests/checkpoint-lifecycle.test.cjs` with stubs for: (1) cmdStateCheckpoint atomicity, (2) buildStateFrontmatter checkpoint field extraction, (3) checkpointArtifactSchema validation, (4) resume routing logic for all four status branches.
</objective>

<execution_context>
@/home/bamn/.claude/get-stuff-done/workflows/execute-plan.md
@/home/bamn/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Key contracts for test stubs — executor reads these to write correct test expectations -->

From get-stuff-done/bin/lib/artifact-schema.cjs:
```javascript
const checkpointArtifactSchema = z.object({
  status: z.enum(['pending', 'awaiting-response', 'resolved']),
  type: z.string().min(1),
  why_blocked: z.string().min(1),
  what_is_uncertain: z.string().min(1),
  choices: z.string().min(1),
  allow_freeform: z.union([z.boolean(), z.enum(['true', 'false'])]),
  resume_condition: z.string().min(1),
  resolved_at: z.string().optional(),
});
// module.exports: { checkpointArtifactSchema, parseCheckpointArtifact, ... }
```

From get-stuff-done/bin/lib/state.cjs (after plan 16-01 adds it):
```javascript
// cmdStateCheckpoint(cwd, { status, checkpointPath }, raw)
// Writes Checkpoint Status and Checkpoint Path body fields atomically
// buildStateFrontmatter extracts them and adds to fm when truthy
```

Existing test file pattern (from tests/state.test.cjs):
```javascript
const { test, describe } = require('node:test');
const assert = require('node:assert/strict');
// tests use tmp dirs via fs.mkdtempSync
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create checkpoint-lifecycle.test.cjs scaffold with all required describe blocks</name>
  <files>tests/checkpoint-lifecycle.test.cjs</files>
  <read_first>
    - tests/state.test.cjs (full file — read to understand existing test patterns: tmp dir setup, require paths, assert patterns, describe/test structure)
    - get-stuff-done/bin/lib/artifact-schema.cjs (for exact field names and enum values to use in test assertions)
    - get-stuff-done/bin/lib/state.cjs (confirm export names after plan 16-01 — cmdStateCheckpoint, buildStateFrontmatter)
  </read_first>
  <action>
Create `tests/checkpoint-lifecycle.test.cjs` using the Node.js built-in test runner (`node:test`). Model the file structure exactly after `tests/state.test.cjs` (tmp dir pattern, require paths, describe/test/assert layout).

The file must contain four describe blocks:

**Block 1: cmdStateCheckpoint atomicity (CHECKPOINT-03)**
```javascript
describe('cmdStateCheckpoint', () => {
  test('writes Checkpoint Status and Checkpoint Path atomically', () => {
    // Setup: create tmp dir with minimal STATE.md that has Checkpoint Status: and Checkpoint Path: lines
    // Action: call cmdStateCheckpoint(tmpDir, { status: 'pending', checkpointPath: '.planning/phases/X/CHECKPOINT.md' }, false)
    // Assert: STATE.md contains "Checkpoint Status: pending" and "Checkpoint Path: .planning/phases/X/CHECKPOINT.md"
    // Assert: frontmatter (extractFrontmatter) has checkpoint_status: 'pending' and checkpoint_path set
    throw new Error('TODO: implement after plan 16-01 completes');
  });

  test('clears checkpoint fields when status is empty string', () => {
    // Setup: STATE.md with Checkpoint Status: pending, Checkpoint Path: X
    // Action: cmdStateCheckpoint(tmpDir, { status: '', checkpointPath: '' }, false)
    // Assert: STATE.md body has "Checkpoint Status: " (empty value)
    // Assert: frontmatter does NOT include checkpoint_status or checkpoint_path (falsy guard)
    throw new Error('TODO: implement after plan 16-01 completes');
  });
});
```

**Block 2: buildStateFrontmatter checkpoint field extraction (CHECKPOINT-03)**
```javascript
describe('buildStateFrontmatter checkpoint fields', () => {
  test('includes checkpoint_status when body has non-empty Checkpoint Status field', () => {
    // Setup: build a STATE.md body string with "Checkpoint Status: awaiting-response"
    // Action: call buildStateFrontmatter(bodyContent, null)
    // Assert: result.checkpoint_status === 'awaiting-response'
    throw new Error('TODO: implement after plan 16-01 completes');
  });

  test('excludes checkpoint_status from frontmatter when field is empty', () => {
    // Setup: body with "Checkpoint Status: " (empty)
    // Action: buildStateFrontmatter(bodyContent, null)
    // Assert: result.checkpoint_status is undefined (field omitted)
    throw new Error('TODO: implement after plan 16-01 completes');
  });
});
```

**Block 3: checkpointArtifactSchema validation (CHECKPOINT-01 contract)**
```javascript
describe('checkpointArtifactSchema', () => {
  test('accepts a fully valid CHECKPOINT.md frontmatter object', () => {
    const valid = {
      status: 'pending',
      type: 'human-verify',
      why_blocked: 'Layout needs visual confirmation',
      what_is_uncertain: 'Whether mobile layout matches design',
      choices: '[approve, reject]',
      allow_freeform: true,
      resume_condition: 'User types approved or describes issues',
    };
    // Assert: checkpointArtifactSchema.safeParse(valid).success === true
    throw new Error('TODO: implement — schema already exists in Phase 15');
  });

  test('rejects object missing required field why_blocked', () => {
    const invalid = { status: 'pending', type: 'human-verify', what_is_uncertain: 'x', choices: '[a]', allow_freeform: true, resume_condition: 'y' };
    // Assert: checkpointArtifactSchema.safeParse(invalid).success === false
    // Assert: result.error.issues has at least one issue mentioning 'why_blocked'
    throw new Error('TODO: implement — schema already exists in Phase 15');
  });

  test('rejects invalid status value', () => {
    const invalid = { status: 'unknown', type: 'human-verify', why_blocked: 'x', what_is_uncertain: 'x', choices: '[a]', allow_freeform: true, resume_condition: 'y' };
    // Assert: safeParse.success === false
    throw new Error('TODO: implement — schema already exists in Phase 15');
  });
});
```

**Block 4: resume routing logic (CHECKPOINT-02)**
```javascript
describe('resume-project checkpoint routing', () => {
  // These tests verify the routing LOGIC described in resume-project.md.
  // They test the decision tree: given a STATE.md checkpoint_status + an optional CHECKPOINT.md,
  // what message/action does the router produce?
  // Since resume-project.md is a workflow document (not a .cjs module), these are
  // integration-style assertions on the CLI output of gsd-tools state json + file presence.

  test('no routing needed when checkpoint_status is absent from STATE.md frontmatter', () => {
    // Setup: STATE.md with no checkpoint fields set
    // Assert: state json output has no checkpoint_status field
    throw new Error('TODO: implement after plan 16-04 completes');
  });

  test('no routing needed when checkpoint_status is "resolved"', () => {
    // Setup: STATE.md with Checkpoint Status: resolved
    // Assert: state json shows checkpoint_status: 'resolved'
    // Resume should skip routing (resolved = normal flow)
    throw new Error('TODO: implement after plan 16-04 completes');
  });

  test('fallback behavior when checkpoint_status is awaiting-response but file is missing', () => {
    // Setup: STATE.md with Checkpoint Status: awaiting-response, Checkpoint Path: /nonexistent/CHECKPOINT.md
    // This test documents expected behavior: state json returns the status correctly
    // Actual resume-project routing is workflow-level; test verifies state read is correct
    throw new Error('TODO: implement after plan 16-04 completes');
  });

  test('valid CHECKPOINT.md parses correctly with checkpointArtifactSchema', () => {
    // Setup: write a CHECKPOINT.md with all required fields at status: awaiting-response
    // Action: read file, extractFrontmatter, safeParse with checkpointArtifactSchema
    // Assert: success === true, data.status === 'awaiting-response'
    throw new Error('TODO: implement after plan 16-04 completes');
  });
});
```

Use the exact import paths from existing tests in the project (read tests/state.test.cjs to confirm). Add a module-level comment explaining which plan completes each block.
  </action>
  <verify>
    <automated>node --test tests/checkpoint-lifecycle.test.cjs 2>&1 | grep -E "(FAIL|fail|Error|TODO)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/checkpoint-lifecycle.test.cjs exists on disk
    - `node --test tests/checkpoint-lifecycle.test.cjs 2>&1` exits with non-zero (tests fail as expected — stubs throw TODO errors)
    - Output contains "TODO" or "Error" strings (confirming stubs are active, not passing vacuously)
    - File contains `describe('cmdStateCheckpoint'`
    - File contains `describe('buildStateFrontmatter checkpoint fields'`
    - File contains `describe('checkpointArtifactSchema'`
    - File contains `describe('resume-project checkpoint routing'`
    - File uses `require('node:test')` (matching project test runner pattern)
  </acceptance_criteria>
  <done>tests/checkpoint-lifecycle.test.cjs exists with four describe blocks, all tests failing with TODO errors — ready for implementation plans to make them pass</done>
</task>

</tasks>

<verification>
```bash
# Confirm file exists and fails correctly
node --test tests/checkpoint-lifecycle.test.cjs 2>&1 | tail -20

# Confirm syntax is valid (node parses the file)
node -c tests/checkpoint-lifecycle.test.cjs && echo "syntax ok"

# Confirm no accidental passing tests
node --test tests/checkpoint-lifecycle.test.cjs 2>&1 | grep "pass" | wc -l
# Should be 0
```
</verification>

<success_criteria>
- tests/checkpoint-lifecycle.test.cjs runs without syntax errors
- All test cases fail (TODO stubs) — confirms they are real failing tests, not vacuous passes
- Four describe blocks present matching the four lifecycle behaviors
</success_criteria>

<output>
After completion, create `.planning/phases/16-checkpoint-artifact-lifecycle/16-02-SUMMARY.md`
</output>

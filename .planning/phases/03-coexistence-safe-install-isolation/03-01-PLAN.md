---
phase: "03"
plan: "01"
name: "Isolate installed namespace and engine paths from upstream GSD"
wave: 1
requirements:
  - "FR-16"
---

# PLAN: 03-01 - Isolate installed namespace and engine paths from upstream GSD

## Goal
Make this fork install into its own command namespace and engine directory so it can coexist with an already-installed upstream GSD without overwriting shared assets.

## Read First
- `.planning/phases/03-coexistence-safe-install-isolation/03-RESEARCH.md`
- `bin/install.js`
- `commands/**/*.md`
- `get-stuff-done/workflows/**/*.md`
- `tests/copilot-install.test.cjs`
- `tests/antigravity-install.test.cjs`

## Tasks
<task type="auto">
  <name>Move installed command surface to dostuff-only</name>
  <files>bin/install.js, commands/**/*.md, tests/*.test.cjs</files>
  <action>
    Ensure this fork installs its user-facing commands only under the `dostuff` namespace instead of writing into the shared `gsd` command namespace.
    Keep the repo’s internal migration/reference artifacts as needed, but do not let installation of this fork overwrite upstream command files.
  </action>
  <acceptance_criteria>
    - Installing this fork does not replace upstream `/gsd:*` command files.
    - This fork exposes `/dostuff:*` as its installed command surface.
    - Runtime-specific install tests cover the isolated command surface.
  </acceptance_criteria>
  <verify>Focused install tests prove only fork-owned command assets are installed.</verify>
</task>

<task type="auto">
  <name>Move installed engine assets to a fork-specific path</name>
  <files>bin/install.js, commands/**/*.md, get-stuff-done/workflows/**/*.md, tests/*.test.cjs</files>
  <action>
    Give this fork a separate installed engine root and update command/workflow path rewriting so installed prompts resolve to that isolated engine directory rather than the shared upstream GSD path.
  </action>
  <acceptance_criteria>
    - The fork no longer installs into the shared upstream engine directory.
    - Installed command/workflow references resolve to the fork-specific engine root.
    - Existing upstream engine files are left untouched.
  </acceptance_criteria>
  <verify>Tests and manifest assertions confirm the isolated engine path is used.</verify>
</task>

## Verification
- Installing this fork leaves upstream `/gsd:*` command files untouched.
- Installed command references point at the fork-specific engine path.
- Coexistence safety is proven in focused install coverage before narrative work starts.

---
*Status: Ready*

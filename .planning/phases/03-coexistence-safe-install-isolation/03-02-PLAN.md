---
phase: "03"
plan: "02"
name: "Scope uninstall, manifest, and agents to fork-owned assets"
wave: 2
requirements:
  - "FR-16"
---

# PLAN: 03-02 - Scope uninstall, manifest, and agents to fork-owned assets

## Goal
Finish coexistence safety by ensuring uninstall, patch backup, manifest generation, and installed agent naming only operate on this fork’s assets.

## Read First
- `.planning/phases/03-coexistence-safe-install-isolation/03-RESEARCH.md`
- `.planning/phases/03-coexistence-safe-install-isolation/03-01-PLAN.md`
- `bin/install.js`
- `agents/*.md`
- `tests/copilot-install.test.cjs`
- `tests/antigravity-install.test.cjs`

## Tasks
<task type="auto">
  <name>Scope uninstall and manifest logic to the fork</name>
  <files>bin/install.js, tests/*.test.cjs</files>
  <action>
    Update uninstall, manifest, and patch-backup logic so running this fork’s lifecycle commands only touches fork-owned assets and does not remove or rewrite upstream GSD files.
  </action>
  <acceptance_criteria>
    - Uninstall removes only this fork’s installed assets.
    - Manifest entries describe only fork-owned files.
    - Patch backup logic does not capture upstream GSD files.
  </acceptance_criteria>
  <verify>Install/uninstall/manifest tests assert fork-only scope.</verify>
</task>

<task type="auto">
  <name>Isolate installed agents from upstream gsd-* names</name>
  <files>agents/*.md, commands/**/*.md, get-stuff-done/workflows/**/*.md, bin/install.js, tests/*.test.cjs</files>
  <action>
    Introduce a fork-safe installed agent namespace where needed and update command/workflow references so this fork does not rely on or overwrite upstream `gsd-*` agents in shared runtime directories.
  </action>
  <acceptance_criteria>
    - Installing this fork does not overwrite upstream `gsd-*` agent files.
    - Installed prompts resolve to fork-owned agents instead of upstream agents.
    - Runtime-specific install tests cover the isolated agent surface.
  </acceptance_criteria>
  <verify>Focused tests prove agent install isolation and command-to-agent references stay coherent.</verify>
</task>

## Verification
- Uninstall/manifest/patch handling is fork-scoped.
- Installed agent assets do not collide with upstream `gsd-*` agents.
- The fork is safe to install alongside real GSD before Phase 4 narrative work begins.

---
*Status: Ready*

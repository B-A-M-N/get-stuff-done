---
phase: 15-schema-foundation
plan: 04
type: execute
wave: 3
depends_on: ["15-03"]
files_modified:
  - get-stuff-done/bin/lib/verify.cjs
  - tests/checkpoint-validator.test.cjs
autonomous: true
gap_closure: true
requirements: [SCHEMA-04, CHECKPOINT-04]

must_haves:
  truths:
    - "cmdVerifyCheckpointResponse returns { valid: false, errors: [...] } on invalid input instead of throwing TypeError"
    - "checkpoint-validator.test.cjs 'rejects CHECKPOINT.md missing required fields' test passes in isolation"
  artifacts:
    - path: "get-stuff-done/bin/lib/verify.cjs"
      provides: "Working Zod v4 error path in cmdVerifyCheckpointResponse"
      contains: "result.error.issues.map"
    - path: "tests/checkpoint-validator.test.cjs"
      provides: "Passing CHECKPOINT-04 rejection unit test"
      contains: "result.error.issues"
  key_links:
    - from: "get-stuff-done/bin/lib/verify.cjs"
      to: "result.error.issues"
      via: "Zod v4 safeParse failure API"
      pattern: "result\\.error\\.issues\\.map"
    - from: "tests/checkpoint-validator.test.cjs"
      to: "result.error.issues"
      via: "Zod v4 safeParse failure API"
      pattern: "result\\.error\\.issues\\."
---

<objective>
Fix two Zod v3-to-v4 API bugs introduced in Plan 03 that break the checkpoint response validation failure path and a CHECKPOINT-04 unit test.

Purpose: Close the two verification gaps so SCHEMA-04 and CHECKPOINT-04 are fully satisfied. Without this fix, cmdVerifyCheckpointResponse throws TypeError on any invalid input (instead of returning errors), and the CHECKPOINT-04 rejection test fails silently.
Output: Two single-word fixes — `result.error.errors` changed to `result.error.issues` in both files.
</objective>

<execution_context>
@/home/bamn/.claude/get-shit-done/workflows/execute-plan.md
@/home/bamn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-schema-foundation/15-VERIFICATION.md
@.planning/phases/15-schema-foundation/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Zod v4 API in verify.cjs cmdVerifyCheckpointResponse</name>
  <files>get-stuff-done/bin/lib/verify.cjs</files>
  <read_first>
    - get-stuff-done/bin/lib/verify.cjs (see line 222 — the bug)
    - get-stuff-done/bin/lib/artifact-schema.cjs (reference: uses result.error.issues correctly)
  </read_first>
  <action>
On line 222 of verify.cjs, change the single word `errors` to `issues` in the property access.

Current (line 222):
```javascript
    const errors = result.error.errors.map(e => e.message);
```

Replace with:
```javascript
    const errors = result.error.issues.map(e => e.message);
```

This is the ONLY change in this file. Do not modify any other line.
  </action>
  <verify>
    <automated>cd /home/bamn/get-stuff-done && node -e "const v = require('./get-stuff-done/bin/lib/verify.cjs'); const fs = require('fs'); const tmp = '/tmp/bad-checkpoint.txt'; fs.writeFileSync(tmp, 'status: pending'); const out = []; const origLog = console.log; console.log = (...a) => out.push(a.join(' ')); v.cmdVerifyCheckpointResponse('/tmp', 'bad-checkpoint.txt', false); console.log = origLog; const r = JSON.parse(out[0]); if (r.valid !== false) { process.exit(1); } if (!Array.isArray(r.errors) || r.errors.length === 0) { process.exit(1); } console.log('PASS: cmdVerifyCheckpointResponse returns errors array on invalid input');"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "result.error.issues.map" get-stuff-done/bin/lib/verify.cjs returns line 222
    - grep -c "result.error.errors" get-stuff-done/bin/lib/verify.cjs returns 0 (no Zod v3 API remaining)
    - The automated verify command exits 0 and prints PASS
  </acceptance_criteria>
  <done>cmdVerifyCheckpointResponse returns { valid: false, errors: [...] } on invalid checkpoint input instead of throwing TypeError</done>
</task>

<task type="auto">
  <name>Task 2: Fix Zod v4 API in checkpoint-validator.test.cjs</name>
  <files>tests/checkpoint-validator.test.cjs</files>
  <read_first>
    - tests/checkpoint-validator.test.cjs (see lines 132-133 — the bugs)
    - tests/artifact-schema.test.cjs (reference: uses result.error.issues correctly throughout)
  </read_first>
  <action>
On lines 132 and 133 of tests/checkpoint-validator.test.cjs, change the property access from `errors` to `issues`.

Current (lines 132-133):
```javascript
  assert.ok(result.error.errors.length > 0);
  const paths = result.error.errors.map(e => e.path.join('.'));
```

Replace with:
```javascript
  assert.ok(result.error.issues.length > 0);
  const paths = result.error.issues.map(e => e.path.join('.'));
```

These are the ONLY changes in this file. Do not modify any other line.
  </action>
  <verify>
    <automated>cd /home/bamn/get-stuff-done && node --test tests/checkpoint-validator.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "result.error.issues" tests/checkpoint-validator.test.cjs returns lines 132 and 133
    - grep -c "result.error.errors" tests/checkpoint-validator.test.cjs returns 0 (no Zod v3 API remaining)
    - node --test tests/checkpoint-validator.test.cjs exits 0 with all tests passing
    - The specific test "checkpointArtifactSchema rejects CHECKPOINT.md missing required fields" passes (not masked)
  </acceptance_criteria>
  <done>All 5 checkpoint-validator tests pass including the CHECKPOINT-04 rejection test, with zero Zod v3 API usage remaining</done>
</task>

</tasks>

<verification>
Both gaps from 15-VERIFICATION.md are closed:

1. verify.cjs uses `result.error.issues.map()` (Zod v4) — cmdVerifyCheckpointResponse returns errors array on invalid input
2. checkpoint-validator.test.cjs uses `result.error.issues` (Zod v4) — CHECKPOINT-04 rejection test passes

Verify no Zod v3 `.errors` API remains in either file:
```bash
grep -rn "result\.error\.errors" get-stuff-done/bin/lib/verify.cjs tests/checkpoint-validator.test.cjs
```
Expected: no output (exit code 1).

Full test suite still passes:
```bash
node --test tests/checkpoint-validator.test.cjs && node --test tests/artifact-schema.test.cjs
```
</verification>

<success_criteria>
- Zero occurrences of `result.error.errors` in verify.cjs and checkpoint-validator.test.cjs
- cmdVerifyCheckpointResponse returns `{ valid: false, errors: [...] }` on invalid input (no TypeError)
- All 5 checkpoint-validator tests pass (including CHECKPOINT-04 rejection test)
- All 14 artifact-schema tests still pass (no regression)
- SCHEMA-04 fully satisfied: checkpointResponseSchema replaces manual checking with working error path
- CHECKPOINT-04 fully satisfied: rejection behavior has a passing unit test
</success_criteria>

<output>
After completion, create `.planning/phases/15-schema-foundation/15-04-SUMMARY.md`
</output>

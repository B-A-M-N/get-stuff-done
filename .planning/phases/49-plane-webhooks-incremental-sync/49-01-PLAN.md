---
phase: 49
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-stuff-done/bin/lib/plane-webhook-sync.cjs
  - get-stuff-done/bin/lib/planning-server.cjs
  - tests/plane-webhook-sync.test.cjs
autonomous: true
requirements:
  - PLANE-WEBHOOK-01
  - PLANE-TRIGGER-01
must_haves:
  truths:
    - "GSD exposes an authenticated inbound Plane webhook endpoint on the existing Planning Server"
    - "Supported Plane webhook payloads are normalized into a stable internal event shape"
    - "Accepted webhook events publish broker messages for downstream automation"
    - "Malformed or unauthorized requests fail clearly without crashing the server"
  artifacts:
    - path: "get-stuff-done/bin/lib/plane-webhook-sync.cjs"
      provides: "Plane webhook verification, normalization, and broker publication"
      contains: "handlePlaneWebhook / normalizePlaneWebhookEvent"
    - path: "get-stuff-done/bin/lib/planning-server.cjs"
      provides: "POST /v1/plane/webhook endpoint"
      contains: "/v1/plane/webhook route"
    - path: "tests/plane-webhook-sync.test.cjs"
      provides: "Focused webhook sync coverage"
      contains: "valid webhook, invalid auth, malformed payload, publish behavior"
  key_links:
    - from: "planning-server.cjs:/v1/plane/webhook"
      to: "plane-webhook-sync.cjs:handlePlaneWebhook"
      via: "route dispatch"
      pattern: "plane-webhook-sync"
    - from: "plane-webhook-sync.cjs"
      to: "broker.cjs:publish"
      via: "publish normalized inbound event"
      pattern: "broker\\.publish"
---

<objective>
Implement the first inbound Plane event path for GSD by adding a webhook endpoint to the existing Planning Server and normalizing accepted events into broker-published internal triggers.
</objective>

<execution_context>
@~/.claude/get-stuff-done/workflows/execute-plan.md
@~/.claude/get-stuff-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-plane-webhooks-incremental-sync/49-RESEARCH.md
@.planning/phases/48-plane-checkpoint-sync/48-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused webhook tests first</name>
  <files>
    tests/plane-webhook-sync.test.cjs
  </files>
  <read_first>
    - tests/roadmap-plane-sync.test.cjs
    - tests/plane-client.test.cjs
    - get-stuff-done/bin/lib/planning-server.cjs
  </read_first>
  <action>
    Create `tests/plane-webhook-sync.test.cjs` using `node:test`.

    Cover:
    - accepted webhook with valid token publishes a normalized broker event
    - unauthorized webhook is rejected
    - malformed JSON payload is rejected
    - unsupported webhook type returns a safe no-op result instead of crashing
    - normalized event preserves `plane_issue_id` and any present `gsd_phase_number` / `gsd_plan_id`

    Prefer direct handler tests with mocked broker publication over full broad server integration unless route coverage is needed for a specific behavior.
  </action>
  <verify>
    <automated>
      test -f tests/plane-webhook-sync.test.cjs
    </automated>
  </verify>
  <done>
    Focused webhook tests define the expected inbound behavior before implementation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Plane webhook normalization and publication</name>
  <files>
    get-stuff-done/bin/lib/plane-webhook-sync.cjs
  </files>
  <read_first>
    - get-stuff-done/bin/lib/broker.cjs
    - get-stuff-done/bin/lib/roadmap-plane-sync.cjs
    - get-stuff-done/bin/lib/core.cjs
  </read_first>
  <action>
    Create `get-stuff-done/bin/lib/plane-webhook-sync.cjs`.

    Required behavior:
    - verify inbound webhook authorization using a dedicated Plane webhook token env var
    - parse JSON safely from the raw request body or passed payload
    - normalize supported webhook events into a stable internal shape
    - publish normalized events through `broker.publish(...)`
    - degrade unsupported events to an explicit no-op result
    - avoid logging raw secrets or full unsafe payloads
  </action>
  <verify>
    <automated>
      test -f get-stuff-done/bin/lib/plane-webhook-sync.cjs && grep -q "broker.publish" get-stuff-done/bin/lib/plane-webhook-sync.cjs
    </automated>
  </verify>
  <done>
    Plane webhook handler exists as a standalone, testable module.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Planning Server webhook route</name>
  <files>
    get-stuff-done/bin/lib/planning-server.cjs
  </files>
  <read_first>
    - get-stuff-done/bin/lib/planning-server.cjs
    - get-stuff-done/bin/lib/plane-webhook-sync.cjs
  </read_first>
  <action>
    Extend `planning-server.cjs` with `POST /v1/plane/webhook`.

    Route requirements:
    - use existing server security headers and auth posture
    - read bounded JSON request bodies
    - call the new webhook handler module
    - return clear JSON for accepted, rejected, and ignored events
    - do not crash the server on malformed input
  </action>
  <verify>
    <automated>
      grep -q "/v1/plane/webhook" get-stuff-done/bin/lib/planning-server.cjs
    </automated>
  </verify>
  <done>
    Planning Server exposes a deterministic inbound Plane webhook route.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify focused webhook behavior</name>
  <files>
    tests/plane-webhook-sync.test.cjs
    get-stuff-done/bin/lib/plane-webhook-sync.cjs
    get-stuff-done/bin/lib/planning-server.cjs
  </files>
  <action>
    Run focused verification:
    - `node --check get-stuff-done/bin/lib/plane-webhook-sync.cjs`
    - `node --check get-stuff-done/bin/lib/planning-server.cjs`
    - `node --check tests/plane-webhook-sync.test.cjs`
    - `node --test tests/plane-webhook-sync.test.cjs`

    If route testing requires a disposable server instance, keep it local and tear it down in the test flow.
  </action>
  <verify>
    <automated>
      node --check get-stuff-done/bin/lib/plane-webhook-sync.cjs &&
      node --check get-stuff-done/bin/lib/planning-server.cjs
    </automated>
  </verify>
  <done>
    The inbound webhook path is implemented and covered by focused automated tests.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/plane-webhook-sync.test.cjs`
- optional disposable-server smoke check against `POST /v1/plane/webhook`
</verification>

<success_criteria>
Phase 49 is satisfied when Plane can push authenticated inbound events into GSD through the Planning Server, those events are normalized into a stable internal contract, and broker publication makes them available for downstream automation without introducing full bidirectional state mutation.
</success_criteria>

<output>
After completion, create `.planning/phases/49-plane-webhooks-incremental-sync/49-01-SUMMARY.md`
</output>

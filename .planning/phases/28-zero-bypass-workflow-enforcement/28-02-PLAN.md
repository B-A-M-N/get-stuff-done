---
phase: 28-zero-bypass-workflow-enforcement
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - get-stuff-done/bin/lib/sandbox.cjs
  - get-stuff-done/bin/gsd-tools.cjs
  - agents/gsd-executor.md
  - get-stuff-done/bin/lib/authority.cjs
  - tests/authority.test.cjs
autonomous: true
requirements: [ENFORCE-03, ENFORCE-04]

must_haves:
  truths:
    - "Files modified by Claude contain a hidden authority envelope signature"
    - "Manual modifications to protected files are detected by gsd-tools verify"
  artifacts:
    - path: "get-stuff-done/bin/lib/authority.cjs"
      provides: "Signing and verification logic for authority envelopes"
    - path: "get-stuff-done/bin/lib/sandbox.cjs"
      provides: "Bypass detection during runtime"
  key_links:
    - from: "gsd-executor"
      to: "authority.cjs"
      via: "Signing files on write"
---

<objective>
Implement Authority Envelopes and Bypass Detection to ensure all codebase changes are traceable to a GSD execution wave.

Purpose: Prevent "silent bypass" where the user or Claude modifies files outside the established planning/execution flow.
Output: A signing mechanism for authorized changes and a verification gate that flags unsigned modifications.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement Authority Envelope signing</name>
  <files>get-stuff-done/bin/lib/authority.cjs, get-stuff-done/bin/gsd-tools.cjs, tests/authority.test.cjs</files>
  <action>Create authority.cjs to generate and verify SHA-256 signatures for files based on the current Phase/Plan/Wave. Integrate into gsd-tools to sign files modified during execution.</action>
  <verify><automated>node tests/authority.test.cjs</automated></verify>
  <done>Files modified via gsd-tools are signed with authority envelopes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Bypass Detection in Sandbox</name>
  <files>get-stuff-done/bin/lib/sandbox.cjs, agents/gsd-executor.md</files>
  <action>Update sandbox.cjs to check for valid authority envelopes before allowing file operations. Update executor prompts to enforce signing requirement.</action>
  <verify><automated>gsd-tools verify-bypass src/sample.ts</automated></verify>
  <done>Workflow bypass attempts are detected and flagged.</done>
</task>
</tasks>
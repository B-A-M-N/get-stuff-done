# Second Brain configuration
# Override the default hashed database name to use the existing DB
GSD_DB_NAME=gsd_local_brain
# Use Unix socket for local Postgres connection (peer auth)
PGHOST=/var/run/postgresql
